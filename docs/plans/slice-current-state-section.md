# Slice — current-state-section — ‏בריף

> **‏תאריך**: 2026-08-18
> **‏סוג מסמך**: ‏בריף ביצועי לסלייס — ‏לא תוכנית טרום-בריף
> **‏סטטוס**: ‏טיוטה
> **‏אימות אביגיל**: ‏לא מאומת
> **Dispatch**: ‏מותר לאליעזר רק אם `אימות אביגיל = READY`
> **Complexity**: 3/10 (verifier: light)
> **‏תלויות (`depends_on`)**: [] ‏— ‏בנוי ישירות על main
> **‏Base**: main
> **‏Dev tip**: `5cbace3`

---

## §0 — Pre-flight

### ‏תלויות (‏חובה!)

‏אין תלויות. ‏הסלייס בנוי ישירות על `main` ‏ב-tip `5cbace3`. ‏אין branch פתוח בריפו,
‏ואין slice ממתין שנוגע באותם קבצים.

### Worktree

```bash
cd /home/user/Projects/brief-driven-slices
git worktree add .worktrees/current-state-section -b slice/current-state-section main
cd .worktrees/current-state-section
```

‏(‏אין `pnpm install` — ‏אין `package.json` ‏בריפו כלל. Markdown + ‏שני סקריפטי Python.)

> ⚠️ **‏תלות שאינה stdlib**: `scripts/distill.py` ‏עושה `import yaml` — **PyYAML ‏נדרשת**.
> ‏מאומת על המכונה: `python3 -c "import yaml; print(yaml.__version__)"` → `6.0.2`.
> ‏אל תניח stdlib-בלבד. ‏ההבחנה מתועדת ב-`docs/decisions/bds.md` (‏עוגן: `‏תיקון 2026-06-01`):
> ‏ספריית-python (PyYAML — ‏קיימת) ‏מול בינארי (`yq` — ‏חסר).

### ‏איך להריץ

- ‏גנרטור: `python3 scripts/generate-cli-configs.py all`
- ‏טסטים: `python3 tests/test_distill.py` · `python3 tests/test_path_substitution.py`
- **‏אין pytest ‏על המכונה.** ‏קונבנציית הריפו היא stdlib `unittest` ‏מורץ ישירות.

### Browser

‏לא רלוונטי — ‏אין FE, ‏אין שרת.

### Reading list

**must-read**:
- `briefs/BRIEF_TEMPLATE.md` — ‏יעד Commit 0. ‏שים לב למבנה §0…§9.
- `agent-definitions/prompts/avigail.md` — **‏מקור-האמת** ‏של אביגיל. ‏הבדיקות הן `## 1.`…`## 9.`
- `scripts/generate-cli-configs.py` — ‏קורא `agent-definitions/prompts/<agent>.md` ‏ומייצר את הנגזרים

**reference**:
- `plan-pitfalls.md` ‏קטגוריה 4 (`dropped-branch`) — ‏זו הקטגוריה שהסלייס מטפל בה
- `recommendations.md` ‏פריט 40 — ‏"‏תיקון ממצא הוא שאילתה"

### ‏מקורות חיצוניים

‏אין. ‏הסלייס נוגע רק בקבצי הריפו עצמו.

---

## §1 — ‏מטרה

‏מרדכי לא יוכל יותר לכתוב בריף שמתאר את השינוי בלי לומר קודם מה הקוד עושה **‏היום**.
‏התבנית תדרוש מלאי מפורש של הקיים, ‏אביגיל תבדוק אותו מול הקוד, ‏והפער שהיום מתגלה
‏בסבב 2–4 ‏יתגלה בסבב 0 — ‏אצל מי שממילא חייב למנות אותו.

---

## §1.5 — ‏מה קיים · ‏למה זה טעון תיקון · ‏מה התיקון

> ‏(‏הסלייס הזה מדגים את הסעיף שהוא מוסיף.)

### ‏א. ‏מה קיים היום

| # | ‏מה | ‏מצב נוכחי (‏נמדד ב-`5cbace3`) |
|---|---|---|
| 1 | `briefs/BRIEF_TEMPLATE.md` | ‏מבנה `§0` Pre-flight → `§1` ‏מטרה → `§2` Scope → `§3` ‏דיאגרמה → `§4` ‏קומיטים → `§5` DoD → `§6` Risks → `§7` Escalation → `§8` Complexity → `§9` ‏שאלות. **‏אין סעיף שמתאר את ההתנהגות הקיימת.** ‏קוד קיים מופיע רק כ-Reading-list ‏ב-§0 ‏וכ-anchors בתוך §4. |
| 2 | `agent-definitions/prompts/avigail.md` | ‏תשע בדיקות. ‏פקודת המנייה: `grep -c "^## [1-9]\." agent-definitions/prompts/avigail.md` → **9**. ‏האחרונה: ‏עוגן `## 9. ‏מוסכמות הפרויקט`. ‏הקרובה לענייננו: ‏עוגן `## 2. ‏Pseudo-code ‏לא מחסיר branches` — ‏אבל היא חלה **‏רק כאשר ה-brief מציג pseudo-code**. |
| 3 | `workflow.md:64` | ‏מצהיר `‏אביגיל מריצה 9 בדיקות`. |
| 4 | ‏הנגזרים של avigail | **‏חמישה** ‏קבצים: `agents/avigail.md`, `cli-configs/{opencode,qoder}/agents/avigail.md`, `cli-configs/codex/agents/avigail.toml`, `cli-configs/claude-code/agents/avigail.md`. |
| 5 | `scripts/generate-cli-configs.py` (‏עוגן: `choices=(`) | `choices=("all","codex","opencode","qoder")` — ‏מייצר **‏ארבעה** ‏מתוך החמישה. `claude-code` **‏אינו בגנרטור** ‏ומתוחזק ידנית. |
| 6 | `agent-definitions/prompts/mordechai.md` | ‏**‏אין בו הוראה לאמת טענות עובדתיות.** ‏מנייה: `grep -c "‏טענה עובדתית\|‏הפקודה שהפיקה" …` → **0**. ‏גם לו **‏חמישה** ‏נגזרים (`ls agents/mordechai.md cli-configs/*/agents/mordechai.*` → 5). |

### ‏ב. ‏למה זה טעון תיקון

| # | ‏למה לא מספיק |
|---|---|
| 1 | `dropped-branch` — "‏התוכנית לא מתחשבת בענף / ‏קורא / ‏תופעת-לוואי שהקוד הקיים מטפל בו" — ‏היא **145 ‏מתוך 378 ‏הממצאים האדומים בארכיון (38%)**, ‏הקטגוריה הגדולה ביותר בפער גדול. ‏מבנה שקופץ ‏מ"‏מטרה" ‏ל"‏Scope" ‏אינו מבקש את המלאי בשום שלב. |
| 2 | ‏בדיקה 2 מותנית בכך שיש pseudo-code. ‏סיווג של 145 ‏המקרים מראה ש**‏רק 21% ‏מהם נוסחו כמחיקה/‏החלפה**; ‏79% ‏הם פערים סמנטיים בהתנהגות (`"obsidianText ‏פולט רק op ‏ו-origin"`) ‏שאין להם pseudo-code להשוות אליו. ‏הבדיקה לא מגיעה אליהם. |
| 3 | ‏מספר שנגזר מרשימה ונכתב ביד — ‏`recommendations.md` ‏פריט 39. ‏אם הבדיקות ‏יהיו 10 ‏וההצהרה תישאר 9, ‏זה ממצא בסבב הבא. |
| 4 | ‏אין כאן פגם — ‏זו עובדה שהתיקון חייב לכסות: ‏עריכת המקור **‏חייבת** ‏להגיע לכל החמישה. |
| 5 | ‏זו המלכודת: `generate-cli-configs.py all` ‏יעדכן ארבעה ויחמיץ את `claude-code` ‏**‏בשקט**. ‏אם ה-DoD יסתמך על הגנרטור בלבד, ‏אדפטר אחד יישאר עם תשע בדיקות ‏ואף שער לא יתפוס. ‏זה בדיוק `gate-cannot-fail`. |
| 6 | **‏זה הפער שמבטל את פריט 1 אם לא ייסגר איתו.** ‏§1.5 ‏מבקש ממרדכי למנות את הקיים; `recommendations.md` ‏פריט 31 (`‏כל טענה עובדתית נכתבת עם הפקודה שהפיקה אותה`) ‏ו-`RAW` §25 (`‏מיקור-חוץ של האימות הוריד את הזהירות במעלה הזרם`) ‏קובעים **‏מי אחראי לאמת** — ‏והכלל הזה מעולם לא הגיע לפרומפט. ‏סעיף שנוסף בלי הכלל הוא **‏טופס שמלבין טענות מהזיכרון**: ‏מרדכי ימלא את §1.5 ‏כמו שמילא כל טענה אחרת. ‏זה גרוע מהיעדר הסעיף. |

### ‏ג. ‏מה התיקון

| ‏מכסה את | ‏מה |
|---|---|
| א1 | ‏סעיף חדש `§1.5` ‏בתבנית, ‏בין §1 ‏ל-§2. **‏ללא מספור-מחדש** ‏של §2–§9. |
| א2 | ‏בדיקה `## 10.` ‏אצל אביגיל — ‏מאמתת את §1.5 ‏מול הקוד. ‏בדיקה 2 **‏נשארת כמו שהיא** (‏היא עדיין נכונה כשיש pseudo-code). |
| א3 | `workflow.md:64` → `10 ‏בדיקות`. |
| א4+א5 | `generate-cli-configs.py all` ‏לארבעה, ‏**+ ‏בנייה ידנית של `claude-code`**, ‏+ ‏שורת DoD שסופרת את כל **‏חמשת** ‏הנגזרים — ‏לאביגיל **‏ולמרדכי**. |
| א6 | ‏כלל פריט 31 ‏נכנס ל-`mordechai.md` ‏תחת `## ‏כל הזמן`. **‏באותו commit** ‏של בדיקה 10, ‏כי זה אותו כלל: §1.5 ‏אומר "‏מנה את הקיים", ‏פריט 31 ‏אומר "‏עם הפקודה", ‏ובדיקה 10 ‏מאמתת ששניהם קרו. |

> **‏למה זה סלייס אחד ולא שלושה.** ‏שלושת הקבצים נושאים **‏כלל אחד** ‏בשלוש נקודות: ‏הטופס
> (‏תבנית), ‏הכותב (`mordechai`), ‏והשער (`avigail`). ‏הנחתה חלקית היא בדיוק `recommendations.md`
> ‏פריט 40 — "‏תיקון אתר אחד בזמן שהמחלקה שורדת" — ‏ובדוקטרינה זה מסוכן יותר מאשר בקוד:
> ‏סוכן קורא את החצי שנחת ‏ופועל לפיו.

**‏מה שנשאר במכוון מחוץ ל-scope**: ‏הכללת `claude-code` ‏בגנרטור, ‏ו-`scripts/lint-brief.js`. ‏ראה §2.

---

## §2 — Scope

| ‏פיצ'ר | ‏כן/לא | ‏לאן |
|------|------|------|
| `§1.5` ‏בתבנית הבריפים | ✅ | ‏בסלייס הזה |
| ‏בדיקה 10 לאביגיל + ‏הפצה ל-5 ‏הנגזרים | ✅ | ‏בסלייס הזה |
| ‏כלל פריט 31 ‏ב-`mordechai.md` + ‏הפצה ל-5 ‏הנגזרים | ✅ | ‏בסלייס הזה — **‏אותו כלל, ‏אותו commit** |
| `workflow.md` 9→10 | ✅ | ‏בסלייס הזה |
| `scripts/lint-brief.js` | ❌ | **‏slice נפרד, ‏ומותנה**: ‏פריט 34 ‏מציב תנאי — ‏רטרו על 117 ‏החוסמים מסוג `missing-symbol`/`wrong-path`/`missing-dependency` ‏לפני שנכתבת שורה. ‏זה סקריפט, ‏לא דוקטרינה |
| ‏הכללת `claude-code` ‏ב-`generate-cli-configs.py` | ❌ | **‏slice נפרד** — ‏שינוי בקוד הגנרטור, ‏דורש renderer ‏משלו ‏ו-target חדש. ‏כאן הוא נבנה ידנית ‏ו-DoD#5 ‏מאמת שהוא בסנכרון. |
| ‏שינוי בדיקות 1–9 ‏הקיימות | ❌ | ‏אין סיבה. ‏תוספת בלבד. |
| ‏מספור-מחדש של §2–§9 | ❌ | ‏ראה §6 ‏סיכון 1 |
| ‏עדכון בריפים היסטוריים ב-`docs/plans/` | ❌ | ‏התבנית חלה על בריפים **‏חדשים** |

---

## §3 — Architecture diagram

```
agent-definitions/prompts/avigail.md   ← ‏מקור-אמת, ‏נערך ידנית (‏בדיקה 10)
            │
            ├── generate-cli-configs.py all
            │        ├──► agents/avigail.md
            │        ├──► cli-configs/opencode/agents/avigail.md
            │        ├──► cli-configs/codex/agents/avigail.toml
            │        └──► cli-configs/qoder/agents/avigail.md
            │
            └── ‏ידנית (‏אינו בגנרטור!) ──► cli-configs/claude-code/agents/avigail.md

briefs/BRIEF_TEMPLATE.md   ← §1.5 ‏חדש (‏עצמאי, ‏אינו נגזר)
workflow.md:64             ← 9 → 10
```

---

## §4 — Commits ‏בסדר

### Commit 0 — `§1.5` ‏בתבנית (approach: manual)

**‏קבצים שמשתנים**: `briefs/BRIEF_TEMPLATE.md` — ‏הוספה בלבד.

**‏מיקום**: ‏מיד אחרי סוף §1, ‏לפני `## §2 — Scope`.
> grep anchor ‏לתחילת ההוספה: `## §1 — ‏מטרה`
> grep anchor ‏לסוף ההוספה: `## §2 — Scope`

**‏הטקסט המדויק להוספה** (‏אליעזר: ‏העתק verbatim):

```markdown
## §1.5 — ‏מה קיים · ‏למה זה טעון תיקון · ‏מה התיקון

> **‏למה הסעיף הזה קיים**: `dropped-branch` — "‏התוכנית לא מתחשבת בענף / ‏קורא /
> ‏תופעת-לוואי שהקוד הקיים כבר מטפל בו" — ‏היא **38% ‏מכל הממצאים החוסמים בארכיון**
> (145/378), ‏הגדולה ביותר בפער ניכר. ‏היא מרוכזת בבסיסי-קוד בוגרים ‏וכמעט נעדרת
> ‏מפרויקטים קטנים. ‏79% ‏מהמקרים **‏אינם ניתנים לגילוי ב-grep** — ‏הם דורשים
> ‏שמישהו **‏ימנה את הקיים לפני שיתאר את החדש**. ‏זה הסעיף שמכריח את המנייה.

### ‏א. ‏מה קיים היום

‏רשימה **‏שאפשר לספור מולה**. ‏שתי מגבלות, ‏ושתיהן מחייבות:

1. **‏התנהגויות, ‏לא קוד.** ‏אל תדביק גופי-פונקציות. "`hydrate` ‏מחיל היום ארבעה
   ‏כללי-גבול: X, Y, Z, W" — ‏לא המימוש. ‏קוד שמודבק לבריף הופך לטקסט שצריך להגיה
   ‏במקום קוד שאפשר להריץ.
2. **‏מוגבל למה שהסלייס נוגע בו** — ‏האתרים שמשתנים ‏ומי שצורך אותם. ‏לא סקירת מערכת.

- `<‏סמל/‏רכיב>` — ‏עושה היום: ‏(1) … ‏(2) … ‏(3) …
- ‏נקרא מ-: `<file:anchor>`, `<file:anchor>` — ‏**‏עם פקודת המנייה**: `grep -rn "<symbol>" <src>`

> ‏לכל סמל שהסלייס **‏מוחק, ‏מחליף, ‏או משנה לו חתימה** — ‏המנייה היא חובה, ‏לא רשות.
> ‏אם ה-grep מחזיר ‏4 ‏אתרים והרשימה מונה ‏2, ‏זה 🔴 ‏לפני שהתחלנו.

### ‏ב. ‏למה זה טעון תיקון

‏לכל פריט ב-‏א' — ‏שורה אחת: ‏למה הוא לא מספיק, ‏או מה נשבר בו.

> **‏זה הסעיף שנושא את המשקל.** ‏פריט מ-‏א' ‏שאין לו שורה כאן הוא אחד משניים:
> ‏מחוץ ל-scope (‏אמור זאת במפורש), ‏או **‏פריט ששכחת**. ‏בלי הסעיף הזה שתי הרשימות
> ‏חיות זו לצד זו בלי להיפגש — ‏וזו בדיוק הצורה שבה ענף נשמט.

### ‏ג. ‏מה התיקון

‏ההצעה. ‏כל התנהגות מ-‏א' ‏חייבת להיות מכוסה באחת משלוש: **‏משתנה** ‏(איך),
**‏נשמרת כמו שהיא** (‏במפורש), ‏או **‏נמחקת** (‏ומי שקורא לה טופל).
```

**Verification**:

```bash
grep -c "§1.5" briefs/BRIEF_TEMPLATE.md              # ‏היה 0 → ‏צריך ≥1
grep -c "‏מה קיים היום" briefs/BRIEF_TEMPLATE.md       # ‏היה 0 → ‏צריך ≥1
grep -n "## §2 — Scope" briefs/BRIEF_TEMPLATE.md      # ‏עדיין §2, ‏לא §3
grep -c "## §9" briefs/BRIEF_TEMPLATE.md              # ‏עדיין 1 — ‏אין מספור-מחדש
```

---

### Commit 1 — ‏הכלל: ‏מרדכי כותב-ומאמת, ‏אביגיל בודקת + ‏הפצה (approach: manual)

> ‏שתי עריכות-מקור **‏באותו commit**, ‏ואז regenerate אחד. ‏הן אינן ניתנות להפרדה: ‏ראה §1.5 ‏ב6.

**‏קובץ מקור א׳** (‏נערך ידנית): `agent-definitions/prompts/avigail.md`

**‏מיקום**: ‏אחרי סוף בדיקה 9, ‏לפני הסעיף הבא.
> grep anchor: `## 9. ‏מוסכמות הפרויקט`

**‏הטקסט המדויק להוספה**:

```markdown
## 10. §1.5 — ‏מלאי הקיים (‏חדש 2026-08-18!)

‏אם ה-brief ‏מכיל `§1.5` — ‏אמת אותו **‏מול הקוד**, ‏פריט-פריט. ‏זו הבדיקה הזולה ביותר
‏שלך: ‏ה-brief ‏**‏הצהיר** ‏מה קיים, ‏ואתה מאמת הצהרה ‏במקום לגלות בעצמך.

- **‏כל טענה ב-‏א' ‏נכונה?** ‏סמל, ‏שדה, ‏ענף, ‏ערך-ברירת-מחדל — ‏מול הקוד ב-base tip.
- **‏המנייה שלמה?** ‏לכל סמל שהסלייס מוחק/‏מחליף — ‏הרץ בעצמך את פקודת המנייה
  ‏שה-brief ‏מצטט. ‏החזירה יותר אתרים מהרשימה → 🔴 `dropped-branch`.
  ‏ה-brief ‏לא ציטט פקודה בכלל → 🟡.
- **‏כל פריט מ-‏א' ‏מופיע ב-‏ב'?** ‏פריט בלי שורת-הצדקה הוא ענף שנשמט, ‏אלא אם ‏ב'
  ‏אומר במפורש "‏מחוץ ל-scope".
- **‏כל פריט מ-‏ב' ‏מכוסה ב-‏ג'?** ‏משתנה / ‏נשמר / ‏נמחק — ‏אחת משלוש, ‏מפורשות.
  ‏התנהגות קיימת שנעלמה בין ‏ב' ‏ל-‏ג' ‏היא 🔴.
- **‏§1.5 ‏מכיל גופי-פונקציות?** → 🟡. ‏הסעיף אמור למנות התנהגויות. ‏קוד שמודבק
  ‏לבריף הוא קוד שצריך להגיה ‏במקום להריץ.

> **‏§1.5 ‏חסר לגמרי** ‏בסלייס שנוגע בקוד קיים → 🟡 ‏(‏לא 🔴): ‏ציין זאת ‏והמשך לבדוק
> ‏רגיל. ‏בסלייס greenfield ‏אמיתי — ‏אין קיים, ‏והיעדרו תקין.
```

**‏קובץ מקור ב׳** (‏נערך ידנית): `agent-definitions/prompts/mordechai.md`

**‏מיקום**: ‏בתוך `## ‏כל הזמן` — ‏סעיף הכללים התמידיים.
> grep anchor: `## ‏כל הזמן`

**‏הטקסט המדויק להוספה** (‏אליעזר: ‏העתק verbatim):

```markdown
- **‏כל טענה עובדתית נכתבת יחד עם הפקודה שהפיקה אותה.** ‏נתיב, ‏סמל, ‏ספירה, ‏גודל,
  ‏גרסה, ‏"X ‏מיוצא מ-Y" — ‏הפקודה נכתבת **‏לידה בבריף**. ‏טענה בלי פקודה צמודה היא
  ‏**‏זיכרון, ‏לא ידיעה** — ‏ומבפנים השניים מרגישים זהה. ‏אתה "‏יודע" ‏שסמל קיים כי
  ‏ראית אותו בסבב קודם, ‏מול עץ אחר.
- **‏אתה המאמת של מה שכתבת — ‏לא אביגיל.** ‏אביגיל היא קורא **‏זר**, ‏והערך שלה הוא
  ‏במה שאתה לא יכול לראות: ‏ההנחות שלך. ‏אם רוב מה שהיא מחזירה הוא סמלים שאינם
  ‏קיימים — ‏הפכת שומר-סף למעבדה, ‏ותת-אימתת. ‏הממצאים שלה אמורים **‏להפתיע**.
- **‏הזזת base ‏מבטלת את כל המסמך.** ‏בריף הוא טענה על עץ מסוים. ‏החלפת עץ — ‏כל
  ‏הטענות נבדקות מחדש, ‏לא רק הסעיפים שערכת. ‏אותו דבר אחרי כל סבב תיקונים.
- ❌ ‏אל תשלח לאביגיל **‏רשימת חשדות** ("‏בדקי את X, ‏הוא הכי סביר להיות שגוי").
  ‏מיקוד-**‏היקף** ‏מותר ורצוי ("‏אמתי את מה שהשתנה מאז r1"); ‏מיקוד-**‏חשד** ‏מפנה את
  ‏תשומת-לבה למה שאתה כבר בדקת, ‏ומחליש את השער.
```

**‏אחרי שתי העריכות — ‏הפצה. ‏שני מנגנונים, ‏ושניהם נדרשים:**

```bash
python3 scripts/generate-cli-configs.py all      # ‏מייצר 4: agents/, opencode, codex, qoder
# claude-code ‏אינו בגנרטור — ‏בנייה ידנית מהמקור:
#   cli-configs/claude-code/agents/avigail.md ‏מקבל את אותו בלוק, ‏בהתאמה לפורמט הקיים בקובץ
```

**Verification**:

```bash
grep -c "^## 10\." agent-definitions/prompts/avigail.md          # ‏היה 0 → 1
grep -c "‏הפקודה שהפיקה" agent-definitions/prompts/mordechai.md   # ‏היה 0 → ≥1
grep -rl "§1.5" agents/avigail.md cli-configs/*/agents/avigail.* | wc -l        # ‏צריך 5
grep -rl "‏הפקודה שהפיקה" agents/mordechai.md cli-configs/*/agents/mordechai.* | wc -l   # ‏צריך 5
python3 scripts/generate-cli-configs.py all                       # ‏פעם שנייה → "no changes"
git status --porcelain                                            # ‏ריק אחרי commit
```

---

### Commit 2 — `workflow.md` + walkthrough (approach: manual)

**‏קבצים שמשתנים**:
- `workflow.md:64` — `‏אביגיל מריצה 9 בדיקות` → `10 ‏בדיקות`
- `docs/walkthrough.md` — ‏ערך חדש בראש היומן (‏הוא הפוך-כרונולוגי)

**Verification**:

```bash
grep -c "9 בדיקות" workflow.md     # ‏היה 1 → ‏צריך 0
grep -c "10 בדיקות" workflow.md    # ‏היה 0 → ‏צריך 1
```

---

## §5 — DoD verifiable

| # | ‏בדיקה | ‏איך | ‏מצב בבסיס `5cbace3` |
|---|------|------|------|
| 1 | §1.5 ‏קיים בתבנית | `grep -c "§1.5" briefs/BRIEF_TEMPLATE.md` ≥1 | **0** |
| 2 | ‏שלושת תתי-הסעיפים | `grep -c "‏מה קיים היום\|‏למה זה טעון תיקון\|‏מה התיקון" briefs/BRIEF_TEMPLATE.md` ≥3 | 0 |
| 3 | ‏אין מספור-מחדש | `grep -c "## §9" briefs/BRIEF_TEMPLATE.md` = 1 **‏וגם** `grep -q "## §2 — Scope"` | ‏מתקיים |
| 4 | ‏בדיקה 10 במקור | `grep -c "^## 10\." agent-definitions/prompts/avigail.md` = 1 | **0** |
| 5 | **‏כל 5 ‏נגזרי אביגיל** | `grep -rl "§1.5" agents/avigail.md cli-configs/*/agents/avigail.* \| wc -l` = **5** | **0** |
| 5א | ‏כלל פריט 31 ‏במקור | `grep -c "‏הפקודה שהפיקה" agent-definitions/prompts/mordechai.md` ≥1 | **0** |
| 5ב | **‏כל 5 ‏נגזרי מרדכי** | `grep -rl "‏הפקודה שהפיקה" agents/mordechai.md cli-configs/*/agents/mordechai.* \| wc -l` = **5** | **0** |
| 5ג | ‏האיסור על רשימת-חשדות | `sed -n '/## ‏כל הזמן/,/^## /p' agent-definitions/prompts/mordechai.md \| grep -c "‏רשימת חשדות"` ≥1 | **0** |
| 6 | ‏גנרטור אידמפוטנטי | `python3 scripts/generate-cli-configs.py all` ‏פעמיים → `no changes`; `git status --porcelain` ‏ריק | ‏מתקיים |
| 7 | `workflow.md` ‏מסונכרן | `grep -c "9 בדיקות" workflow.md` = 0 **‏וגם** `grep -c "10 בדיקות"` = 1 | 1 / 0 |
| 8 | ‏בדיקות 1–9 ‏לא נגעו | `git diff main -- agent-definitions/prompts/avigail.md` — ‏רק hunk ‏אחד, ‏תוספת בלבד | ‏מתקיים |
| 9 | ‏רגרסיה | `python3 tests/test_distill.py` (39/39) · `python3 tests/test_path_substitution.py` (10/10) | ‏ירוק |

> ‏כל שער נמדד על `5cbace3` **‏לפני** ‏הכתיבה. ‏שערים 1, 2, 4, 5 ‏אדומים בבסיס —
> ‏הם באמת מודדים את הסלייס. ‏שערים 3, 6, 8, 9 ‏הם שערי-רגרסיה ‏וירוקים בכוונה.

---

## §6 — Risks + mitigations

| ‏סיכון | ‏מקור | ‏מיטיגציה |
|------|------|----------|
| **‏מספור-מחדש של §2–§9** ‏ישבור הפניות | `pre-run-decision-sheet` r2 ‏ממצא 3 — ‏הוספת DoD#4 ‏הזיזה מספור, ‏ו-§6 ‏הפנה לשורה הלא-נכונה | `§1.5` ‏ולא `§2`. DoD#3 ‏אוכף. |
| **`claude-code` ‏יישאר עם 9 ‏בדיקות ‏בשקט** | ‏קומיט `3df40ec`: *"cli-configs/claude-code ‏אינו חלק מ-generate-cli-configs.py ‏ולכן היה מתיישן בשקט"* | DoD#5 ‏סופר **5** ‏ולא 4. ‏זה השער היחיד שתופס את זה. |
| ‏בדיקה 10 תהפוך לטקס — §1.5 ‏נכון וחסר-ערך | `plan-pitfalls.md` ‏קטגוריה 11 `faithful-but-inadequate` | ‏הבדיקה דורשת **‏להריץ את פקודת המנייה**, ‏לא לאשר שהיא צוטטה. ‏זו ההבחנה בין נאמנות להלימה. |
| §1.5 ‏יתמלא בקוד מודבק | ‏מדידה: 420 ‏מתוך 1,047 ‏שורות בבריף אחד היו קוד | ‏מגבלה 1 בטקסט הסעיף + 🟡 ‏בבדיקה 10. |
| ‏אליעזר יערוך נגזר במקום מקור | ‏חמישה קבצים בשם `avigail` | §3 ‏מסמן מקור מול נגזרים; DoD#6 ‏(אידמפוטנטיות) ‏נופל אם נערך נגזר. |

---

## §7 — Escalation triggers

- ‏הפורמט של `cli-configs/claude-code/agents/avigail.md` ‏אינו מאפשר הוספה מקבילה לשאר — ‏עצור ושאל.
- ‏הגנרטור מייצר diff ‏מעבר לבלוק החדש (‏drift ‏שקדם לסלייס) — ‏עצור, ‏אל "‏תתקן" ‏אותו כאן.
- ‏אתה מוצא את עצמך ממספר מחדש סעיפים — ‏עצור. ‏זה מפורשות מחוץ ל-scope.

---

## §8 — Complexity score + verifier tier

| ‏פרמטר | ‏ניקוד |
|------|------|
| Refactor ‏של קוד קיים (‏עריכת מקור-אמת + ‏הפצה) | +1 |
| >5 files ‏ב->2 ‏ספריות (**‏10 ‏נגזרים** — ‏5 ‏אביגיל + ‏5 ‏מרדכי — ‏+ ‏4 ‏מקורות) | +1 |
| ‏Pure logic, ‏אין IO | -2 |
| ‏מנגנון-הפצה עם מסלול ידני אחד | +2 |
| ‏אין call sites ‏שנשברים | -1 |

**Score**: **3** / 10 → **`verifier-slice-light`**

**‏Verifier-phase**: ‏אין.

> **‏הערה על ה-tier**: ‏אין כאן runtime במובן הרגיל, ‏אבל **‏יש מה להריץ** —
> ‏הגנרטור, ‏האידמפוטנטיות, ‏ספירת חמשת הנגזרים ‏ושני קבצי הטסטים. ‏כלב מריץ אותם
> ‏בפועל. ‏זה **‏לא** ‏סלייס בלי אימות-ריצה.

---

## §9 — ‏שאלות פתוחות

| # | ‏שאלה | ‏ברירת מחדל | ‏חוסם? |
|---|------|----------|------|
| 1 | `§1.5` ‏או `§1א`? | `§1.5` — ‏ASCII, ‏לא שובר grep ‏בעברית | ❌ |
| 2 | ‏האם §1.5 ‏חובה בכל בריף או רק ב"‏נוגע בקוד קיים"? | ‏חובה כשנוגעים בקיים; ‏ב-greenfield ‏מותר "‏אין קיים". ‏בדיקה 10 ‏מנסחת כך. | ❌ |
| 3 | ‏האם להוסיף §1.5 ‏גם ל-`docs/plans/` ‏ההיסטוריים? | ‏לא — ‏חל על חדשים בלבד | ❌ |
| 4 | ‏האם `mordechai.md` ‏צריך הנחיה מקבילה? | **‏נסגר — ‏כן, ‏ובאותו commit.** ‏החלטת המשתמשת 2026-08-18: ‏זה כלל אחד ולא שלושה. ‏ראה §1.5 ‏ב6 ‏ו-Commit 1 ‏קובץ-מקור ב׳. | — |
| 5 | ‏הרטרו של `lint-brief` (‏פריט 34) — ‏להריץ לפני או אחרי הסלייס? | **‏אחרי.** ‏הוא לא חוסם: 117 ‏החוסמים כבר בארכיון ‏ולא ילכו לשום מקום. | ❌ |

---

## ‏סטיות מהתכנון (‏מתעדכן ע"י executor ‏תוך כדי)

- ...
