---
id: "#1"
date: 2026-08-23
area: verifier
reported_by: משתמש
severity: blocker
---

# ‏#1 — לולאת ה-plan-gate אינה יכולה להתכנס: ‏READY בלתי-השיג, התקרות לא נאכפות, והלקחים לא מוחזרים לצנרת

## התסמין — במילים של מי שראה

> "עדיין הפיתוח מאוד מאוד איטי, וכל משימה קטנה נמשכת שעות. משהו במנגנון לא עובד."

## שחזור

```
1. מרדכי כותב בריף ומריץ אביגיל (חובה, אוטומטי).
2. agents/mordechai.md §plan-gate: ‏"fix + הרץ אביגיל שוב… עד READY".
3. agents/avigail.md:312: ‏"❌ לחשוב 'אולי הבריף בסדר ואני טועה'…
   אם לא מצאת — בדוק שוב". ⇒ אפס-ממצאים אסור לה מבנית.
4. כל ממצא — כולל קוסמטי — מונע READY ⇒ סבב נוסף. היציאה היחידה: התקרה.
5. בפועל גם התקרה לא עוצרת (control-roles: ‏7 סבבים מול תקרת 5 במשימה).
```

## ראיות

- **‏replay-quiet (23/08):** בריף 10:32→16:53, ‏3 סבבי אביגיל, ‏3 שכתובים,
  ‏~6.5 שעות, **אפס שורות קוד**. שניים מארבעת חוסמי r2 **הופרכו** ב-r3
  (‏probe שגוי · טסט שנפל רק בגלל הרצות מקבילות) — חוסמי-שווא גררו שכתוב
  מלא של בריף נכון. ‏`autonomous-runs/runs/2026-08-23-replay-quiet-avigail{,-r2,-r3}.md`
- **‏control-roles (22–23/08):** ‏12 גרסאות בריף, ‏7 סבבי אביגיל, ‏~10 שעות תכנון
  מול ‏~40 דק' ביצוע. ‏docs-repo ‏`aa0b9d9..08655bb`.
- **‏רוחבי:** ‏596 דוחות אביגיל על 273 סלייסים — ‏זנב של 36 סלייסים (13%)
  ‏צרך 4–13 סבבים (~197 סבבים). ‏כלב, לעומת זאת: ‏261 מ-265 בסבב יחיד.
- **‏ריצה 7:** ‏5 סבבים מוצו בלי להתכנס; ‏9 מ-10 חוסמים ברתמת-האימות; ‏סקירה
  ‏בדיעבד מצאה 2 פגמים שכל הסבבים פספסו ("עקביות אינה נכונות").
- **‏ריצה 8 הוכיחה את התיקון במדידה** (‏סבב יחיד ממוקד-הנחות) — ‏ו-`MISSION_TEMPLATE.md`
  ‏§5 עדיין 3/5 (‏לא נגעו בו מאז `364cdc7`, ‏21/08). ‏פקודת replay-quiet נכתבה
  ‏אחרי הלקח — ‏עם התקרות הישנות.
- ‏`bds init` ‏נספר כהתערבות-צנרת **חמש ריצות ברציפות** (‏4→8) ולא בוצע.

## היקף

כל סלייס בזנב משלם שעות של סבבי-אימות שסבב-הביצוע היה מקצר; בריפים
נכונים משוכתבים בגלל חוסמי-שווא; הבסיס זז בזמן התכנון הארוך ומייצר
סבבי-ריקבון נוספים (מעגל שמזין את עצמו). הפירוט המלא, כולל היסטוגרמות
וטבלת לקח-מול-יישום: ‏[[../investigations/2026-08-23-slow-mechanism-diagnosis.md]]

## מה ידוע על השורש

נחקר ואומת (23/08). חמישה מנגנונים, כולם מתועדים בחקירה; מנגנון-העל תועד
כבר ב-`case-studies/RAW-2026-08-verification-cost.md` §25: ‏"המאמת הוא קו
ההגנה האחרון, לא הראשון" — והשורש התפעולי הוא ש**התיקונים להגדרות-הסוכנים
נדחים שוב ושוב** ("שינוי פרומפט דורש slice", ‏פריט 38-ב — מופיע כ"טרם יושם"
בשני case-studies וגם היום).

## פקודת-הסגירה

```bash
# ‏1 — התבנית עברה לברירת-מחדל של סבב-תכנון יחיד:
grep -A4 '§5' autonomous-runs/MISSION_TEMPLATE.md | grep 'אביגיל'
#     ⇒ סבב אחד לפני dispatch (או תקרה עם הסייג "‏🔴 חדש שאינו תוצר התיקון הקודם")

# ‏2 — דוקטרינת ה-100% הוסרה:
grep -c 'בדוק שוב את' agents/avigail.md        # ⇒ 0

# ‏3 — נאכפות (התנאי המכריע): שתי הריצות הבאות בפנקס מציגות
#     plan-gate של סבב אחד לפני ביצוע, בלי חריגת-תקרה,
#     וזמן-קיר מכתיבת הבריף עד dispatch ≤ ‏2 שעות — מתועד בדוח-הריצה.
```
