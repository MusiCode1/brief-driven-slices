# Distillation Report — 2026-07-19

> **תאריך**: 2026-07-19
> **data.json**: `distillations/2026-07-19-data.json`
> **דוחות שנכללו**: 28 דוחות (avigail: 7, calev: 21)
> **מאז הזיקוק הקודם**: 28 דוחות חדשים מ-2026-06-27 (שכיסה 224). פרויקטים:
> drive-coding (16), obsidian-web (11), be-diag-harness (1).
> **חידוש הסבב**: לראשונה זוקקו גם **סשני-המאמת הגולמיים** (142 טרנסקריפטים), לא רק
> הדוחות. ראה חלק 5 — זו הכותרת האמיתית של הסבב.

---

## חלק 1 — מבט-לאחור: הכללים מהדוח הקודם

> **מקור**: `delta` מה-data.json. אזהרת-מדגם: הקורפוס הנוכחי (28) קטן ושונה-פרויקטית
> מהקודם (224), אז "נעלם" הוא לרוב אפקט-מדגם ולא הכחדה. אבל דפוס שנעלם מ**שני**
> המאמתים גם יחד = סיגנל אמיתי.

| כלל מהזיקוק הקודם | קטגוריה | מגמה | פרשנות |
|-------------------|---------|-------|---------|
| **כלל ה-anchor** (לא לעגן בריפים במספרי-שורות) | `wrong-line-number` | 🚫 63 → **0** | **הניצחון הגדול.** נעלם משני המאמתים. הכלל שהוכנס בזיקוק הקודם + `BRIEF_TEMPLATE`/`avigail.md` עבד. |
| `outdated-risk` | avigail | ⬇️ 44 → 2 | ירד חד (חלקית מדגם). |
| `naming-inconsistency` | calev | 🚫 32 → 0 | נעלם אצל כלב; אצל אביגיל 2. |
| `dropped-branch` | calev | 🚫 31 → 0 | נעלם אצל כלב; אצל אביגיל 1. |
| `missing-dependency` | calev | 🚫 15 → 0 | נעלם. |
| `reload-reconnect` (מועמדת שנוספה) | calev | ➡️ 3 → 1 | ממשיכה לצבור טפטוף. עדיין מועמדת, לא קנון. |

**מסקנה**: כלל ה-anchor הוא הצלחת-הזיקוק המובהקת — הבעיה הדומיננטית ביותר (63 ממצאים)
ירדה לאפס. שאר הירידות מעורבות עם אפקט-מדגם.

---

## חלק 2 — התפלגות נוכחית

### אביגיל (plan-verifier)

| severity | count |
|----------|-------|
| confusion | 6 |
| minor | 5 |
| regression | 2 |

| category | count | reports תורמים |
|----------|-------|----------------|
| `unique` | 5 | drive-coding/localhost-bind-hotfix, obsidian-web/folder-vault-blob-uri |
| `naming-inconsistency` | 2 | drive-coding/be-lifecycle-hardening, obsidian-web/extract-runtime-server |
| `missing-symbol` | 2 | drive-coding/be-lifecycle-hardening |
| `outdated-risk` | 2 | drive-coding/be-lifecycle-hardening, obsidian-web/extract-runtime-server |
| `dropped-branch` | 1 | drive-coding/localhost-bind-hotfix |
| `wrong-path` | 1 | obsidian-web/extract-runtime-server |

**Hitrate**: 4/7 דוחות עם ממצאים (57%). ממוצע: 1.86 ממצאים/דוח.
**Verdicts**: READY 4, USABLE-AFTER-FIX 3.
→ **3/7 בריפים (43%) נדרשו לתיקון לפני dispatch** — כולל 2 regressions ו-2 missing-symbol
(createLogger לא-מיובא, אין withTimeout) שהיו מקרקעים את אליעזר. **אביגיל ממשיכה להחזיר ROI.**

### כלב (runtime-verifier)

| severity | count |
|----------|-------|
| minor | 18 |
| info | 1 |

| category | count | reports תורמים |
|----------|-------|----------------|
| `unique` | 13 | obsidian-web/cf-*, opfs-ux, service-worker-offline, drive-coding/slash-menu, be-lifecycle |
| `spec-drift` | 4 | obsidian-web/cf-mobile-seed, cf-mobile-serve, cf-worker-proxy, opfs-ux |
| `library-compat` | 1 | drive-coding/slice-acp-stack-upgrade-commit3 |
| `reload-reconnect` | 1 | obsidian-web/service-worker-offline |

**Hitrate**: 10/21 דוחות עם ממצאים (48%). ממוצע: 0.9 ממצאים/דוח.
**Verdicts**: GO 20, PARTIAL 1.
→ **אפס ממצא חוסם בכל 21 הדוחות.** ה-PARTIAL היחיד (slash-menu-native-fix) **לא היה באג** —
הודעה כנה ש-Chromium headless חסר libs ולא אִפשר אימות ויזואלי. כלב לא תפס אף defect בסבב הזה.

---

## חלק 3 — חדשות: טקסונומיה מתפתחת

### קטגוריות לא-קנוניות

| category | verifier | count | פרשנות |
|----------|----------|-------|---------|
| `unique` | calev | 13 (68% מהממצאים) | **חוזר מהזיקוק הקודם** (שם 48%). הטקסונומיה של כלב דקה מדי מחוץ ל-UI. לא לקנון (זה ה-fallback), אבל סיגנל מתמשך. |
| `unique` | avigail | 5 (38%) | זנב רגיל. |

**אין קטגוריה חדשה שראויה לקנון בסבב זה** — המדגם קטן מדי (28) והזנב הוא singletons.
`spec-drift` ו-`library-compat` כבר קנוניות (נוספו בעבר). `reload-reconnect` ממשיכה כמועמדת.

**חוב-טקסונומיה שחוזר**: כלב מייצר 68% `unique`. זו הפעם השנייה שהדפוס עולה. שווה
לשקול בזיקוק הבא הידוק הוראת-הטקסונומיה ב-`calev.md`, אך רק אם המדגם הבא יאשש.

---

## חלק 4 — עדכוני-קטלוג

> **החלטה מודעת: אין שכתוב קטלוגים בסבב זה.** הקורפוס (28) קטן מכדי להצדיק שינוי מבני
> אחרי השכתוב-המלא של הקודם (224). הפעולה היחידה: אישוש שהכלל שהוכנס עבד.

### plan-pitfalls.md (אביגיל)
- **ללא שינוי מבני.** כל הקטגוריות הנוכחיות כבר בקטלוג.

### patterns.md (כלב)
- **ללא שינוי מבני.**
- **אישוש**: כלל ה-anchor (`wrong-line-number` → 0) — הצלחה מאומתת. להשאיר.
- **`reload-reconnect`**: נשארת מועמדת (1 נוסף). לא מקדמים לקנון עד 12+.

---

## חלק 5 — מחקר-מאמץ: מה המאמתים *באמת* עושים (חדש)

> **הרקע**: המשתמשת ביקשה להבין מה אביגיל/כלב עושים בפועל — הם רצים הרבה זמן, ולא היה
> ברור אם המאמץ מוצדק. חילצנו את הטרנסקריפטים הגולמיים לניתוח.

### תשתית שנבנתה
- **מיקום הגלם**: `~/.claude/projects/<אב>/<סשן>/subagents/agent-<id>.jsonl` — הכלי `Agent`
  משגר subagents ברקע, כל אחד עם טרנסקריפט נפרד לפי agentId. 218 טרנסקריפטים, מהם 119 מאמתים.
- **כלי קבוע**: `scripts/extract_sessions.py` — ממפה agentId→סוג מסשני-האב, מחלץ פר-סשן
  {מטא, היסטוגרמת-כלים, רצף-צעדים, נרטיב}, **מסנן גופי-פלט ענקיים**, שומר סיגנל בלבד.
- **קורפוס**: 142 סשנים → `main/reports/_sessions/` (1.9MB, **gitignored — מקומי/פרטי**).

### פרופיל-המאמץ (142 סשנים)

| סוכן | n | median קריאות | median משך | מודל | Bash% |
|------|---|---------------|-----------|------|-------|
| **avigail** | 97 | 14 | 4m17s | — | 70% |
| **calev** (רגיל) | 24 | 41 | 8m56s | Sonnet | 81% |
| **calev-heavy** | 21 | 56 | **16m31s** | **Opus** | 79% |

### התזה (מגובת-מאמץ + payoff)

1. **calev-heavy = מוקד-העלות, לא מצדיק את עצמו ברוב המקרים.** 16.5 דק' Opus × 56 קריאות,
   ובזיקוק: 8/8 heavy → GO, אפס ממצא חוסם (2 עם 0 findings). **Over-provisioning.** ה-gate
   של מורכבות-8 נדיב מדי. חריג מוצדק: `cf-worker-proxy` (SSRF+base64) — שם heavy הגיוני.
2. **calev רגיל = אזור-אפור.** payoff מדוד קרוב-לאפס (0 באגים), **אבל** זול (Sonnet, 9 דק')
   ו**באמת מריץ קוד** — פילוח ה-Bash: **317 הרצות-אמת (bun/curl/tsc) מול 240 exploratory**.
   גלאי-עשן זול שלא צפצף — לא מסירים, מכיילים.
3. **avigail = ROI מוכח.** יעילה (4 דק', 14 קריאות) ותופסת תיקוני-חובה אמיתיים (3/7). **לא לגעת.**

### תצפית-תהליך
רוב סשני-כלב יצאו `mode=unspecified` — ה-prompt כלל לא הכריז phase/light/heavy. משמעת
ה-mode רופפת → כלב מחליט לבד כמה לעבוד (שונוּת 0–94 קריאות באותה דרגה).

---

## המלצות לתהליך

1. **Slice מומלץ הבא: "כיול-mode של כלב"** — (א) gate צר יותר ל-heavy (מורכבות-8 לבד לא
   מספיק — לדרוש סיכון-סוג: security/concurrency/data-integrity); (ב) `mode` מפורש-חובה בכל
   dispatch של כלב; (ג) default=רגיל. ROI גבוה — heavy הוא הרכיב היקר ביותר בכל הצנרת.
2. **חילוץ-סשנים כקלט-זיקוק קבוע.** `extract_sessions.py` הופך את "כמה עבדו" למדיד. להריץ
   בכל זיקוק לצד `distill.py`.
3. **הגלם מתפוגג — נחוץ progress/todo-file.** ה-store המקומי מתרוקן (opencode.db כבר נוקה).
   החילוץ נותן forensics-בדיעבד, לא נראות-חיה ולא עמידות. מחזק את רעיון קובץ-ה-progress הקבוע.
4. **הזיקוק שוב רץ ידנית** — הטיימר עדיין לא הותקן. עקבי עם המלצת הזיקוק הקודם: **קדנציה
   ידנית** (~כל 20 slices) אמינה יותר מ-systemd שלא מותקן.
