# Distillation Report — 2026-08-31 — זיקוק-מתכנן

> **תאריך**: 2026-08-31
> **מוקד**: עידן הריצות האוטונומיות 19–31/08 (ריצות 1–30 בפנקס) — **מקור-הכשל זז במעלה-הזרם, מהמבצע אל המתכנן**
> **קלט**: 34 דוחות-ריצה (`autonomous-runs/runs/`) · **172 דוחות-אימות** מ-`main/reports/drive-coding/` עם `date ≥ 2026-08-19` (בהם **100 ממצאי-blocker**) · `OPEN-GAPS.md` + `README.md` · יומן-ההחלטות `docs-repo/drive-coding/plans/autonomous-decisions-log.md` (HR-01..HR-05, D-01..D-11)
>
> ⚠️ **סטייה מ-`TEMPLATE-report.md`, במכוון**: התבנית בנויה לזיקוק כמותי של `distill.py` על כל הריפוזיטוריים (עם `data.json`). הזיקוק הזה הוא **סיווג-לפי-מקור-כשל** על חלון 19–31/08 בלבד, והשאלה שלו שונה: לא "אילו קטגוריות-תוכן חוזרות" אלא **"מי ייצר את הכשל — מתכנן, מבצע, מאמת או צנרת"**. אין `data.json`; כל המספרים נגזרו בפרסינג של ה-front-matter (הפקודות בגוף הדוח) וניתנים לשחזור.

---

## חלק 0 — הטענה, במשפט אחד

**רק 3 מתוך 100 ממצאי-ה-blocker של אוגוסט הם באגי-מבצע. 96 מצביעים על הבריף (92 ממצאי-אביגיל + 4 חוסמי-כלב שגופם טענת-בריף מופרכת) — ו-63 מהם הם הצהרה/שער של המתכנן שנראו כמדודים ולא נמדדו.** הטקסונומיה הקיימת ב-`patterns.md` (כשלי-executor) מתארת את הזנב, לא את הגוף.

הראיה המרוכזת ביותר לכך היא של המתכנן על עצמו:

> "בריצה 16 **שתיים** מארבע ההנחות שנפלו היו ב-§11 שלי; בריצה 17 — **שלוש מתוך שלוש**. כולן נראו כמדידות; בפועל היו קריאת-קוד או הרצה בסביבה שאינה זו של המבצע." — `autonomous-runs/README.md`
>
> "בשני הסבבים הכשל היה זהה באופיו: **בניתי הכרעה על התנהגות-מערכת שלא מדדתי**." — HR-05, `autonomous-decisions-log.md`

---

## חלק 1 — מבט-לאחור: הכללים מהזיקוק הקודם (03/08)

| כלל (03/08) | מגמה | פרשנות |
|---|---|---|
| הוספת `unrun-claim` / `gate-cannot-fail` / `intermediate-state-unverified` ל-`plan-pitfalls.md` | ➡️ **נקלט בסיווג, לא בהתנהגות** | שתי הקטגוריות הראשונות הפכו ל-**#1 ו-#3** בממצאי-ה-blocker של אוגוסט (37+12 מתוך 100). המאמתים משתמשים בהן בדיוק; המתכנן המשיך לייצר אותן עד 28/08 |
| "מספרי-שורות — עוגן ב-symbol" (`wrong-line-number`) | ⬇️ נעלם כמעט | 0 blockers בקטגוריה בחלון. הבריפים עברו לעוגני-symbol+grep |
| `lint-brief` מכני (פריט 34) | ⬜ לא יושם | מה שכן הוריד את המספרים (29–31/08) הוא **פרה-בריף עם מדידות חיות**, לא לינטר |

**הלקח מהשוואת-הזמן**: הכלל הטקסטואלי ("שער חייב להיות בר-כישלון") תועד ביולי־אוגוסט בשלושה מקומות ולא מנע 37 מופעים. מה שעבד נמדד בטבלת-הימים למטה.

---

## חלק 2 — הספירה: 100 ממצאי-blocker לפי מקור-הכשל

> שיטת המדידה: פרסינג front-matter של כל `main/reports/drive-coding/**/*.md` עם `date ≥ 2026-08-19`; ממצא = ‏`severity: blocker` (או `gate-failure`). ‏172 דוחות · 52 עם ממצא-blocker · 100 ממצאים (92 אביגיל · 8 כלב). פסקי-דין בחלון: אביגיל — 78 USABLE-AFTER-FIX · 15 NEEDS-REWORK · 9 READY; כלב — 47 GO · 14 PARTIAL · 3 NO-GO (‏non-GO ‏26.6%).

| מקור-הכשל | ספירה מתוך ה-100 | מהם |
|---|---|---|
| **(1) מתכנן — הצהרה מדודה-למחצה / שער-שלא-הורץ** | **63** | ‏אצל אביגיל: `gate-cannot-fail` ‏37 · `unrun-claim` ‏12 · ‏10 ממצאי `faithful-but-inadequate` שגופם הפרכה של טענה עובדתית בבריף. ‏אצל כלב: ‏4 חוסמים שגופם טענת-בריף (‏ר' 2.1 סיפא) |
| **(2) מתכנן — בחירת-בסיס/עץ שגויה** | **2** (+1 ביומן, מחוץ ל-100) | `live-context-avigail` B1 · `live-ears-avigail` B1 · ‏D-11 ביומן (לא ממצא-דוח) |
| **(3) מבצע — קטגוריות patterns.md** | **3** (+6 אירועי-דיווח-כוזב בריצות, מחוץ ל-100) | `sse-liveness-calev` · `live-ears-calev` B1,B2 |
| **(—) מתכנן — פגמי-תכנון אחרים** (מכוסים ב-`plan-pitfalls.md` הקיים) | **31** | dropped-branch ‏5 · missing-dependency ‏3 · faithful-but-inadequate ‏17 · אחר ‏6 |
| **פגם-עיצוב שנחשף בריצה** (כלב) | **1** | `live-secretary-calev` B1 |
| **(4) מאמת — ממצא-אימות שגוי** | **מחוץ ל-100**: ‏4 (+2 של מתזמר) | ‏תועדו בדוחות-הריצה, לא כ-blockers במאגר: replay-quiet r2 ×2 · frame-ingest (מתאם) · כלב ריצה 4 (InboxSMS) |
| **(5) צנרת/מכניקה** | לא נספר כאן | שייך ל-`OPEN-GAPS.md` — ר' §2.5 |

‏סה"כ בתוך המאה: ‏63+2+3+31+1 = ‏100. ‏קטגוריות (4)+(5) חיות מחוץ למאגר-ה-blockers ונמנות בנפרד.

### 2.1 — קטגוריה (1): מתכנן, מדוד-למחצה — הראיות

הגרעין המכני (49): **כל** ‏37 ה-`gate-cannot-fail` ו-12 ה-`unrun-claim` הם שערים/טענות שהמתכנן כתב ולא הריץ. דוגמאות מובהקות עם ציטוט:

| דוח | הציטוט (מקוצר) |
|---|---|
| `ttl-ownership-avigail-r2` B1 | "L9's greps return 0 **on correct code — PROVEN LIVE**. raw frames are JSON-escaped" |
| `ttl-ownership-avigail-r3` B2 | "BOOT check cannot fail — 'listening' is logged unconditionally even on EADDRINUSE" |
| `msg-diagrams-avigail` B3 | "DoD 6 (mount MessageBubble in jsdom) **cannot run** on the repo test config — mount() throws lifecycle_function_unavailable" |
| `live-ears-avigail` B5 | "לשער-ההד אין מפיק ל-outputLevel — אין נתיב השמעה בסלייס ⇒ DoD 10 ו-14 אינם ניתנים להרצה" |
| `live-secretary-avigail-delta` B1 | "מדידת ה-AEC אינה ניתנת להרצה על הוסט של אליעזר — ‏`/dev/snd` מכיל seq+timer בלבד" |
| `wire-rec-compress-avigail` B1 | "מוטציה D מתוחמת למסלול apply בעוד הבקרה-השלילית רצה dry-run ⇒ שער-מוטציות שלא יכול ליפול" |
| `msg-coalesce-avigail` B2 | "DoD 4 gate is **already RED on the base** … contradicting §0's 'no svelte-kit sync needed'" |
| `ws-transport-dedupe-calev` (NO-GO) | "DoD #10 alternate gate does not fire — all FE ws tests mock @drive-coding/acp-wire" |

‏ריצה 2 ניסחה את המכנה המשותף כבר ב-19/08: **"כל תשעת החוסמים הם אותו דבר: קוד שנכתב מהזיכרון ולא הורץ"** (`runs/2026-08-19-ttl-ownership.md`).

עשרת ה-`faithful-but-inadequate` שנספרו כאן (טענה עובדתית בבריף שהופרכה במדידה): ‏`frame-ingest-unify-avigail` B1 ("אוצר-המילים של חוט ה-HTTP רחב מזה של `#onSessionUpdate` … יימחקו בשקט") · `meta-passthrough-avigail` B1 ("HTTP wire **never** emits sessionUpdate tool_call") · `msg-coalesce-avigail` B1 ("the §3(ג) claim … is **measurably false**") · `fe-defaults-avigail` B1 ("היפוך DEFAULTS מפיל 8 טסטים … **הבריף מצהיר הפוך** בארבעה מקומות") · `explicit-disconnect-avigail` B1 ("broadcaster.close() without epoch++ makes SSEReader … re-take ownership within ~1s — the explicit button undoes itself") · `explicit-disconnect-avigail-delta` B1 ("the FE holds no ownership epoch anywhere — the SSE parser drops `id:` lines"; ‏שכנתה B2 — "D2's headline justification is **false**: on the F5 path the epoch does NOT move" — ‏מסווגת `unrun-claim` ‏ונמנית שם) · `live-contract-gemini-avigail` B1 ("constraints.config replaces the client config … dead parameters") · `live-ears-avigail-delta` B2 ("נמדד: אפס engines מייבאים adapter") · `live-secretary-avigail` B2 ("המפיק של mouth הוא Speaker — ו-Commit 5 מכבה TTS ב-Live; הצרכן לעולם לא ידלק") · `playlist-invariants-avigail` B3 ("I2 is **already false on the base**").

**וגם בשער-הריצה החץ מצביע למעלה**: מתוך 8 ה-blockers של כלב באוגוסט, ‏4 הם טענות-מתכנן — ‏`live-ears-calev` B3 ("ל-ear/mouth אפס צרכני-ייצור — **הבריף מצהיר ב-DoD 7 שיש להם צרכנים**, והמדידה מפריכה") · `live-secretary-calev` B2 ("optionId is a free string rather than the enum **the brief assumed**") · `live-contract-gemini-calev` B1 (‏probe שהמתכנן תכנן "מדביק" BE ישן ⇒ ‏DoD 14 ‏false-PASS) · `ws-transport-dedupe-calev` B1 (שער שנבחר ולא נבדק שהוא יורה). ‏דוח-ריצה 18 סיכם: "מתוך 5 הופעות דפוס-השער, **4 היו תחזיות של המתכנן** … הסיכון המרכזי אינו ביצוע שגוי — הוא תכנון שמצהיר כיסוי שלא נמדד."

מחוץ ל-drive-coding, אותו דפוס ב-InboxSMS: ‏`wrangler d1 list` ‏עם `num_tables: 0` "נלקח כראיה בלי אימות מול `sqlite_master`" (תיקון עובדתי בראשי דוחות ריצות 4–5) · ‏`toUtcIso` תלוי-TZ שנתפס רק בסקירת-הרצה אחרי ש**חמישה** סבבי-plan-gate התכנסו על עקביות ("**עקביות אינה נכונות**", ריצה 8) · ‏מנגנון-חנק TTL שלם ש"המדידה הפריכה" (ריצה 5, ‏KV דוחה `expirationTtl < 60`).

### 2.2 — קטגוריה (2): בסיס/עץ שגוי — 2 ממצאים + אירוע-יומן

- ‏`live-ears-avigail` B1: "Base מיושן — הפקודה ב-§0 פותחת worktree מ-`slice/live-contract-gemini` (58209f30), שאינו מכיל את `dfbb4078` — **בדיוק תיקון ה-connect שה-§0 מצהיר עליו כמחייב**".
- ‏`live-context-avigail` B1: "Base מיושן — קומיט אחד מאחורי ענף-ההרצה, והדלתא נוגעת בדיוק בקבצים שהסלייס עורך".
- ‏D-11 (יומן, `transport-polish`): "`slice/view-switch` אינו קצה השרשרת. הקצה … **14 קומיטים קדימה**" — והלקח שנכתב שם הוא ההגדרה של הקטגוריה: **"אימות מאמת את הבריף מול העץ שנמסר לו — לא את בחירת העץ. את זה חייב לבדוק המתכנן, פעם אחת, לפני הכל."** ‏שלושה סבבי-אימות (23 ממצאים) לא יכלו לראות את זה.

(‏"הבסיס זז באמצע הריצה" — ‏replay-quiet, ‏control-roles "הוחלף באמצע, 208 קומיטים" — סווג **צנרת**, כי התיקון היה מכני: worktree נפתח מענף-ההרצה, לא מענף-אינטגרציה חי.)

### 2.3 — קטגוריה (3): מבצע — 3 ממצאי-דוח + 6 אירועי-דיווח

באגי-מימוש אמיתיים שנתפסו בשער-הריצה:

1. `sse-liveness-calev` (NO-GO): "Commit 4 breaks **ALL** remote/HTTP SSE connects in a real browser — bare setInterval/clearInterval stored as class fields throw 'Illegal invocation'" — ‏patterns קטגוריות 1+4 (מוקים הסתירו; ‏30/30 ירוקים).
2. `live-ears-calev` B1: "מפתח each שהוא role+text — מפתחות כפולים … Svelte 5.55.7 זורק גם ב-production" — ‏קטגוריה 4 (library-compat).
3. `live-ears-calev` B2: "close() בזמן connect() מתבטל בשקט" — ‏race, ‏`unique`.

ומשפחת **"דיווח-מבצע כוזב"** שנתפסה בתוך הריצות (לא בדוחות-אימות): ריצה 6 — "אליעזר דיווח 4 קומיטים; `git log` הראה 2. ‏478 שורות ישבו לא-מקומטות"; ריצה 19 — "271 שגיאות lint" מול 270/268 מדודים; ריצה 20 — "typecheck עברו" בעוד `TS2305` אדום בעץ שלו; ריצה 22 — `docs/walkthrough.md` בריפו הציבורי בניגוד ל-`AGENTS.md`; ‏פער 8 ‏ב-OPEN-GAPS — פעמיים "טבלת-קומיטים מסודרת, ‏אפס ב-git" (28/08). המשפחה הזו כבר ממופה שם (פערים 3+8) — היא **צנרת-אכיפה**, לא תוכן.

### 2.4 — קטגוריה (4): מאמת — 4 ממצאים שגויים (+2 של מתזמר)

1–2. ‏replay-quiet, ‏אביגיל r2, ‏שני 🔴 שהופרכו (ושניהם היו עוצרים ריצה על ראיה תקינה): "הערך `8` אינו בר-מדידה על הבסיס החדש" — ‏probe עם טקסט סינתטי קצר ש-`MIN_CHARS=20` קיפל (‏`replay-quiet-avigail-r2` ‏ממצא 2); "‏`https-serve.test.ts` נופל **דטרמיניסטית**" — כשל הרצה-מקבילה (ממצא 3). שניהם אושרו כמופרכים ע"י אביגיל עצמה ב-r3. ‏הלקח שכבר נגזר: 🔴 מבוסס-מדידה חייב לצטט את **הקלט המדויק וההיקף**.
3. ‏frame-ingest (מתאם בכובע-מאמת): "אף סוכן ACP לא מגיע ל-`ready` **במכונה הזאת**" — הופרך; הרישום פר-שרת. "'מדדתי אצלי' ≠ 'במכונה'".
4. ‏ריצה 4 (InboxSMS): כלב החזיר "GO, ‏12/12" בלי להריץ מוטציות 2ב/2ג — GO לא-מורווח, נתפס ע"י מרדכי.
‏(+2 של המתזמר, ‏queue-audit 29/08: אבחנה שגויה על מנגנון הפנקס · מרקר-אימות שאינו של הסלייס — "שניהם היו **הצהרה לפני עיגון**".)

‏ארבעת המקרים חולקים צורה אחת עם קטגוריה (1): **מדידה בלי ציטוט קלט/היקף אינה בת-הפרכה** — אצל המתכנן היא מייצרת בריף שבור, אצל המאמת ממצא-שווא.

### 2.5 — צנרת (לא נספר בזיקוק — שייך ל-OPEN-GAPS)

בחלון נצפו ותועדו שם: משפחת-הצופה (ריצות 27–29, ‏פער 9) · סנטינל `exit 0` על עבודה חלקית (פער 2) · זרם-סרק 570s (פער 6, ‏נסגר 31/08) · סשנים לא-נסגרים (פער 7) · דיווח-בלי-קומיט (פער 8) · פנקס/מספור ידני (פערים 0+4, ‏תיקון 31/08) · ‏`BDS_REPORTS` ‏חמש ריצות ברצף (נסגר ב-11) · ‏מיזוג-בלי-אישור, ‏טריות-דוח (ריצה 1, ‏נסגרו). ‏אין צורך לזקק — יש להם קובץ-עבודה בר-סגירה.

---

## חלק 3 — תת-הדפוסים: מה מבדיל הצהרה שנפלה מהצהרה שהחזיקה

ההיגד המכונן — HR-05: **"את מסלול ה-cwd מדדתי חי — ולכן א' עבר בסבב אחד; את מחזור-חיי ה-epoch לא מדדתי, והנחתי."** ‏אותו מתכנן, אותו יום, אותה משימה: סלייс א' (הצהרות מדודות-חי) עבר בסבב אחד; סלייс ב' (הצהרות מקריאת-קוד) — ‏שני NEEDS-REWORK ועצירה.

חמישה תת-דפוסים בתוך "נראה מדוד, נפל" (הספירות מתוך 63 ממצאי קטגוריה-1 + טענות-הבריף שנפלו בתוך הריצות; ממצא יכול לגעת בשניים — הסיווג לפי הפגם הראשי):

| # | תת-דפוס | ‏היקף | דוגמה מובהקת |
|---|---|---|---|
| **מ1** | **שער שנכתב ולא הורץ** (ובראשו: לא הוכח אדום-על-הבסיס / המוטציה לא הורצה) | ‏~44 (‏37 gate-cannot-fail + ‏חלק מ-unrun) | ‏9 חוסמי `ttl-ownership` — "קוד שנכתב מהזיכרון ולא הורץ" |
| **מ2** | **קריאת-קוד שהוצגה כמדידה** — טענת-התנהגות-מערכת בלי הרצה | ‏~10 | ‏HR-05: "ה-epoch זז ב-F5" — הקוד אומר "never bump the epoch"; ‏`meta-passthrough` B1: החוט "לעולם" אינו נושא `tool_call` |
| **מ3** | **נמדד — בסביבה אחרת** (עץ חם של המתכנן ≠ עץ טרי של המבצע; שער שלא מכסה את היעד; פיקסצ'ר ≠ חוט; TZ) | ‏~8 | ‏ריצה 17: `bunx svelte-kit sync` "עבדה אצלי **רק מפני שהרצתי `bun install` לפניה** — צעד שלא תיעדתי"; ‏ריצה 18: "typecheck 0" שלא נגע ב-FE כלל |
| **מ4** | **תחזית בלשון עובדה** ("יהיו צרכנים", "השער יהיה אדום") | ‏~5 | ‏`live-ears-calev` B3: הבריף מצהיר צרכנים ב-DoD 7; ‏נמדד — אפס |
| **מ5** | **נמדד — האובייקט הלא-נכון** | ‏~5 | ‏ריצה 16: §11ג בדק שומרי-**גרסה** והכפילות מגיעה מ**סריאליזציה בפולט** — "בדקתי את המנגנון הלא-נכון והכרזתי על ניצחון"; ‏`grep` שלא פותח `.jsonl.zst` (‏181/613 קבצים) |

**הדיסקרימיננטה** — הצהרה החזיקה כשהתקיימו כל החמישה, ונפלה כשחסר אחד:

1. **הורצה** (לא נקראה) — מ1/מ2;
2. **בסביבת המבצע** — עץ טרי, אותה מכונה/קונפיג — מ3;
3. **על האובייקט שהטענה עליו** — החוט ולא הפיקסצ'ר, המנגנון ולא שכנו — מ3/מ5;
4. **בכיוון הכישלון** — אדום-על-הבסיס / המוטציה הפילה את השורה הצפויה בלבד — מ1/מ4;
5. **עם קלט+פלט+היקף מצוטטים** — כך שהיא בת-הפרכה בלי לשחזר מאפס (זה גם מה שחסר בשני ממצאי-המאמת השגויים של replay-quiet).

**האימות החיצוני של הדיסקרימיננטה — טבלת-הימים.** ‏blockers פר-יום בדוחות drive-coding:

```
19/08: 12 · 20/08: 12 · 21–24/08: 7 · 25/08: 18 · 26/08: 5 · 27/08: 35 · 28/08: 4
29/08: 0 · 30/08: 0 · 31/08: 1        ← ‏26 דוחות, ‏blocker יחיד
```

‏מה השתנה ב-29–31/08: ‏פרה-בריפים עם מדידות-חיות **לפני** הבריף (‏`pre-brief-plan-rules-map.md` ‏642 שורות · `fe-structure` ‏612), ‏שערים שהוכחו אדומים-על-הבסיס (‏ריצה 22: "חוזה late-arrive אדום על הבסיס"), ‏ופיקסצ'רים מהקלטות-חוט אמיתיות. ‏זה גם מה שריצה 7 ‏(‏dedup-by-provider, ‏אפס כשלי-מסירה) ‏זיהתה כבר ב-22/08: "**העובדות נאספו לפני הבריף, לא במהלכו**". ‏וריצה 2 ‏מדדה את נקודת-המעבר בתוך ריצה אחת: "מסבב 3 והלאה **לא נמצא אף חוסם שנבע מדבר שהרצתי** — רק מדברים שעדיין כתבתי."

---

## חלק 4 — עדכוני-קטלוג שנעשו בזיקוק זה

- **`patterns.md`**: נוסף סעיף **"טקסונומיית-מתכנן — זיקוק 2026-08-31"** (חמשת תת-הדפוסים מ1–מ5, תדירויות, דוגמה מובהקת והגנה מוצעת פר-דפוס), ‏ועודכן הפתיח בשורת-הפניה. ‏זה השינוי היחיד בקבצי-השיטה בזיקוק הזה.
- **לא שוּנה**: `plan-pitfalls.md` — ‏הקטגוריות 8–10 ‏מ-03/08 (‏unrun-claim, ‏gate-cannot-fail) ‏כיסו את הסיווג היטב; ‏הפער אינו טקסונומי אלא **אכיפתי**, ‏והבית שלו הוא `MISSION_TEMPLATE`/plan-gate (‏ההצעות בסעיף החדש ב-`patterns.md`).

## חלק 5 — מה נשאר פתוח

| # | פריט | למה |
|---|---|---|
| 1 | ‏פורמט-חובה ל-§11 (‏✅נמדד-חי/📖נקרא/🔮תחזית + פקודה+פלט+סביבה) ב-`MISSION_TEMPLATE` | ‏63/100 — ‏ההגנה המרכזית של הזיקוק; ‏מפורט ב-`patterns.md` §מ1–מ5 |
| 2 | ‏בדיקת-טרום "האם הענף הוא קצה-השרשרת" (‏D-11) | ‏קטגוריה 2 — ‏זול (‏`git log base..chain-tip`), ‏ואף שער-אימות אינו מסוגל לתפוס אותה |
| 3 | ‏חובת ציטוט-קלט בממצא-מאמת מבוסס-מדידה (‏`agents/avigail.md`) | ‏שני ממצאי-replay-quiet השגויים היו נחסכים "בשורה אחת של קלט" |
| 4 | ‏זיקוק כמותי מלא (`distill.py`) על נפח אוגוסט | ‏הדוח הזה חלון-ממוקד; ‏המגמות הרב-פרויקטיות לא נמדדו |
