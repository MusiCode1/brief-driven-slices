# Distillation Report — 2026-06-27

> **תאריך**: 2026-06-27
> **data.json**: `distillations/2026-06-27-data.json`
> **דוחות שנכללו**: 224 דוחות (avigail: 106, calev: 118)
> **מאז הזיקוק הקודם**: זהו **הזיקוק הראשון בפועל**. המנגנון נבנה ב-slice-2 (2026-06-01)
> אך מעולם לא רץ — הטיימר לא הותקן, `distillations/` נשארה ריקה 4 חודשים. זוקק ידנית.

---

## חלק 1 — מבט-לאחור: הכללים מהדוח הקודם

אין זיקוק קודם. כל הקטגוריות בסטטוס "new".

**הערת-מטא חשובה**: ה-`plan-pitfalls.md` ו-`patterns.md` נכתבו ב-2026-06-01 על בסיס
12 דוחות / case study בודד, עם 2 placeholders ריקים (קטגוריות 3, X) וטקסונומיה
פיקטיבית ("slice-9-calev (לא דוח פורמלי)"). בפועל הצטברו 224 דוחות שלא זוקקו. הזיקוק
הזה סוגר את הפער — אבל הלקח האמיתי הוא שהאוטומציה לא רצה. ראה "המלצות לתהליך" למטה.

---

## חלק 2 — התפלגות נוכחית

### אביגיל (plan-verifier)

| severity | count |
|----------|-------|
| minor | 119 |
| confusion | 74 |
| blocker | 34 |
| regression | 21 |
| outdated | 9 |
| type-error | 8 |

| category | count | reports תורמים (דוגמה) |
|----------|-------|----------------|
| `wrong-line-number` | **63** | bds/slice-2, drive-coding/cache-headers-version, drive-coding/P1a, P1b |
| `outdated-risk` | 44 | bds/slice-2, drive-coding/cache-headers, agent-busy-indicator, bunx-single-command |
| `naming-inconsistency` | 32 | bds/slice-2, slice-3, dev/slice-23, drive-coding/latex-math |
| `dropped-branch` | 31 | dev/slice-23, drive-coding/P1b, enter-toggle, input-autogrow |
| `wrong-path` | 25 | bds/slice-2, slice-3, drive-coding/P1a, remove-idle-reaper |
| `missing-symbol` | 20 | drive-coding/cache-headers, latex-math, session-prefs-per-cwd |
| `type-error` | 20 | dev/slice-23, drive-coding/P1a, P1b, agent-busy-indicator |
| `unique` | 17 | — (זנב) |
| `missing-dependency` | 15 | bds/slice-2, dev/slice-23, drive-coding/P1b |
| `cross-verification` | 8 | סבבי אביגיל חוזרים |

**Hitrate**: 87/106 דוחות עם ממצאים (82%). ממוצע: 2.74 ממצאים/דוח.
**Verdicts**: READY 56, USABLE-AFTER-FIX 44, NEEDS-REWORK 5, PASS-WITH-NOTES 1.
→ **כמעט מחצית מהבריפים (45%) נדרשו לתיקון לפני dispatch.** אביגיל מחזירה ROI עצום.

### כלב (runtime-verifier)

| severity | count |
|----------|-------|
| minor | 49 |
| blocker | 10 |
| שאר (info/cosmetic/deviation) | ~5 |

| category | count | reports תורמים (דוגמה) |
|----------|-------|----------------|
| `unique` | **31** | drive-coding/chat-render-polish, chat-virtualization, npm-publish |
| `spec-drift` | 13 | obsidian-web/server-bootstrap-perf, voice-acp/redesign-3, ws-reconnect-infra |
| `regression` | 7 | drive-coding/release-cli-hardening, voice-acp/22-tts-ordering |
| `reload-reconnect` | 3 | opencode-config/slice-D-memfs-claude-spike, voice-acp/fix-turnstate-stuck |
| `cross-store-null` | 3 | drive-coding/chat-virtualization, obsidian-web/server-bootstrap-perf |
| `library-compat` | 2 | drive-coding/chat-virtualization, obsidian-web/server-bootstrap-perf |

**Hitrate**: 40/118 דוחות עם ממצאים (34%). ממוצע: 0.54 ממצאים/דוח.
**Verdicts**: GO 101, PARTIAL 11, NO-GO 5, GO-WITH-NOTE 1.
→ ה-hitrate הנמוך **תקין ורצוי**: כלב הוא קו-הגנה אחרון אחרי אביגיל. 16 slices לא-מוכנים
(PARTIAL+NO-GO) נתפסו לפני merge — חוב-שקט שנמנע.

---

## חלק 3 — חדשות: טקסונומיה מתפתחת

### קטגוריות לא-קנוניות (avigail)

| category | count | פרשנות |
|----------|-------|---------|
| `cross-verification` | 8 | אביגיל מאמתת ממצא של סבב קודם. מועמדת-קנון אם תחצה 12+ בזיקוק הבא. |
| `algorithm-gap`, `logic-inconsistency`, `spec-gap`, `type-mismatch`, `stale-state`, `typecheck-risk`, `style-inconsistency`, `error-isolation`, `wrong-symbol` | 1 כ"א | זנב singleton. לא לקנון. רובם תת-מקרים של dropped-branch/type-error. |

### קטגוריות לא-קנוניות (calev)

| category | count | פרשנות |
|----------|-------|---------|
| `unique` | 31 | **הגדולה ביותר.** לא לקנון (זה ה-fallback) — אבל מסמן שהטקסונומיה (5 קטגוריות frontend) דקה מדי מחוץ ל-UI. |
| `Test-infra`, `Deviation`, `Pre-existing` | 1 כ"א | + וריאציות capitalization של spec-drift. רעש-נרמול. |

#### שינוי-טקסונומיה

**2026-06-27**: הוספת קטגוריה `reload-reconnect` ל-`patterns.md` (קטגוריה 6, מועמדת).
מקור: 3 דוחות (opencode-config/slice-D, voice-acp/fix-turnstate-stuck, drive-coding/chat-render-polish).
פרשנות: state שלא שורד reload/reconnect — תת-דפוס חוזר שלא נכנס ל-5 הקטגוריות המקוריות.

**הערה לנרמול**: כלב מייצר וריאציות-capitalization (`Spec-drift` מול `spec-drift`) ומחרוזות
מורכבות (`Hardcoded-null / Spec-drift`). distill.py סופר אותן בנפרד. שווה להוסיף נרמול
ל-`distill.py` או להדק את הוראת-הטקסונומיה ב-`agents/calev.md`.

---

## חלק 4 — עדכוני-קטלוג

### plan-pitfalls.md (אביגיל) — **שוכתב במלואו**

- **הוספה**: `wrong-line-number` כקטגוריה 1 (הדומיננטית, 63) — היה placeholder. הלקח המבני:
  לא לעגן בריפים במספרי-שורות מוחלטים. → גם תיקון ב-`BRIEF_TEMPLATE`, `agents/avigail.md`, `workflow.md`.
- **הוספה**: `outdated-risk` (44), `naming-inconsistency` (32), `dropped-branch` (31),
  `missing-symbol`/`type-error` (20+20) — כולן היו placeholders או חסרות.
- **שדרוג**: `wrong-path` (קט' 2 קודם → קט' 5, 25 ממצאים) ו-`missing-dependency` (קט' 1 קודם → קט' 7, 15)
  נשמרו אך מוקמו לפי תדירות אמיתית.
- **traceability**: כל קטגוריה עם מקורות אמיתיים (היה 12 דוחות → עכשיו 106).

### patterns.md (כלב)

- **הוספה**: קטגוריה 6 `reload-reconnect` (מועמדת, 3) — היה placeholder "קטגוריה X".
- **תיקון traceability**: 5 הקטגוריות קיבלו מקורות אמיתיים מ-118 דוחות, במקום
  "slice-9-calev (לא דוח פורמלי)" הפיקטיבי.
- **ממצא-על**: תועד ש-`unique`=31 (48%) — הטקסונומיה דקה מחוץ ל-UI.
- **ללא שינוי**: 5 הקטגוריות המקוריות (TDD-green, cross-store, spec-drift, library, CSS) — תקפות.

---

## המלצות לתהליך (מעבר לקטלוגים)

1. **האוטומציה לא רצה 4 חודשים.** הטיימר (systemd) לא הותקן; distillations/ נשארה ריקה.
   המשתמשת חוששת מזיקוק אוטומטי (מרדכי-אוטומטי כותב לקטלוגים בלי עין אנושית). **המלצה:
   זיקוק ידני מתוזמן** ("כל ~20 slices, מרדכי מריץ `distill.py` + מזקק") — אמין יותר
   מ-systemd שלא הותקן, ושומר את הפרשנות תחת עין אנושית.
2. **נרמול טקסונומיה ב-distill.py** — lower-case + פיצול מחרוזות מרובות-קטגוריה.
3. **כלל מספרי-השורות** (תוקן בזיקוק זה) — לאמת בזיקוק הבא שהתדירות של `wrong-line-number` יורדת.
