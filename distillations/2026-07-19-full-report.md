# Distillation Report — 2026-07-19 (‏קורפוס מלא)

> **תאריך**: 2026-07-19
> **data.json**: `distillations/2026-07-19-full-data.json`
> **דוחות שנכללו**: 307 (avigail: 152, calev: 155)
> **מחליף את**: `2026-07-19-report.md` (הראשוני, 28 דוחות) — ראה "תיקון" למטה.
> **למה שניים**: הראשוני רץ על `~/Projects/.../main/reports` (28 דוחות — מיקום חלקי,
> לא-קנוני). לאחר שאותר וסונכרן ה-repo הייעודי (`brief-driven-slices-reports`, 266+
> דוחות), זוקק הקורפוס המלא. **הראשוני נשאר כתיעוד של איך מדגם קטן הטעה.**

---

## תיקון מתודולוגי — ה-headline של הסבב

הדוח הראשוני (28 דוחות, ה-batch האחרון) הסיק: *"כלב תופס כמעט-כלום — 0 ממצא חוסם."*
**זו הייתה מסקנה שגויה, תוצר של מדגם קטן.** בקורפוס המלא (155 דוחות כלב):

| | מדגם 28 | קורפוס 155 |
|--|---------|-----------|
| NO-GO | 0 | **5** |
| PARTIAL | 1 (infra, לא באג) | **14** |
| blockers | 0 | **11** |
| non-GO rate | ~5% | **12%** |

**הלקח**: אירוע-חוסם ב-runtime הוא **נדיר** (~12%). מדגם 28 (וה-batch האחרון היה נקי
במקרה) פשוט לא מכיל מספיק דגימות כדי לראות אותו. **מדידת ערך של גלאי-אירועים-נדירים
מחייבת קורפוס מלא.** לעומת זאת — מדידת *השפעת-כלל* (anchor) נכונה על מדגם-עדכני, כי שם
מסתכלים בכוונה על מה שאחרי הכלל. הראשוני ערבב את שני הסוגים; זה מתקן.

**מה שנשאר תקף מהראשוני**: (א) ROI של אביגיל, (ב) שכלל ה-anchor עבד (ב-delta),
(ג) שכלב-heavy יקר במאמץ — אבל "יקר" ≠ "מבוזבז" עד שנפלח payoff לפי mode.

---

## חלק 1 — מבט-לאחור (delta מול 2026-06-27)

> **אזהרה**: הקורפוס גדל (224 → 307) **בחפיפה** — ספירות מצטברות אינן בנות-השוואה ישירה.
> הסיגנל האמין הוא ב-**batch העדכני בלבד** (הראשוני, 28), לא בדלתא של קורפוסים חופפים.

| כלל | סיגנל אמין (batch עדכני) | פרשנות |
|-----|--------------------------|---------|
| **כלל ה-anchor** | `wrong-line-number` = **0** ב-28 העדכניים (מול 63 ביוני) | ✅ עבד. הספירה המצטברת (79) עדיין נשלטת ע"י העידן הישן. |
| `reload-reconnect` (מועמדת) | 4 מצטבר | ➡️ צוברת לאט. עדיין מועמדת. |

---

## חלק 2 — התפלגות (קורפוס מלא)

### אביגיל (plan-verifier) — 152 דוחות

| severity | count |
|----------|-------|
| minor | 172 |
| confusion | 105 |
| blocker | **38** |
| regression | **27** |
| outdated | 13 |
| type-error | 11 |

| category | count |
|----------|-------|
| `wrong-line-number` | 79 (עידן ישן) |
| `outdated-risk` | 59 |
| `dropped-branch` | 49 |
| `naming-inconsistency` | 43 |
| `unique` | 38 |
| `wrong-path` | 33 |

**Hitrate**: 119/152 עם ממצאים (78%). ממוצע **2.57**.
**Verdicts**: READY 89, USABLE-AFTER-FIX 56, NEEDS-REWORK 6, PASS-WITH-NOTES 1.
→ **62/152 (41%) בריפים דרשו תיקון לפני dispatch.** 38 blockers + 27 regressions נתפסו.
**ROI חד-משמעי — אביגיל היא הרכיב בעל-הערך הגבוה ביותר בצנרת.**

### כלב (runtime-verifier) — 155 דוחות

| severity | count |
|----------|-------|
| minor | 71 |
| blocker | **11** |
| שאר (HIGH/MED/info/cosmetic/deviation) | ~11 |

| category | count |
|----------|-------|
| `unique` | 50 (32%) |
| `spec-drift` | 17 |
| `regression` | 7 |
| `reload-reconnect` | 4 |
| `library-compat` | 3 |

**Hitrate**: 55/155 עם ממצאים (35%). ממוצע **0.6**.
**Verdicts**: GO 135, PARTIAL 14, NO-GO 5, GO-WITH-NOTE 1.
→ **19 slices לא-מוכנים (12%) נעצרו לפני merge** + 11 blockers. ה-hitrate הנמוך **תקין**:
כלב הוא קו-הגנה אחרון אחרי אביגיל. **חוב-שקט שנמנע — כלב מרוויח את מקומו.**

---

## חלק 3 — טקסונומיה

`unique` אצל כלב = 50 (32%) — הדפוס החוזר (היה 48% ביוני, 68% ב-batch העדכני).
הטקסונומיה של כלב דקה מחוץ ל-UI. **מועמד עקבי להידוק ב-`calev.md`** — הפעם עם 3 סבבים
שמאששים. `spec-drift`/`library-compat`/`reload-reconnect` קנוניות/מועמדות — יציב.

**רעש-נרמול (חוזר מיוני)**: severity מופיע כ-`HIGH`/`MED`/`LOW` לצד `blocker`/`minor`,
ו-`unknown`/`non-blocking / deviation-ok`. `distill.py` סופר בנפרד. הידוק-פורמט עדיין תלוי.

---

## חלק 4 — עדכוני-קטלוג

> **אין שכתוב בסבב זה** — הקטלוגים שוכתבו במלואם ב-2026-06-27 על אותו עידן. הפעולות:
- **אישוש**: כלל ה-anchor עבד (0 ב-batch העדכני). להשאיר.
- **מועמד להבשלה הבא**: הידוק טקסונומיית-כלב (`unique` 32-68% ב-3 סבבים) + נרמול severity/category ב-`distill.py`.
- `reload-reconnect`: נשארת מועמדת (4 מצטבר).

---

## חלק 5 — מאמץ מול payoff (מעודכן)

מהראשוני (142 סשנים שחולצו): avigail median 4 דק', calev רגיל 9 דק' (Sonnet),
calev-heavy 16.5 דק' (Opus). **התיקון**: כעת שידוע שכלב תופס 11 blockers ב-155 ריצות,
השאלה **אינה** "לבטל כלב" אלא **"האם ה-11 נתפסו ב-heavy או ב-רגיל?"** — פילוח שדורש
הצלבת verdict×mode על הקורפוס המלא (לא זמין ב-`distill.py` הנוכחי; דורש הרחבה).

**המלצת-slice מעודכנת**: "כיול-mode של כלב" עדיין בעל-ערך — אבל היעד הוא **לוודא ש-heavy
מנותב היכן שהוא באמת תופס**, לא לקצץ כיסוי. עד שיהיה פילוח verdict×mode — לא לקצץ heavy.

---

## המלצות לתהליך

1. **תמיד לזקק על הקורפוס הקנוני המלא** — הסבב הזה הוכיח שמדגם חלקי הפך מסקנה. תנאי-סף:
   `main/reports` = קלון של `brief-driven-slices-reports` (תוקן ב-2026-07-19).
2. **path-neutrality (slice B)** — למנוע את פיזור-הדוחות שגרם לכל זה מלכתחילה.
3. **הרחבת `distill.py`**: (א) פילוח verdict×mode לכלב, (ב) נרמול severity/category.
4. **הזיקוק שוב ידני** — עקבי. קדנציה ידנית ~כל 20 slices.
