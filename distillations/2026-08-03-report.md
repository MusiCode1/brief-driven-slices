# Distillation Report — 2026-08-03

> **תאריך**: 2026-08-03
> **data.json**: `distillations/2026-08-03-data.json`
> **דוחות שנכללו**: 581 דוחות (avigail: 334, calev: 247)
> **מאז הזיקוק הקודם**: 285 דוחות חדשים (‏snapshot קודם: `2026-07-19-full-data.json`, ‏324 דוחות)
> **הערה**: הזיקוק האיכותני של שני ה-case-studies מאוגוסט נעשה **לפני** ההרצה הכמותית הזאת
> ונמצא ב-`recommendations.md` פריטים 28–38. הדוח הזה בודק אם המספרים תומכים בו.

---

## חלק 0 — תקלת-נתונים שנמצאה בהרצה (‏ותוקנה)

**‏`scripts/distill.py:99` השתמש ב-`text.split("---", 2)`** — מפריד לא-מעוגן לתחילת שורה.
כל `---` **בתוך מחרוזת** ב-front-matter קטע את הבלוק באמצע, וה-YAML נשבר → הדוח כולו
נשמט מכל זיקוק, **בשקט**.

```
‏נפגע בפועל:  main/reports/obsidian-eq-checker/slice-5-page-split-calev-phase1.md
‏מה אבד:      verdict=PARTIAL · 1 blocker + 4 minor
‏מה היה החוסם: "‏שער הזהב אינו נופל על שתי מוטציות עצמאיות" — **‏בדיוק §7**
```

זהו הממצא שהפך ל-`recommendations.md` פריט 33 (מוטציות של מאמת עצמאי) — והוא
**מעולם לא נספר** באף זיקוק. ‏PARTIAL בודד גם נעדר מכל התפלגות ה-verdicts ההיסטורית.

**תוקן**: regex מעוגן (`^---\n(.*?)\n---`), + טסט רגרסיה
(`test_md_triple_dash_inside_front_matter_string`) **שהוכח כנופל על הקוד הישן**. ‏39/39 ירוקים.

**תקלה שנייה, מסוג אחר**: `pi-acp/pi-acp-bash-output-fix-calev.md` חסר `---` סוגר —
הפרת פורמט אמיתית (`docs/reports-format.md`). תוקן בדוח עצמו. המפריד הנאיבי הסווה
אותה כשגיאת YAML מבלבלת במקום כ"missing closing ---".

> **הלקח, ובאותו ניסוח של פריט 31**: הזיקוק **הצהיר** 285 דוחות חדשים ולא **בדק** שכולם
> נקראו. ‏7 אזהרות התגלגלו ל-stderr בכל הרצה. ‏5 מהן לגיטימיות (דוחות ישנים בלי
> front-matter כלל), שתיים היו אובדן-נתונים אמיתי.

---

## חלק 1 — מבט-לאחור: הכללים מהזיקוק הקודם

| ‏כלל (2026-06-27 / 07-19) | ‏קטגוריה | ‏מגמה | ‏פרשנות |
|---|---|---|---|
| ‏עוגן ב-symbol+grep, ‏לא במספר-שורה | `wrong-line-number` | ⬆️ 79→**162** | ‏הכלל תועד ב-3 ‏מקומות ו**‏לא עבד**. ‏ראה למטה |
| ‏path-neutrality ‏בהגדרות | `wrong-path` | ⬆️ 33→**113** | ‏עלייה מקבילה לנפח; ‏ה-slice נסגר רק ב-07/19 |
| ‏אימות tool/lib ‏מול env | `missing-dependency` | ⬆️ 19→**74** | ‏#1 ‏בקצב הגידול היחסי |
| ‏כיול-mode ‏של כלב (‏פריט 18) | verdicts כלב | ➡️ | non-GO ‏11.7% (‏היה 12.3%) — **‏יציב, ‏ה-payoff אמיתי** |
| `reload-reconnect` ‏כמועמדת | `reload-reconnect` | ⬆️ 4→**9** | ‏מתקרבת לסף קנוניזציה |

### ‏`wrong-line-number` — ‏הכלל שלא עבד, ‏והנתון שמסביר למה

הזיקוק של 2026-06-27 קבע שמספרי-שורות הם **חוב מובנה**, ועדכן `BRIEF_TEMPLATE.md` §4,
‏`agents/avigail.md` בדיקה 4 ו-`workflow.md` §3. **הקטגוריה הכפילה את עצמה** (‏79→162).

זה לא בהכרח כישלון של הכלל — הנפח כמעט הוכפל אף הוא. אבל היחס לא השתפר, ו**זה
מדיד**: הכלל הוא הנחיה טקסטואלית שאיש לא אוכף. זו בדיוק הצדקת פריט 34 (`lint-brief`),
שבו "מספרי שורות — אזהרה תמיד" הוא שלב 5, מכני לחלוטין.

---

## חלק 2 — התפלגות נוכחית

### אביגיל (plan-verifier)

| severity | count |
|---|---|
| minor | 613 |
| confusion | 503 |
| **blocker** | **153** |
| regression | 133 |
| outdated | 61 |
| type-error* | 34 |

\* `type-error`/`wrong-line-number`/`major`/`warning` מופיעים כ-**severity** למרות שהם
קטגוריות — ‏`recommendations.md` פריט 19 (נרמול severity) **טרם יושם**.

| category | count | ‏קודם |
|---|---|---|
| `unique` | 502 | 38 |
| `dropped-branch` | 225 | 49 |
| `outdated-risk` | 166 | 59 |
| `wrong-line-number` | 162 | 79 |
| `naming-inconsistency` | 140 | 43 |
| `wrong-path` | 113 | 33 |
| `missing-dependency` | 74 | 19 |
| `missing-symbol` | 67 | 26 |
| `type-error` | 49 | 22 |

**Hitrate**: ‏289/334 דוחות עם ממצאים (‏86.5%). ממוצע **4.56** ממצאים/דוח — **‏עלייה של
77%** מ-2.57.

**Verdicts**: ‏READY 147 · USABLE-AFTER-FIX 165 · NEEDS-REWORK 20 → **‏56% מהדוחות
אינם READY** (‏היה 41%).

### כלב (runtime-verifier)

| severity | count |
|---|---|
| minor | 133 |
| **blocker** | **22** |
| outdated | 12 |
| confusion | 7 |
| regression | 4 |

| category | count | ‏קודם |
|---|---|---|
| `unique` | 112 | 50 |
| `spec-drift` | 32 | 17 |
| `regression` | 9 | 7 |
| `reload-reconnect` | 9 | 4 |
| `library-compat` | 6 | 3 |
| `cross-store-null` | 6 | 3 |

**Hitrate**: ‏100/247 (‏40%). ממוצע **0.78** (היה 0.60).

**Verdicts**: ‏GO 218 · PARTIAL 18 · NO-GO 10 · GO-WITH-NOTE 1 → **‏28 slices נעצרו
לפני merge** (‏11.3% non-GO, ‏מול 12.3% קודם — יציב).

---

## חלק 3 — הממצא המרכזי של הזיקוק הזה: התפלגות הסבבים

‏`distill.py` סופר דוחות, לא בריפים. הפילוח לפי **brief** הוא הנתון שמאשש את פריטים 27–33:

| ‏סבבים | ‏בריפים | % | ‏דוחות שנצרכו |
|---|---|---|---|
| 1 | 182 | **81%** | 182 |
| 2 | 16 | 7% | 32 |
| 3 | 7 | 3% | 21 |
| 4 | 10 | 4% | 40 |
| 5 | 3 | 1% | 15 |
| 6 | 3 | 1% | 18 |
| 7 | 1 | 0% | 7 |
| **10** | **2** | **1%** | **20** |

‏224 בריפים ייחודיים · ‏335 דוחות · חציון **1** · ממוצע 1.50.
(‏נגזר מ-`report_ids` ב-data.json, לא מספירת קבצים.)

> **‏19 בריפים (‏8%) צרכו 100 דוחות (‏30% ‏מכל מאמץ האימות).**
> **‏81% מהבריפים נסגרים בסבב אחד.**

**מה זה משנה בפרשנות של פריט 28 (תקרת סבבים):** התקרה **אינה** מס על רוב העבודה —
היא נוגעת ב-18% מהבריפים, ורוב החיסכון מרוכז ב-19 מקרים. שני בריפים הגיעו ל-**10 סבבים**:
`obsidian-eq-checker/slice-4-engine-truth` ו-`obsidian-web/monorepo-foundation`.

**ותיקון קטן לתיעוד**: הנתון "‏6 סבבים על slice-5" נכון, אבל הוא **לא** השיא —
‏`slice-4-engine-truth` קדם לו עם 10, ולא נותח מעולם. מועמד ראשון לניתוח הבא.

**התפלגות לפי פרויקט** — התופעה מרוכזת: ‏`obsidian-eq-checker` (‏6 בריפים ב-4+ סבבים),
‏`obsidian-web` (‏3), ‏`drive-coding/cline-acp-*` (‏5). כלומר **מאפיין פרויקט, לא מאפיין שיטה** —
מה שמחזק את המבחן הא-פריורי של פריט 32 (‏אלה בדיוק הפרויקטים עם תוכן שנועד להיות מורץ).

---

## חלק 4 — טקסונומיה מתפתחת

### `unique` = 502 אצל אביגיל (‏60% מהממצאים)

הזיקוק של 06-27 קבע ש-`unique` גבוה אצל כלב הוא תקין (fallback). **אצל אביגיל 60%
אינו תקין** — הוא אומר שהטקסונומיה לא מכסה את מה שהיא מוצאת בפועל.

שלוש הקטגוריות שהוספתי אתמול ל-`plan-pitfalls.md` נחתכות בדיוק מהגוש הזה, ואף אחת
מהן **אינה** ב-`CANONICAL_CATEGORIES` של `distill.py`:

| ‏קטגוריה חדשה | ‏מקור | ‏מצב ב-distill.py |
|---|---|---|
| `unrun-claim` (‏8) | RAW §23–§25 | **‏חסרה** — ‏נופלת ל-`unique` |
| `intermediate-state-unverified` (‏9) | obsidian-web §2 | **‏חסרה** |
| `gate-cannot-fail` (‏10) | obsidian-web §3,§5 · RAW §6,§16 | **‏חסרה** |

**המלצה**: להוסיף את השלוש ל-`CANONICAL_CATEGORIES["avigail"]`, ואת
`stale-user-visible-string` ל-`calev`. בלי זה הזיקוק הבא ימדוד אותן כ-`unique` ולא
נדע אם הכללים החדשים עובדים. **זה תנאי לכך שהמדידה הבאה תהיה בעלת משמעות.**

### מועמדות שהבשילו

- **`reload-reconnect`** ‏4→9 — סומנה כמועמדת ב-06-27. עברה את סף ה-6 שנקבע אז →
  **לקנוניזציה** ב-`patterns.md` (היא כבר קטגוריה 6 שם, אבל מסומנת "מועמדת").
- **`cross-verification`** ‏8→8, **`fix-verification`** ‏5→5 — **קפאו לחלוטין**. שתיהן
  "אביגיל מאמתת ממצא של אביגיל קודמת". ההקפאה מעניינת: היא סותרת לכאורה את מקדם
  המשוב מפריט 27 — כנראה כי הסבבים החוזרים מסווגים לפי **תוכן** הממצא ולא לפי מקורו.
  → **תמיכה נוספת בפריט 27**: הסיווג "קדם/נולד-מתיקון" חייב להיות שדה נפרד, לא קטגוריה.

### רעש-טקסונומיה אצל כלב

‏5 ערכי-קטגוריה הם **תיאורים מלאים** במקום slugs:
`"3 (spec drift — a concrete instruction inside a general paragraph)"`,
`"1 (green TDD != behavioral coverage)"`, `"Hardcoded-null / Spec-drift"`, ועוד.
ובנוסף severities גולמיים: `HIGH`/`MED`/`LOW`/`cosmetic`/`non-blocking / deviation-ok`.
זהו **פריט 19 שלא יושם** — ‏4 מופעים בלבד, אבל הם מזהמים כל טבלת התפלגות.

---

## חלק 5 — עדכוני-קטלוג שנעשו בזיקוק זה

### `plan-pitfalls.md` (אביגיל)
- **הוספה**: קטגוריה 8 `unrun-claim` — `> מקורות: RAW §22–§25 · obsidian-eq-checker slice-5, slice-7`
- **הוספה**: קטגוריה 9 `intermediate-state-unverified` — `> מקורות: obsidian-web R0 §2, §10`
- **הוספה**: קטגוריה 10 `gate-cannot-fail` — `> מקורות: obsidian-web R0 §3, §5 · RAW §6, §16`
- **ללא שינוי**: 1–7. `wrong-line-number` נשארת #1 בקנון (‏162) — ראה חלק 1.

### `patterns.md` (כלב)
- **הוספה**: מטא-תופעה 2 — "המתקן מייצר את הממצא הבא"
- **הוספה**: קטגוריה 7 `stale-user-visible-string` (מועמדת) — `> מקורות: RAW §19`
- **מומלץ בזיקוק זה**: לקדם `reload-reconnect` ממועמדת לקנונית (‏9 ממצאים)

### `recommendations.md`
- פריטים **28–38** (בלוק "שיפורים מסשן 2026-08-01/02")

### `docs/methodology-evolution.md`
- ערך 2026-08-02 + עקרונות 6 ו-7

---

## חלק 6 — מה נשאר פתוח

| # | ‏פריט | ‏למה זה חשוב |
|---|---|---|
| 1 | ‏להוסיף 4 ‏קטגוריות ל-`CANONICAL_CATEGORIES` | ‏בלעדיו הזיקוק הבא לא ימדוד את הכללים החדשים |
| 2 | ‏פריט 19 — ‏נרמול severity/category | ‏פתוח מ-2026-07-19; ‏מזהם כל טבלה |
| 3 | ‏פילוח **‏סבבים** ‏ב-`distill.py` ‏עצמו | ‏חושב כאן ידנית. ‏זה הנתון החזק ביותר בזיקוק |
| 4 | `scripts/lint-brief.js` (‏פריט 34) | ‏162 `wrong-line-number` ‏אחרי שהכלל תועד = ‏הנחיה לא מספיקה |
| 5 | ‏ניתוח `slice-4-engine-truth` (‏10 ‏סבבים) | ‏השיא האמיתי, ‏לא נותח |
| 6 | ‏חיווט 28–37 ‏לפרומפטים + regenerate | ‏פריט 38-‏ב |

---

## נספח — traceability

`trace` מלא (‏category → reports) ב-`distillations/2026-08-03-data.json`.
‏5 דוחות עדיין נדחים כ"‏no YAML front-matter" — כולם בפורמט הישן, לפני `docs/reports-format.md`:
`drive-coding/CUT-3b-iii-2-calev`, `drive-coding/slice-ws-reconnect-fix-nbug2-calev`,
`obsidian-eq-checker/slice-1-modular-split-calev-light`, `voice-acp/slice-cli-specs-override-calev`,
`voice-acp/slice-fix-idle-flaky-calev`.
