# המלצות — שינויים ל-brief template ול-pipeline

> מבוסס על 5 הקטגוריות ב-`patterns.md`. כל המלצה מסומנת באיזו קטגוריה היא מטפלת.

---

## 1. DoD חייב להיות ויזואלי, לא test-based

**מטפל ב:** קטגוריות 1, 3, 5

**הבעיה:** "כל הטסטים ירוקים" לא מעיד על כלום. ב-Slice 9 — 114 טסטים ירוקים,
19 באגים בפועל.

**הפתרון בתבנית ה-DoD:**

```markdown
## DoD

- [ ] `pnpm typecheck` + `pnpm lint` + `pnpm test` ירוקים
- [ ] **לכל phase שמשפיע על UI** — screenshot ב-`/tmp/<slice>/<phase>.png` +
      השוואה ל-mockup. הצמד screenshot לcommit message.
- [ ] **e2e flow ידני מלא** — את כל ה-feature: פתח דפדפן, הרץ את התרחיש
      הראשי מקצה לקצה. כל פעולה מהמפרט חייבת לעבוד. רשום אילו flows רצת.
- [ ] **Mobile + desktop** — וודא ששני ה-viewports עובדים. צלם את שניהם.
- [ ] **Mockup compliance audit** — phase ייעודי בסוף (ראה סעיף 4).
```

ה-`pnpm test` הוא תנאי **הכרחי, לא מספיק**. הוא מבטיח שהקוד מתקמפל ו-units
שעבדו לא נשברו. הוא לא מבטיח שהפיצ'ר עובד.

---

## 2. סעיף "Anti-patterns ידועים" בכל brief

**מטפל ב:** קטגוריות 1, 2, 4

**הבעיה:** Executor נופל בדפוסים חוזרים שכבר תועדו, כי ה-brief לא מתריע עליהם.

**הפתרון:** template section שמועתק לכל brief, ומותאם לתחום הספציפי. דוגמה:

```markdown
## Anti-patterns ידועים — אל תעשה

### Bubble grouping / streaming text
- ❌ לעולם לא `bubble.segments.push(newSegment)` כשמדובר ב-same-kind same-messageId.
  ✅ במקום: append ל-`text` של ה-segment האחרון שב-streaming.

### Cross-store data flow
- כל פעם ש-Store A מייצר metadata ו-Store B מציג — חובה לתעד את הגשר ב-brief.
- ❌ לעולם לא `someFunction(realArg1, null)` — `null` כפלייסהולדר ל-id ש"נמלא
  אחר כך" כמעט תמיד נשכח. אם הערך לא זמין — restructure.

### Library / framework conflicts
- ❌ `lucide.createIcons()` ב-Svelte → מערבב DOM, יוצר icons כפולים.
  ✅ השתמש ב-`lucide-svelte` npm package (inline SVG).
- (להוסיף עוד אחרי כל case study)

### Refactor vs rebuild
- אם ה-brief אומר "הסר X" — לרוב יש block קונקרטי למחיקה. **חפש אותו ומחק לחלוטין**.
  אל תוסיף flag מעליו.
```

הסעיף הזה גדל מ-case study ל-case study. כל פעם שמתגלה דפוס — מתעדכן ב-`patterns.md`
ומועתק/מותאם ל-brief הבא.

---

## 3. תיעוד מפורש של כל data bridge

**מטפל ב:** קטגוריה 2 (הגדולה ביותר).

**הפתרון:** בכל brief שיש בו יותר מ-store אחד או יותר מ-module אחד, להוסיף section:

```markdown
## Data Flow Bridges

לכל זוג (Producer, Consumer) — מנגנון מפורש:

| Producer | Consumer | Data | Mechanism | קובץ:שורה |
|----------|----------|------|-----------|-----------|
| ‏WS: audio_chunk | agent-session.bubbles | originalText, translatedText | `voice-session.handleAudio()` קורא ל-`agentSession.addTranslatedSegment(messageId, ...)` | voice-session.ts:67 |
| ‏WS: audio_chunk | player | messageId | `+page.svelte:51` — pass `meta.messageId` ל-`player.addSegment` (לא null) | +page.svelte:51 |
| ‏agent-orchestrator.create() | projects-registry | cwd | קריאה ל-`projectsRegistry.recordCwd(cwd)` אחרי spawn | orchestrator.ts:create |

חובה: לכל שורה בטבלה — integration test שעובר על שני הצדדים יחדיו.
```

זה גם מאלץ את ה-planner לחשוב על הגשרים מראש, וגם נותן ל-executor רשימת
checks ספציפית.

---

## 4. Phase ייעודי: "Mockup Compliance Audit"

**מטפל ב:** קטגוריות 3, 5

**הפתרון:** במקום להניח שכל phase יישם את ה-mockup שלו — להוסיף phase אחרון
ייעודי:

```markdown
### Phase N+1 — Mockup Compliance Audit

(תמיד אחרון. אסור לדלג.)

**מטרה:** וודא שה-UI בפועל **זהה** ל-mockup, view-by-view.

**משימות:**
1. פתח דפדפן ב-mobile viewport (390×844). צלם כל route.
2. פתח דפדפן ב-desktop viewport (1280×800). צלם כל route.
3. ‏Open mockup files (final.html). עבור על כל view במקביל לscreenshot.
4. לכל divergence — תקן או תעד למה הוחלט אחרת.
5. flows ידניים: הקלטה→STT→ACP→TTS, click bubble→play, navigate sessions, וכו'.

**Tests:** ויזואלי בלבד. אין test runner.

**DoD:** screenshot per route per viewport + table:

| ‏Route | Mobile | Desktop | Divergences |
|-------|--------|---------|-------------|
| / | ✅ | ✅ | none |
| /agent/[id] | ⚠️ | ❌ | desktop header ישן |

**Commit:** `chore(frontend): Phase N+1 — mockup compliance audit`
```

ב-Slice 9, אם ה-phase הזה היה קיים, היו נתפסים מראש: B4, B11, N1, N2, N3, B6.

---

## 5. הוראות "הסר X" קונקרטיות

**מטפל ב:** קטגוריה 3

**הפתרון:** בכל brief, כל פעם שיש מחיקה — לתת file:line ספציפי, לא תיאור.

**במקום:**
> ‏Remove the textbox + send button from the agent page.

**לכתוב:**
> ‏DELETE block: `packages/frontend/src/routes/agent/[id]/+page.svelte`,
> lines 468–490 (the `<form class="text-form">` block).
> וודא שאין import-ים שנותרו ללא שימוש.

זה דורש מה-planner קצת יותר עבודה (לקרוא קוד קיים בזמן כתיבת ה-brief),
אבל מונע 100% של "Sonnet שכח למחוק".

---

## 6. Verifier Agent Design

**מטפל ב:** הקטגוריות שלא ניתן לחזות מראש (מטא-תופעה).

**עקרון יסוד:** investigation (קריאת קוד + צפייה) ≠ verification (הרצת flows אמיתיים).
שניהם נחוצים. ה-verifier הוא **שלב נפרד**, לא תוספת ל-investigator.

### מטרת ה-verifier

- **לא** לבדוק שהקוד "נראה נכון".
- **לא** להריץ את הטסטים.
- **כן** להפעיל את התוצר בסביבה אמיתית (דפדפן/CLI/API), לעבור על כל ה-flows
  המובטחים ב-brief/fixes, ולדווח על divergence/regressions.

### תוצרים נדרשים

1. ‏**Table של DoD items** — אחד-אחד, האם עובד? עדות (screenshot/log/curl).
2. **רשימת flows שעבדו מקצה לקצה**.
3. **רשימת flows שנשברו** + root cause hypothesis (לא חובה fix).
4. **בעיות חדשות שלא תועדו** — NBug1, NBug2, וכו'.
5. **Regressions** — דברים שעבדו לפני התיקון ולא עובדים אחרי.

### תפקיד המודל

- ‏Sonnet 4.6 כנראה **מספיק** ל-verifier. המשימה היא:
  - הפעלת CLI tools (curl, playwright, screenshots)
  - השוואה ויזואלית פשוטה
  - מילוי טבלה
  
  אין בה החלטות ארכיטקטוניות → אין צורך ב-Opus.

- אבל **חובה שיהיה סוכן נפרד מה-executor**. לא להריץ את ה-verification באותו
  סשן של הביצוע — ה-executor כבר "מאוהב" בקוד שלו ויראה אותו עובד גם כשלא.

### Prompt skeleton ל-verifier

```
אתה verifier. סוכן <executor> סיים <feature>. תפקידך להפעיל את הקוד
ולוודא שכל ה-DoD items עובדים.

מקורות:
- docs/<brief>.md — מה הובטח
- docs/<investigation>.md — אם היה (מה היה שבור לפני)
- git log — מה נעשה

שלבים:
1. קרא את ה-brief — צא עם טבלת DoD items.
2. הפעל את כל ה-flows הקריטיים בסביבה אמיתית.
   - frontend → linux-gui browser + pw-clean.sh
   - backend → curl + WS client
3. לכל item: סטטוס (✅/⚠️/❌/ⓘ) + evidence.
4. חפש regressions: דברים שעבדו לפני, לא עובדים עכשיו.
5. חפש בעיות שלא היו ברשימה — תפעיל גם flows צדדיים.
6. כתוב דוח ב-docs/<feature>-verification-report.md.

אסור: לערוך קוד. אסור: commit.
מותר: יצירת בדיקות זמניות, screenshots, dummy data.
```

### תזמון

```
Planner → Executor → Verifier-slice → (אם יש באגים) → Executor (fix) → Verifier → ...
```

**Verifier-slice חובה בסוף** — sub-agent נפרד, fresh context, לא executor עצמו.
ה-bias של "כותב הקוד מאמין שזה עובד" אמיתי, וזה תפקיד ה-verifier לעקוף אותו.

**Verifier-phase (בתוך הסליס) לא חובה** — opt-in מה-planner ספציפית לפי
היוריסטיקות בסעיף 8.

---

## 7. ‏שתי דרגות של verifier-slice — light ‏ו-heavy

‏Verifier-slice בסוף הסליס תמיד רץ — ‏אבל בדרגה שונה לפי score מורכבות.

| ‏Tier | ‏זמן | ‏סקופ |
|------|------|------|
| **`verifier-slice-light`** | ~15 דק' | ‏Read brief DoD → walk items עם evidence → 1-2 e2e happy paths → דוח קצר |
| **`verifier-slice-heavy`** | ~30-50 דק' | ‏הפרוטוקול המלא: 7 שלבים, edge cases, regressions, side flows, סיווג ל-patterns.md |

ה-light הוא **‏ביטוח קבוע** ‏(תופס DoD compliance, ‏fresh perspective).
ה-heavy הוא **‏topup ל-risk גבוה** ‏(תופס regressions ‏ו-flows לא רשומים).

‏הplanner קובע את הדרגה ב-brief לפי score (סעיף 8).

---

## 8. ‏Complexity Scoring — ‏מה tier להריץ

‏ה-planner ממלא checklist בכתיבת ה-brief. ‏הציון קובע tier אוטומטית.

### Checklist

```
Integration / Data flow:
[ ] Cross-store data flow ‏חדש (FE↔BE, store↔store, BE↔external)   +2
[ ] ‏Streaming / real-time (WS, SSE, audio chunks, partial text)     +2
[ ] ‏Protocol contract חדש (WS message, REST endpoint, schema)       +2

Code surface:
[ ] ‏Refactor של קוד קיים (לא greenfield)                            +1
[ ] >5 files touched ב->2 packages                                  +1
[ ] ‏State machine / async coordination (races, queues)              +2

External dependencies:
[ ] ‏ספרייה חיצונית חדשה (לא מוכרת מ-slices קודמים)                  +2
[ ] ‏הספרייה עושה DOM mutation / monkey-patching runtime             +1

Risk indicators:
[ ] 3 הslices האחרונים באזור הזה החזירו bugs                         +2
[ ] ‏Test coverage <70% על הקוד שמשתנה                                +1
[ ] ‏Deploy לפרודקשן מיד אחרי הסליס                                   +2

Mitigators:
[ ] ‏Pure logic, ‏אין IO                                              -2
[ ] ‏TDD מלא, ‏tests מקיפים על הbehavior החדש                         -1
[ ] ‏Greenfield, ‏אין call sites קיימים                               -1
```

### ‏Score → tier mapping

```
0-3:  light    (verifier-slice-light בלבד, ‏ללא verifier-phase)
4-7:  light + verifier-phase ‏על 1-2 phases מסוכנים
8+:   heavy   (verifier-slice-heavy) + verifier-phase ‏על mostly phases
```

### ‏תיעוד ב-brief

‏ב-frontmatter / header של ה-brief, ‏הplanner מציין:

```yaml
verifier-slice: light | heavy
complexity-score: 5         # ‏אופציונלי — תיעוד
```

### ‏אימות (retrospective)

| ‏Slice | ‏Score משוער | ‏Tier מתאים | ‏מה קרה |
|-------|--------------|------------|---------|
| ‏migration-system | 1 | light | ‏אכן אפס bugs |
| ‏logging-infra | 9 | heavy | ‏אכן ~7 issues על 2 rounds |
| ‏Slice 9 (frontend) | 14 | heavy | 19 bugs (‏אבל verifier לא הופעל אז) |

‏הציון מכייל נכון על הסיפורים שכבר ראינו. ‏אם case study חדש סותר — ‏לעדכן משקלות.

---

## 9. ‏Per-Phase Testing Strategy

‏ה-planner מציין לכל phase ב-brief את אסטרטגיית הבדיקה. ‏ה-executor **‏כיבד את הבחירה**,
‏לא חולק עליה.

### ‏פורמט ב-phase header

```
‏Phase 3: Wire migrations to profile-manager
  Testing: tdd
```

### ‏ערכים אפשריים

| ‏ערך | ‏מתי | ‏מה Executor עושה |
|------|-----|--------------------|
| `tdd` | ‏Logic חדש, ‏protocol contract, ‏state machine | ‏Red test → green code → refactor |
| `integration` | ‏Wiring, ‏glue code שמחבר tested pieces, ‏refactor | ‏קוד קודם, ‏אז integration test בתוך אותו phase |
| `manual` | UI, CSS, ‏UX flows | ‏אין tests אוטומטיים — ‏בדיקה ידנית בדפדפן/curl |
| `none` | docs, config, pure rename | ‏שום בדיקה (typecheck + lint ‏עדיין כן) |

### ‏Defaults אם missing

‏אם הplanner שכח לציין, ה-executor ישתמש בברירות לפי תוכן ה-phase:

- ‏logic / protocol / schema → `tdd`
- ‏refactor → `integration`
- ‏ui / styling → `manual`
- ‏docs / config / rename → `none`

‏אבל **‏עדיף שה-planner יציין במפורש** — ‏מנע ויכוח באמצע.

### ‏אם executor לא מסכים עם הבחירה

‏זה case של "ראש קטן" — **‏STOP, ‏החזר STATUS: BLOCKED** ‏עם הסבר.
‏לא לסטות מהחלטת הplanner.

---

## 10. רשימת בקרה ל-planner (Opus)

לפני סגירת brief, וודא:

- [ ] ‏DoD ויזואלי + טסטים (לא רק טסטים)
- [ ] ‏Anti-patterns section עם הדפוסים הרלוונטיים מ-`patterns.md`
- [ ] טבלת Data Flow Bridges מלאה (אם יש מולטי-store)
- [ ] ‏Mockup Compliance Audit phase קיים (אם UI)
- [ ] כל "הסר X" — file:line קונקרטי
- [ ] **‏Complexity score ‏חושב + tier מסומן** (light/heavy)
- [ ] **‏Verifier-phase ‏מסומן לphases ספציפיים אם score 4+**
- [ ] **‏Testing strategy פר phase** (tdd/integration/manual/none)
- [ ] אזהרות על library incompatibilities ידועות (אם רלוונטי)
- [ ] **‏הופעל plan-verifier על ה-brief** (‏סעיף 11)
- [ ] **dev/main parity check** ‏(סעיף 12)

---

## 11. ‏Plan Verifier — ‏חובה לפני handoff

**‏מטפל ב:** ‏כל הקטגוריות. ‏זה ה-recommendation ‏עם ה-ROI הגבוה ביותר.

**‏הראיה:** ‏ב-`case-studies/2026-05-voice-acp-slices.md`, 100% (3/3) ‏מהbריפים שנבדקו ‏היו בעיה אמיתית, ‏בממוצע 3 ‏בעיות לbrief. ‏עלות ~10 ‏דק', ‏חיסכון 30-60 ‏דק' debug + ‏מנע ‏regression שקט אחד שהיה מגיע ל-production.

**‏הפתרון:** ‏אחרי שהbrief ‏מוכן ‏ולפני dispatched ל-executor — ‏הפעל [`agents/plan-verifier.md`](agents/plan-verifier.md):

```ts
Task({
  subagent_type: "plan-verifier",
  description: "Verify <slice> plan",
  prompt: `‏אתה plan-verifier. brief: docs/plans/<slice>.md.
Project root: <path>. Base tip: <hash>.
‏בצע את 7 ‏הבדיקות שמתועדות ב-`agents/plan-verifier.md`.`
})
```

**‏סוגי בעיות שתופס בפועל** (‏מ-case study):
- API/symbol לא קיים ב-dev (‏רק ב-main)
- Pseudo-code מחסיר branch ‏קיים → silent regression
- Pseudo-code עם type error צפוי
- Line numbers ‏factually wrong
- Naming inconsistency פנימי
- File paths שלא קיימים
- Risks/escalations ‏מיושנים

**‏Tama ‏מקבלת את הדוח ‏ומתקנת.** ~‏15-20 ‏דק' תיקון בממוצע. ‏ואז handoff.

---

## 12. dev/main Parity Check (‏על plan-verifier)

**‏מטפל ב:** ‏טעות חוזרת ‏שזוהתה ב-3/3 ‏briefs.

**‏הבעיה:** ‏Tama (planner) ‏מסתכלת על main worktree (‏או על תוצרי slices קודמים שעוד לא merged), ‏ומניחה ‏שsymbols קיימים. ‏אבל ה-base של slice ‏החדש ‏הוא dev tip, ‏ושם הם לא בהכרח קיימים.

‏דוגמאות מ-voice-acp:
- `deleteAgent` ‏ב-`adapters/agents-api.ts` — main מייצא, ‏dev לא
- ‏Line counts ‏ב-`sessions-ws.ts` — main 82 ‏שורות, ‏ה-brief טען 75 (`main` ‏ישן)

**‏הפתרון** ב-plan-verifier prompt:

```
‏לכל symbol/API ‏שhbrief מציין — ‏אמת מול dev tip ‏בפועל:

cd <project>/dev   # ‏או base worktree
git log -1 --oneline    # ‏לוודא tip
grep -rn "<symbol>" packages/
wc -l <path>
```

‏אם משהו לא קיים ‏ב-dev — 🔴 blocker.

---

## 13. JIT Briefs > Upfront Briefs

**‏מטפל ב:** ‏Brief stale ‏לפני dispatching.

**‏הראיה:** ‏מ-`~/.config/opencode/learnings.md` 2026-05-29: "‏אסטרטגיית 'גל-גל' — ‏לכתוב 2-3 briefs ‏לפני dispatch, ‏לא 9 briefs מראש — ‏עובדת טוב יותר. ‏כל גל לומד מהקודם (verifier findings, ‏סטיות מהbrief). ‏Brief ‏שנכתב ‏לפנים ‏שלא ‏נדispatched ‏עוד ‏הופך stale (codebase ‏זז)."

**‏הפתרון:**

1. ‏זהה 2-3 ‏ה-slices ‏הבאים בעדיפות.
2. ‏כתוב brief ‏לראשון. ‏הפעל plan-verifier. ‏Dispatch executor.
3. ‏בזמן שexecutor רץ — ‏כתוב brief לשני.
4. ‏אחרי verifier-slice הראשון — ‏עדכן את ה-recommendations / patterns ‏אם משהו ‏עלה.
5. ‏אז ‏כתוב brief לשלישי, ‏עם הלקחים החדשים.

**‏לא:** ‏לכתוב 9 ‏briefs ‏מראש "‏לחסוך זמן". ‏בפועל זה ‏מאבד זמן כי:
- Brief מס' 9 ‏נכתב לפי מצב code ‏שכבר השתנה
- ‏לקחים מ-slice 1-3 ‏לא מועברים ‏ל-briefs 4-9
- ‏שינויי scope מ-stakeholder ‏לא משתקפים

---

## 14. Gotchas טכניים שחוזרים

**‏מטפל ב:** ‏זמן מבוזבז ב-executors ‏על gotchas צפויים.

**‏זוהו ב-voice-acp slices 11, 14, testing-coverage:**

### 14.1 core dist missing אחרי worktree (monorepo)

‏Typecheck נכשל ‏עם TS6305 ב-incremental cache. ‏Sonnet (slice 11) ‏בילה **12 ‏הודעות** ‏באותה שגיאה. ‏Opus פתר ב-1.

**‏פתרון:** ‏ב-EXECUTOR_DISPATCH §0 ‏ובbriefs לפרויקטי monorepo:

```bash
# ‏אחרי worktree + install:
pnpm build --force      # ‏או tsc --build --force ‏ל-core/types package
```

### 14.2 Worktree path mistake

‏Opus (‏slices 14, testing-coverage) ‏יצר ‏worktree ב-`dev/.worktrees/` ‏במקום ב-`.worktrees/` ‏הראשי **‏פעמיים**. ‏זיהה ‏ותיקן עצמאית, ‏אבל זמן מבוזבז.

**‏פתרון:** ‏ב-EXECUTOR_DISPATCH §1:

> ‏אם ‏cwd ‏שלך ‏הוא `dev/` (‏לא root), ‏השתמש ב-absolute path: `git worktree add /full/path/.worktrees/<slice> -b <slice> dev`

### 14.3 ‏CLI assumption שגויה (claude אינו ACP-compliant)

‏slice 8 brief ‏ביקש לטסט עם claude. ‏ה-WS ‏נסגר עם code 1005 ‏מיד.

**‏פתרון:** plan-verifier ‏בבדיקה 7 ‏(risks/escalations מיושנים) ‏צריך לאמת ש-CLI שצוין ב-brief ‏אכן ACP-compliant ‏עם הפרויקט. ‏אם לא — 🟡 ‏המלץ ל-Tama להחליף.

### 14.4 SDKs ‏שצריכים apiKey ‏ב-constructor ‏עם OneCLI

‏SDKs ‏כמו `@ai-sdk/elevenlabs`, `@ai-sdk/google` ‏צריכים `apiKey` ‏ב-constructor. ‏פתרון: ‏העבר string placeholder, OneCLI ‏מחליף ב-proxy. ‏מתועד ב-`~/.config/opencode/learnings.md` 2026-05-16.

---

## 15. ‏אל תbiasing את verifier-prompts

**‏מטפל ב:** ‏False negatives של verifier.

**‏הבעיה:** ‏executor (‏שכותב את הprompt ל-verifier) ‏עלול לא בכוונה למסגר את המשימה בצורה שמסתירה את הבאגים שלו. ‏דוגמה: "‏בדוק ש-bubble grouping עובד" — ‏אם Sonnet בנה bubble grouping שגוי לחלוטין, ‏הוא יבדוק על הקוד הקיים ‏ויאשר.

**‏הפתרון:** ‏verifier prompts ‏צריכים להיות נטרליים:

❌ ‏"‏בדוק ש-X ‏עובד"
✅ ‏"‏אמת את כל DoD items של slice <name> ‏מול brief: ‏docs/plans/<name>.md"
✅ ‏"‏קרא את ה-brief לבד. ‏אל תסמוך על המסגור של הprompt הזה — ‏הוא הגיע מ-executor."

‏מתועד ב-`agents/verifier-phase.md`, `agents/verifier-slice-light.md` ‏ב-section "‏כלל יסוד: ‏אל תסמוך על ה-prompt".

---

## שיפורים מסשן 2026-07-19 (זיקוק מלא + אירוע פיזור-דוחות)

> 7 פריטים שצפו בסשן שבו אותרו: פיצול 91 דוחות ל-3 מיקומים לא-מסונכרנים, תזה שגויה
> ממדגם קטן, ואובדן-גלם עם קריסת-process. מקור: `distillations/2026-07-19-full-report.md`.

### 16. progress/todo-file לסוכנים

**מטפל ב:** נראות + עמידות מול קריסת-process.

**הבעיה:** סשני אביגיל/כלב ארוכים ואטומים — אין לדעת מה הם עושים תוך כדי. וכשה-process
קורס, הגלם מתפוגג (ה-CLI store מסתובב/נמחק). בסשן הזה אביגיל נקטעה כשה-process יצא.

**הפתרון:** כל מאמת מתחזק **to-do file קבוע** (markdown checklist) בנתיב peekable. הכללה
של מנגנון ה-`log_path` הקיים של כלב → always-on לכל המאמתים, לא רק בריצות-יתרו.

### 17. path-neutrality בהגדרות הסוכנים

**מטפל ב:** מעבר בין מכונות.

**הבעיה:** ההגדרות מקשיחות `~/projects` (p קטנה). על מכונה עם `~/Projects` (P גדולה) →
הסוכנים כתבו 90 דוחות לתיקיות שגויות/לא-מגובות.

**הפתרון:** env יחיד פר-מכונה + placeholders (`{{BDS_REPORTS}}`...) במקור-האמת + החלפה
ב-install-time לשלושת ה-CLIs. **מאומת READY** — `docs/plans/slice-path-neutral-agent-configs.md`.

### 18. כיול-mode של כלב

**מטפל ב:** עלות verifier.

**הבעיה:** `calev-heavy` = median 16.5 דק' Opus; לפי הזיקוק רוב ה-heavy מחזיר GO/0-findings.
gate מורכבות-8 נדיב מדי.

**הפתרון:** heavy רק ל**סיכון-סוג** (security/concurrency/data-integrity), לא רק ניקוד;
`mode` מפורש-חובה בכל dispatch; default=רגיל. ⚠️ **לכייל, לא לקצץ** — כלב תופס 11 blockers
ב-155 ריצות (12% non-GO). ה-payoff אמיתי, רק לא מכויל לתיק.

### 19. distill.py: פילוח verdict×mode + נרמול severity

**מטפל ב:** דיוק הזיקוק.

**הבעיה:** אי-אפשר לענות "האם ה-blockers נתפסו ב-heavy או ברגיל" — אין פילוח. וה-severity
מגיע כ-`HIGH`/`MED` לצד `blocker`/`minor` → ספירה כפולה ורעש.

**הפתרון:** הוסף ל-`distill.py` פילוח verdict×mode לכלב; נרמל severity/category (lower-case
+ פיצול מחרוזות מרובות-קטגוריה).

### 20. ארכוב-בריפים + cleanup כריטואל-merge נאכף (slice A)

**מטפל ב:** הצטברות בריפים ו-worktrees.

**הבעיה:** `docs/plans/` מלא בבריפים-שיושמו שלא אורכבו; worktrees מצטברים. הארכוב לא קשור
לטקס ה-merge → נשכח.

**הפתרון:** **merge = פעולה אטומית** שכוללת `archive (brief → docs/plans/archive/)` +
worktree cleanup, ע"י הסוכן הממזג, **מיד אחרי אישור-ה-merge**. לא צעד נפרד שנשכח.

### 21. live-preview gate ל-web לפני merge (slice B)

**מטפל ב:** merge מושכל בפרויקטי web.

**הבעיה:** runtime-gate = כלב GO. אבל כלב הוא **סוכן** שמריץ טסטים — המשתמש לא ראה את
התוצאה בעיניו. calev-heavy עושה "visual review" אבל זה שיפוט שלו, לא העיניים של המשתמש.

**הפתרון:** לפרויקטי web — אחרי כלב GO ולפני merge — מרדכי מעלה **preview חי** (dev server/URL)
ומציג למשתמש לאישור-עיניים. שתי שכבות: אימות-מכונה (כלב) → אימות-אדם-חי (משתמש). זה מה
שהופך את "merge רק באישור מפורש" ל**מושכל**.

### 22. יישור דוקטרינת docs ל-docs-repo

**מטפל ב:** דוקטרינה מול מציאות + פרטיות.

**הבעיה:** הדוקטרינה אומרת "decisions/walkthrough בריפו הפרויקט, ליד הקוד". בפועל כבר קיים
`docs-repo` נפרד (private) עם decisions/plans/reports פר-פרויקט — כי תוכן אישי דלף למסמכים
ועדיף שלא יתפרסם. הדוקטרינה מפגרת אחרי מה שכבר נעשה.

**הפתרון:** לעדכן `mordechai`/`eliezer`/`SKILL`: decisions+walkthrough → docs-repo פרטי,
לא ריפו-הפרויקט הציבורי. reports כבר ב-repo פרטי ייעודי (`brief-driven-slices-reports`).

### 23. Front-loaded decision sheet — כל השאלות + התרחישים לפני הריצה

**מטפל ב:** מקסום אוטונומיה — צמצום הפרעות מ-N-פר-ריצה ל-**1 לפני + 1 במיזוג**.

**הבעיה:** גם בריצה "אוטונומית", החלטות צצות באמצע (בסשן 2026-07-19: ambiguity של scope
A/B, וה-qoder blocker) ומאלצות עצירה. כל עצירה = הפרעה + המתנה, ושוברת את הרצף.

**הפתרון:** לפני שמרדכי מתחיל ריצה (‏אוטונומית **או לא**), הוא מציג **דף-החלטות אחד**:

1. **שאלות scope** — כל ההבהרות על *מה* לבנות.
2. **הרשאות-מראש לתרחישים** — נקודות-ההחלטה החוזרות שצצות תוך-כדי, כל אחת עם default.
   מ-track record, ~7 התרחישים:

| תרחיש | default מוצע | חלופה |
|-------|-------------|-------|
| scope דו-משמעי | הפרשנות השמרנית + תיעוד | לשאול |
| תלות מתגלה כ-WIP לא-committed | לצמצם scope, לדחות ל-slice נפרד | לעצור / לקמיט |
| אביגיל USABLE-AFTER-FIX | fix+re-verify עד READY (אוטומטי) | — |
| אביגיל NEEDS-REWORK | rewrite פעם אחת; עדיין NEEDS → עצור | — |
| כלב PARTIAL/NO-GO | fix-loop עד GO (N סבבים) → עצור | דחייה מתועדת+מאושרת |
| התנגשות-merge בשרשרת | לפתור אם טריוויאלי; אחרת עצור | — |
| פעולה בלתי-הפיכה ל-mainline (commit לא-סקור, מחיקת data) | **לעולם לא אוטונומי — תמיד עצור** | — |

3. המשתמש עונה **פעם אחת** (או מקבל defaults) → מרדכי רץ brief→אביגיל→יישום→כלב עד
   ה-merge gate **בלי הפרעה**.

**התוצאה:** אינטראקציה אנושית = דף-החלטות אחד *לפני* + אישור-merge אחד *אחרי*. זה ה-batching
האולטימטיבי של נקודות-האדם.

**הוכחת-הרעיון (הריצה החיה, 2026-07-19):** ה-qoder blocker היה בדיוק תרחיש #2. ‏לו נשאל
מראש "אם תלות מתגלה כ-WIP — לצמצם או לעצור?", הריצה לא הייתה נעצרת כלל. ה-default (‏לצמצם)
הוא בדיוק מה שמרדכי עשה בפועל — אז front-loading היה חוסך את העצירה.

### 24-26. אימוץ מ-Spec-Kit (ראה `docs/comparisons/spec-kit.md`)

**מטפל ב:** פערים שהתגלו בהשוואה ל-github/spec-kit (2026-07-20). הכרעה: **ללמוד, לא לעבור.**

- **24. Constitution פר-פרויקט** — לרכז עקרונות-על ל-`<project>/constitution.md` (היום הדוקטרינה
  מפוזרת בין mordechai/SKILL/AGENTS). מקור: spec-kit Constitution phase.
- **25. Converge check** — **הפער הגדול ביותר.** שלב מפורש "האם הפרויקט **השלם** תואם את הכוונה,
  ומה נותר?" — מעבר לאימות slice-slice. מקור: spec-kit Converge phase.
- **26. commands-UX** — לעטוף הפעלות-סוכן ב-commands discoverable (כמו `/speckit.*`).

> **נדחה במפורש**: מעבר ל-spec-kit. רציונל: אובדן שלושת ה-crown-jewels (אימות-runtime שמריץ
> קוד, לולאת-זיקוק, merge-gate אנושי) — שכולם הוכחו-אמפירית בסשן 2026-07-19.

### 27. חוק הספייק — כשל brief חוזר באותו אזור הוא סימן לקוד

**מטפל ב:** לולאת אימות שמזינה את עצמה. מקור:
`case-studies/2026-08-eq-checker-slice5-self-feeding-loop.md` (6 סבבים, 79 ממצאים).

**המדידה:** מתוך 9 הממצאים בסבב 5, **ארבעה — כולל החוסם היחיד — נוצרו על ידי
התיקונים של סבב 4**. ה-brief הגיע ל-1,047 שורות והכיל קוד מרשמי, פקודות shell
וחוזי חתימה — כלומר הפך לתוכנית שנכתבת **בלי מהדר**, ואביגיל שימשה כמהדר
במחזור של עשרים דקות.

**הכלל:** אם אותו אזור ב-brief מייצר חוסם **בשני סבבים רצופים** — עצור וכתוב
אותו כקוד. הסימן אינו שהתיאור לוקה; הסימן הוא שהתוכן אינו טקסט.

**שלוש דרגות לקוד ב-brief**, לפי סדר עדיפות:

1. הקוד קיים בריפו, וה-brief מפנה אליו בשם קובץ ופונקציה.
2. חתימה בלבד, בלי גוף — חתימה ניתנת לאימות סטטי, גוף לא.
3. גוף פונקציה שנכתב ב-brief ולא הורץ מעולם. **זו דרגת הסיכון.**

**מדד עזר — מקדם המשוב.** בכל סבב, סווג כל ממצא ל"קדם לסבב" מול
"נולד מהתיקון הקודם" (אביגיל כבר נותנת `source_brief`):

- ‏<20% נולדו-מתיקון → הלולאה מתכנסת, המשך.
- ‏>40% → הלולאה מזינה את עצמה. **שנה מדיום**: ספייק, צמצום scope, או קבלת
  ה-brief עם רשימת ליקויים מפורשת.

**ושתי תוספות שנגזרו מאותו תיק:**

- **שער-נפילה חייב להיות מורץ, לא מתוכנן.** גם הוכחת-הנפילה עצמה יכולה להיות
  חלולה. באותו תיק, מוטציה שאמורה הייתה להוכיח שהשער נופל **שרדה עם 0 הפרשים**,
  כי הפונקציה נקראה עם טיפוס שגוי ונפלה לענף שגיאה שמחזיר קבוע.
  התוספת: כשמפתח נכנס לשער השוואתי לראשונה — **ספור ערכים שונים**. קריסה לקבוע
  היא חתימה מדידה של קריאה שגויה.
- **מסבב 3 — היקף דלתא.** גזירה מלאה בכל סבב מייצרת ממצאים חדשים בטקסט ישן,
  זנב ארוך של קוסמטיקה, ואות מטושטש. גזירה מלאה נשמרת לסבב 1.

---

## שיפורים מסשן 2026-08-01/02 (זיקוק — עלות האימות)

> ‏10 פריטים מ**שני** תיקים שמדדו את אותה תופעה משני צדדים, ומ-RAW שמלווה אותם:
> `case-studies/2026-07-obsidian-web-r0-nine-rounds.md` (‏9 סבבים, 71 ממצאים),
> `case-studies/2026-08-eq-checker-slice5-self-feeding-loop.md` (‏6 סבבים, 79 ממצאים),
> `case-studies/RAW-2026-08-verification-cost.md` (‏נתוני גלם, §1–§25).
>
> **הנתון שממסגר את כל העשרה** — 6 סבבי אביגיל על brief אחד:
>
> | ‏סבב | ‏טוקנים | tool_uses | ‏ממצאים | 🔴 |
> |---|---|---|---|---|
> | r1 | 117,552 | 44 | 21 | 7 |
> | r6 | 114,969 | 21 | 8 | 0 |
> | **‏סה"כ 6** | **825,457** | **285** | **79** | **19** |
>
> **‏העלות לסבב שטוחה בזמן שהממצאים נחתכים ב-62%.** ‏43,445 טוקנים לחוסם.
> ‏לא משלמים על מציאה — משלמים על **בנייה מחדש של ההבנה** בכל סבב.
> ‏פריטים 28–30 תוקפים את זה; ‏31–37 תוקפים את מה שמזין את הסבבים מלכתחילה.

### 28. תקרת סבבים — אחרי 3 סבבים "נקיים" עבור לביצוע-זריק

**מטפל ב:** תשואה פוחתת של אימות סטטי. מקור: `2026-07-obsidian-web-r0-nine-rounds.md` §2, §10.

**הבעיה:** אין נקודה שבה brief "נגמר" — סבב 9 עוד החזיר 2 ממצאים. וגרוע מכך: תשעה
סבבים **לא מצאו** שה-Verification של Commit 1 אינו בר-השגה כלשונו
(`TS18003: No inputs were found` — הקבצים נוצרים ב-Commit 2). ביצוע אחד מצא את זה
ב-**8 דקות**.

**הכלל:** אחרי **3 סבבים ללא 🔴 חדש שאינו תוצר של התיקון הקודם** — הפסק לאמת והרץ
ביצוע על **worktree זריק**. ריצה של 8 דקות שאפשר למחוק ב-`git worktree remove` היא
אבחון זול יותר מסבב אימות, והיא תופסת מחלקה שאימות סטטי **אינו יכול** לתפוס.

**מה כל צד תופס — הטבלה שמצדיקה את שניהם:**

| | ‏אימות סטטי | ‏ביצוע |
|---|---|---|
| ‏סימבולים שלא קיימים, עוגנים שנסחפו | ✅ | ❌ |
| ‏רגרסיות חוט שאף טסט לא מפיל | ✅ | ❌ |
| ‏סתירות פנימיות בין סעיפי ה-brief | ✅ | ❌ |
| **‏מצבי-ביניים של רצף ה-commits** | ❌ | ✅ |
| **‏הרצה בפועל של כלי-בנייה** | ‏חלקית | ✅ |

> זה **אינו** ביטול ה-plan-gate — הוא מנע שם שלוש רגרסיות שקטות. זה משנה **מתי הוא נסגר**.

#### ‏סייג שנמדד אחר כך: התקרה עיוורת ללולאה מתנודדת

‏`case-studies/2026-08-eq-checker-slice4-ten-rounds.md` — **‏10 סבבים, והתקרה לא
הייתה יורה אף פעם.** הרצף: ‏🔴 ב-r1,r2 · נקי ב-r3 · ‏🔴 ב-r4,r5 · נקי ב-r6 ·
‏🔴 ב-r7,r8,r9 · נקי ב-r10. **מעולם לא שלושה סבבים נקיים ברצף.**

הכלל סופר סבבים; הלולאה הזאת התנודדה. **האות המשלים אינו בספירה אלא באופי:**

```
r1-r3    ‏תוכן:      "Commit 5 premise is factually false"
r4-r10   ‏פנקסנות:   "‏שלושה האש-בסיס עדיין בתוקף" · "‏עקבת האימות נעצרת ב-r6"
```

‏24 מתוך 37 הממצאים מ-r4 והלאה (‏65%) היו על **המסמך**, לא על העבודה —
ו**שלושת החוסמים ב-r5, r7, r9 היו כולם פנקסנות.**

> **‏כשרוב הממצאים בסבב הם על המסמך ולא על העבודה — האימות הסתיים.**
> ‏מה שנשאר הוא תחזוקת-מסמך, והיא זולה יותר בהקפאה מאשר בסבב נוסף.

הסיווג זול: `source_brief` שמצביע על שורת-מטא (‏provenance, ‏עקבה, ‏האש, ‏ספירה)
ולא על סעיף-תוכן. **סף מוצע: >50% פנקסנות בסבב → הקפא ושגר.**

### 29. Verification נמדד במצב שאותו commit משאיר — ומצב-בסיס לכל שער

**מטפל ב:** נקודה עיוורת מבנית של אימות-brief. מקור: obsidian-web §2 · RAW §16 שלב 2.

**הבעיה:** ה-brief מתאר **רצף**, והאימות בודק **יעד**. אביגיל מדדה `tsc --build`
ארבע פעמים בשלוש גרסאות TypeScript — ובכל פעם בנתה סקרץ' עם **כל הקבצים במקום**.
רצף ה-commits מייצר מצבי-ביניים שאיש לא מדד.

**שתי דרישות שנוספות לאביגיל:**

1. **פר-commit** — כל Verification שמריץ כלי-בנייה גלובלי (`tsc --build`, `bun run test`,
   `npm run build`) נמדד **במצב שאותו commit משאיר**, לא במצב הסופי. אחרת ה-Verification
   הוא משאלה.
2. **מצב-בסיס** — להריץ כל שער DoD על קומיט הבסיס, **לפני** שנגעו במשהו:
   > **שער שכבר אדום על הבסיס אינו שער.**
   > **שער שכבר ירוק על הבסיס גם אינו שער** — הוא לא מודד את הסלייס.

   תפס בפועל: DoD שהיה אדום מלכתחילה, ו-`grep -c` שמחזיר exit 1 על אפס התאמות.

### 30. היקף מצומצם הוא המנוף — לא סשן מחודש

**מטפל ב:** עלות הסבב. מקור: RAW §17 (ההשערה) → §20 (המדידה שהפריכה אותה חלקית).

**המדידה, שלוש נקודות על שני בריפים:**

| ‏ריצה | ‏סשן | ‏היקף | ‏טוקנים | tool_uses | ‏ממצאים |
|---|---|---|---|---|---|
| slice-5, ‏ממוצע 6 סבבים | ‏מאפס | ‏מלא | **137,576** | 44 | 13 |
| slice-7 r1 | ‏מאפס | **‏מצומצם** | **58,034** | 24 | 10 |
| slice-7 r2 | **‏מחודש** | ‏דלתא | **86,050** | **12** | 12 |

**הממצא — ומנוגד להשערה:** סשן מחודש חתך את קריאות הכלים בחצי (‏24→12) אבל **העלה**
את הטוקנים (‏58K→86K). **הטרנסקריפט נשלח מחדש כקלט בכל חידוש.**

```
‏היקף מצומצם   →  ‏חוסך טוקנים (‏58%)   ← ‏זה המנוף
‏סשן מחודש     →  ‏חוסך קריאות וזמן, ‏לא טוקנים
```

**הכלל:** לצמצם היקף **תמיד** (מסבב 3: "אמת את הדלתא + את הממצאים שאתה עצמך העלית").
לחדש סשן רק כשחשוב שההקשר לא ילך לאיבוד — לא כדי לחסוך מכסה.

**שני סייגים:**
- **חדש רק סוכן שהסתיים בהצלחה.** חידוש של סוכן שנקטע החזיר
  `resumed from transcript` ואז **שתק לחלוטין** — לא דוח, לא שגיאה, לא usage.
  שיגור מאפס שנכשל לפחות מחזיר שגיאה מפורשת.
- **כלב לעולם טרי.** מאמת-ריצה מחודש כבר "יודע" שהשער תקין — וזווית טרייה היא כל
  תפקידו (ראה פריט 33). אביגיל → סשן מחודש; כלב → סוכן חדש בכל פעם.

### 31. כל טענה עובדתית נכתבת עם הפקודה שהפיקה אותה

**מטפל ב:** **המנגנון** שמזין את מקדם המשוב (פריט 27). מקור: RAW §23, §25.

**הבעיה — שלושת החוסמים של slice-7, כל אחד פקודה אחת:**

```
r2-A   ‏כתבתי:  lastOut = {items, out}
       ‏בפועל:  return { items, out, autoOrigin }
       ‏בדיקה:  grep -n "return { items" web/ui/app.js

r2-B   ‏כתבתי:  DoD 11 ‏סורק גם scripts/
       ‏בפועל:  EX_PASSPHRASE ‏מופיע שם 3 ‏פעמים — ‏הוא **‏נקרא** שם
       ‏בדיקה:  ‏להריץ את הגריפ שכתבתי, ‏פעם אחת

r3     ‏כתבתי:  rows[i] = out[i] + items[i]
       ‏בפועל:  analyze ‏מדלג על שורות; out.length < items.length
       ‏בדיקה:  ‏להשוות אורכים
```

עלות: ‏3 סבבים, ~‏230K טוקנים, על שלוש פקודות שלא הורצו.

**המנגנון — ולא רק התסמין:**

> **מיקור-חוץ של האימות הוריד את הזהירות במעלה הזרם.**

הראיה היא אותו סוכן, אותו יום, שתי התנהגויות:

```
‏קוד ספייק   ← ‏אין מאמת במורד הזרם  ← ‏הורץ מיד, 3 ‏פעמים, 4 ‏באגים נתפסו עצמית
‏בריף        ← ‏יש מאמת              ← ‏נכתב מהזיכרון, 3 ‏חוסמים
```

זהו risk compensation קלאסי: רשת ביטחון משנה את התנהגות מי שהיא מגנה עליו. וזה
מנבא את המספר שכבר נמדד — מקדם המשוב ‏44% ב-slice-5 מול **100%** ב-slice-7.

**הניסוח לדוקטרינה:**

> **המאמת הוא קו ההגנה האחרון, לא הראשון.**
> ממצא שהמאמת מצא **ושהכותב היה יכול למצוא בפקודה אחת** — הוא **כישלון של הכותב**,
> לא הצלחה של המאמת.

**מדד — "יעילות המאמת":** אחוז הממצאים שהיו ניתנים למציאה בפקודה אחת ע"י הכותב.
נמוך → המאמת עושה את מה שרק הוא יכול (בריא). גבוה → הוא עושה את עבודת הכותב, וזה
סימן שהזהירות במעלה הזרם צנחה. ‏slice-7: ‏3 מתוך 3 = **100%**.

**מה משתנה בפועל** — לא "לקרוא יותר קוד" ולא עוד סבב. **לפני מסירה לאימות: לעבור
על כל טענה עובדתית ולשאול "הרצתי את זה?"** מה שלא הורץ — או שיורץ, או שיסומן
במפורש כלא-מאומת (‏§0 של ה-brief מצהיר לאליעזר שהטענה אינה מאומתת ומורה לו לבדוק).

### 32. המבחן הא-פריורי לספייק — "יועתק, או יפורש?"

**מטפל ב:** מתי ספייק, ובלי לחכות לכישלון. מקור: RAW §13–§15. משלים את פריט 27.

פריט 27 קובע ספייק **בדיעבד** (אותו אזור, שני סבבים רצופים). זה היה יורה אחרי r2 —
חיסכון של 4 סבבים ≈ ‏550K. **המבחן הא-פריורי יורה לפני סבב אחד:**

> **האם הטקסט הזה יועתק, או יפורש?**

תפקיד המבצע **להעתיק** (רתמה, שער, פונקציית עזר, צינור shell) → **זה קוד, יש להריץ אותו.**
תפקידו **לבנות ממשהו מתואר** (סדר קומיטים, חוזה שכבות, מה עובר לאן) → פרוזה היא הכלי הנכון.

בסלייס הנמדד: **420 שורות מתוך 1,047 היו מהסוג הראשון — 40%.**

**א-סימטריית העלות, הנימוק שמכריע:**

```
‏סבב אימות:  ~137K ‏טוקנים  →  ‏מפיק טקסט
‏ספייק:      ~40 ‏דקות      →  ‏מפיק קוד שנדרש ממילא
```

**ספייק לא יכול להתבזבז.** אם הוא זול מסבב אחד ומפיק תוצר נדרש — הסף צריך להיות נמוך מאוד.

**הצורה בפועל** (‏~420 שורות, ~40 דקות):

```
‏גשש ב-/tmp        ~30 ‏שורות, ‏נמחק      → ‏מפיק **‏ידע**, ‏לא קוד
‏הקובץ האמיתי      188 ‏שורות, ‏מקומט     → ‏תוצר שנדרש ממילא
‏בדיקת דטרמיניזם   3 ‏הרצות רצופות        → ‏תפסה באג
‏הוכחת נפילה       3 ‏מוטציות + 2 ‏של אחר  → ‏הופכת שער לשער
‏הבריף             ‏מצביע, ‏לא מתאר        → 30 ‏שורות → 3
```

**החצי הראשון חד-פעמי ונזרק.** ספייק אינו "לכתוב את הדבר האמיתי מוקדם" — חצי ממנו
גששים בני 30 שורות שכל תפקידם לגלות עובדה אחת ולמות. הידע חוזר כ**הערה בקוד**:
`// location.protocol נדרש בפועל — נמצא בהרצה, לא בקריאה` — זה מה שמונע מהבא בתור
למחוק את זה כי "נראה מיותר".

**שלושה סייגים:**
- **אל תספייק דבר שהעיצוב שלו הוא השאלה הפתוחה.** ספייק מוקדם על עיצוב לא-מוכרע
  **נועל** אותו, ואז מגנים על קוד במקום לבחור נכון.
- **ספייק מוכיח שהקוד רץ, לא שהוא מחובר.** ב-slice-7 הספייק תפס באג מיד — **וייצר
  חוסם**: מודול שאינו מחווט לבנדל.
- **ספייק אינו פותר תיאור של קוד קיים.** הוא פותר קוד חדש. תיקון שנושא טענה על קוד
  קיים — זו מחלקה נפרדת, וזה פריט 31.
- **קיצור ה-brief אינו תחליף.** ‏379 שורות מול 1,161 הקטינו את **הנפח** של הלולאה
  (‏2 חוסמים במקום 7) אבל **החמירו את היחס** (‏44% → 100%).

### 33. הוכחת-נפילה דורשת מוטציות של מישהו אחר

**מטפל ב:** עיוורון-מדגם של כותב השער. מקור: RAW §7 (‏🔴 של כלב).

**הבעיה:** הדוקטרינה דורשת להוכיח שכל שער חדש נופל על הקוד השבור. זה נעשה — שלוש
מוטציות, שלוש נפילות, הפלט בהודעת הקומיט. **ואז כלב בחר שתי מוטציות משלו, והשער
נשאר ירוק.** הסיבה: הקורפוס (‏50 מקרים) חסר את שילובי הקלט שחושפים אותן — שבר יחד
עם סוגריים, וסימן שלילי בכיוון ההפוך.

> **מי שכתב את הקורפוס בוחר, בלי כוונה, מוטציות שהקורפוס שלו מכסה.**

**הכלל:** שער חדש נבדק ב**שתי מוטציות של הכותב ובשתיים של מאמת עצמאי**.
עלות: שורה בתדריך לכלב. זו הכללה של דרישת הוכחת-הנפילה, לא סתירה שלה.

**וזה גם המשקל-הנגדי לספייק שהמתכנן כותב.** מה שהספייק בונה אינו המוצר — הוא
**הסרגל** (`golden-page.js` מודד, `page-src.js` טוען, הקורפוס הוא אמת המידה),
והמתכנן כבר היום בעלים של אמת המידה — זה מה ש-DoD הוא. הסיכון שנשאר הוא בדיוק
עיוורון-המדגם, והמענה הוא מאמת עצמאי שתוקף את השער — לא "שהמתכנן לא יבנה אותו".

### 34. `lint-brief` — הציר שחסר, וגם ל-spec-kit אין אותו

**מטפל ב:** ~רבע מהממצאים, בדיוק הרבע הזול-למצוא/יקר-להעביר-סבב. מקור: RAW §8, §16, §24.

**הרעיון:** כלי מכני שקורא brief **ומריץ** את מה שהוא טוען. תשעה ממצאים מתוך ה-79
היו נתפסים בפקודה אחת:

```
‏גודל בנדל 600-800KB     ← ls -l ‏אמר 54,319       ‏מספרי בדיקות מיושנים   ← node src/test.js
DoD 10 ‏כבר אדום בבסיס   ← ‏הרצת הגריפ עצמו        179 ‏מול 178 ‏מפתחות      ← Object.keys().length
shapeOf ‏לא מיוצא        ← typeof eqChecker.shapeOf 1,878 ‏מול 1,881 ‏שורות  ← wc -l
```

**מה הוא בודק (טיוטה):** כל נתיב קובץ שמוזכר קיים · כל סמל בטבלה קיים בקובץ שהוצהר ·
כל טענת "X מיוצא מ-Y" · **כל פקודת shell בבריף — מורצת** ותוצאתה מושווית למה שכתוב
לידה · כל מספר מתויג-כמדוד מורץ מחדש · מחרוזת גרסת ה-brief תואמת ל-dispatch ·
מספרי שורות = אזהרה תמיד.

**חמישה שלבים, לפי תשואה מול 79 הממצאים:**

| ‏שלב | ‏מה | ‏תפס בפועל |
|---|---|---|
| 1 | **‏גרף תלויות בין קומיטים** — ‏אין קריאה קדימה | **4, ‏מהם 3 ‏חוסמים** — ‏הרווח הגדול |
| 2 | ‏מצב-בסיס לכל שער (‏פריט 29) | 2 |
| 3 | ‏עקביות הפניות — ‏כל מספר שמופיע פעמיים | 4, ‏אחד חוסם |
| 4 | ‏חתימות — ‏מה הגוף באמת מקבל מול טענת ה-brief | ‏ההחמצה שהרגה הוכחת-נפילה |
| 5 | ‏אזהרות (‏עוגן-שורה, ‏גוף לא-מורץ, ‏טענה בלי פלט) | ‏זנב |

**הפרט שהכי קל לפספס:** המהדר צריך לרוץ **ב-worktree של המבצע, בזמן ה-dispatch** —
לא רק בזמן הכתיבה. הבסיס זז: ב-slice-4 ה-brief היה READY ואז `dev` זז שלוש פעמים
תחתיו, ורוב הממצאים מ-r4 ואילך היו **התיישנות, לא ליקויי תכנון**.

**המעמד מול spec-kit** (‏נבדק מול `docs/comparisons/spec-kit-deep-dive.md` ומול הריפו
החי, 2026-08-02): אין שם פקודה שקוראת קוד מקור אמיתי. ‏`/speckit.analyze` הוא הקרוב
ביותר והוא על **הציר הלא-נכון**:

```
/analyze     ‏מסמך↔מסמך   ‏מכני    ← ‏קיים אצלם
‏אביגיל       ‏מסמך↔קוד    ‏אנושי   ← ‏קיים אצלנו, ‏ב-58K–138K ‏טוקנים לסבב
‏הלינטר       ‏מסמך↔קוד    ‏מכני    ← **‏לא קיים באף אחד**
```

**המבחן ההגון היחיד לפני שבונים:** לבנות ולהריץ **רטרואקטיבית** על ה-brief הזה,
ולספור כמה מ-79 הוא באמת תופס. מספר, לא הערכה מכותרות. מיקום מוצע:
`scripts/lint-brief.js`, ~200–300 שורות. **טרם נבנה** — ראה פריט 38 בבקלוג.

### 35. DoD בצורת "‏X **נכון**", לא רק "‏X קיים"

**מטפל ב:** מחלקה שאף שער לא בדק. מקור: RAW §19.

**הבעיה:** בסוף הסלייס, אחרי שכל השערים ירוקים, המשתמש הסתכל בדף החי ושאל "בטוח
שזה מה שהיה צריך להיות?" — **שניים משלושה שדות היו שגויים**, כולל תאריך בנייה
`2026-08-01` על בנייה מ-`2026-08-02`.

מה לא תפס: ‏255 בדיקות מנוע · ‏54 לוגיקה · ‏3,677 ערכי זהב · ‏116 שרשראות פאזר ·
‏6 סבבי אביגיל · ‏3 סבבי כלב. **אף אחד.** נתפס בעין אנושית, בשנייה.

**למה:** השערים בודקים **קיום** של מחרוזות, לא **נכונות** שלהן. ‏`Object.keys().length`
סופר מפתחות; הזהב מקפיא פלט של פונקציות ו-`T.ver` אינו פלט של פונקציה; ה-E2E בודק
שהדף נטען בלי שגיאות, ומחרוזת גרסה שגויה אינה שגיאה.

> **תאריך בנייה שקרי גרוע מהיעדר תאריך — הוא נראה כמו מידע.**
> **ומטא-דאטה שמתוחזקת ביד תרקב, והשערים לא ירגישו.**

**התוספת לתבנית ה-DoD:**

```
□ ‏מטא-דאטה שנראית למשתמש — ‏גרסה, ‏תאריך, ‏שם מנוע — ‏נבדקה מול המציאות?
□ ‏אם היא מתוחזקת ביד: ‏האם היא יכולה להיגזר במקום?
```

הפתרון הנכון הוא **להוציא את השדה מידיים אנושיות** (הזרקה בזמן בנייה,
`esbuild --define`), לא להוסיף עליו שער. תיקון ידני מחזיר את אותה שיחה בעוד סלייס.

### 36. `assert` לפני הכתיבה מגן על נכונות, לא על ביצוע

**מטפל ב:** קומיטים שמכריזים על תיקון שלא קרה. מקור: RAW §22 — **שלושה מקרים באותו יום.**

**הבעיה:** סקריפט-עריכה שנעצר על `assert` באמצע **לא כותב כלום** — והפקודה עדיין
"הצליחה" מבחינת הקומיט שאחריו. מקרה אחד: קומיט שהכריז על **עשרה** תיקונים ושינה
**שתי שורות**. הבדיקה שנעשתה הייתה `git log`, לא תוכן הקובץ.

**התבנית שעובדת:**

```python
ok, miss = [], []
for tag, a, b in EDITS:
    if a in s: s = s.replace(a, b, 1); ok.append(tag)
    else: miss.append(tag)
open(p, "w").write(s)          # ← ‏נכתב **‏תמיד**
print("‏הוחלו:", ok, "· ‏חסרו:", miss)
```

ואז **גריפ על הקובץ** לכל תיקון. לא על קוד היציאה, לא על הודעת הסקריפט — על התוכן.
זהו המקרה החזק ביותר בעד הלינטר: "כל טענה בהודעת קומיט על שינוי בקובץ — האם הקובץ
באמת השתנה בהתאם?" מכני לחלוטין.

זה גם אותו דפוס של פריט 31: **בדיקה של הצהרה במקום בדיקה של מציאות.**

### 37. דוח אימות מציין מצב-התחלה · מחלוקת נפתרת ב-bisect

**מטפל ב:** מחלוקות מדידה בין מתכנן למאמת. מקור: obsidian-web §6, §8.

**המקרה:** אביגיל דיווחה ש**כל** `npm install` בריפו מת. המתכנן שחזר וקיבל exit 0.
**שניהם מדדו נכון:**

| workspaces | npm exit |
|---|---|
| `core` + ‏חבילה אחת עם תלויות | **0** |
| + ‏חבילה **‏שנייה** עם תלויות | **1** |

המתכנן בדק מבנה עם שני חברים; ה-brief שולח חמישה. **המדידה הייתה תקינה, המבנה לא ייצג.**

**שלושה כללים:**
1. **דוח אימות מציין מצב-התחלה לכל מדידה.** בלי זה שני ממצאים באותו דוח יכולים
   לסתור זה את זה (וכך היה) בלי שאיש יבחין.
2. **מתכנן ומאמת חלוקים → bisect משותף, לא הכרעה לפי סמכות.** כאן זה הפך מסקנה
   שגויה לתיקון בן שורה — הסיבה האמיתית הייתה `~/.bunfig.toml` **גלובלי של המכונה**
   עם `linker = "isolated"`, כלומר **אף מפתח אחר לא היה רואה את זה**.
3. **אל תכתוב "בלתי-נמנע" ב-brief לפני bisect. וכשחלק מטענה מופרך — פרק אותה
   לרכיבים ובטל רק את מה שהופרך.** ביטול גורף של טענה שחציה נכון יצר כאן סתירה מול
   commit אחר.

**ובאותה משפחה — "התיקון סוגר את השדה שנבדק ומשאיר את השדה שלידו"** (ניסוח של
אביגיל, שהחזיק ארבעה סבבים רצופים על אותה שגיאה: סטטוס → `code` → `error` →
endpoint שני). **כשתיקון נוגע בשדה או באתר-קריאה — מנה את כל בני-המחלקה לפני
שמכריזים "תוקן".** טבלת השוואה בין ארבעת אתרי-הקריאה היא מה שעצר את הדימום.

### 38. Backlog פתוח מהסשן הזה (לא בוצע)

| # | ‏פריט | ‏מצב |
|---|---|---|
| ‏א | **`scripts/lint-brief.js`** (‏פריט 34) — ‏ובעיקר המדידה הרטרואקטיבית מול 79 הממצאים | ‏לא נבנה. ‏המספר האמיתי לא ידוע |
| ‏ב | ‏חיווט פריטים 28–37 לפרומפטים (`agent-definitions/prompts/*`) ‏+ regenerate | **‏חלקית (2026-08-03)**: ‏4 ‏קטגוריות הוזרמו ל-`avigail`/`calev` ‏+ ‏regenerate. ‏שאר הכללים טרם |
| ‏ה | ‏פילוח **‏סבבים** ‏פר-brief ‏בתוך `distill.py` | ‏מחושב ידנית בשני הזיקוקים. ‏הנתון החזק ביותר, ‏ולא אוטומטי |
| ‏ו | ‏פריט 19 — ‏נרמול severity/category | ‏פתוח מ-2026-07-19 |
| ‏ג | ‏פילוח input-מוטמן מול output בעלות האימות | ‏לא נמדד |
| ‏ד | ‏האם מודל זול יותר תופס את "הרבע המכני" באיכות של סקריפט | ‏לא נבדק (‏כנראה לא) |

### 39. פנקסנות הבריף נגזרת, לא נכתבת ביד

**מטפל ב:** לולאת אימות שניזונה מהרישום של עצמה. מקור:
`case-studies/2026-08-eq-checker-slice4-ten-rounds.md` §4.

**הבעיה:** חמישה ממצאים, בחמישה סבבים נפרדים, על **הרישום של האימות עצמו**:

| ‏סבב | ‏הממצא |
|---|---|
| r4 | "S0 provenance ‏משמיט את סבב 3 ‏לגמרי" |
| r5 | "‏שורת הסטטוס אומרת verified ‏בזמן ששורת האימות נגמרת ב-r4" |
| r6 | "‏שורת הדוחות אומרת r1..r4 ‏בזמן ששורת האימות מונה חמישה" |
| r9 | "‏עקבת האימות נעצרת ב-r6 ‏בעוד r7, r8, r9 ‏באו אחריה" |
| r10 | "‏העקבה מגיעה ל-r9 ‏אבל הערך האחרון קורא USABLE-AFTER-FIX" |

הלולאה: כל סבב מוסיף שורה לעקבה → הסבב הבא מוצא אותה לא-עקבית → זה ממצא →
התיקון מוסיף שורה → חוזר חלילה. **המאמת מדווח על ריקבון שהאימות עצמו ייצר.**

**זהו בדיוק פריט 35** (`patterns.md` קטגוריה 7, "מטא-דאטה שמתוחזקת ביד תרקב") —
רק שהמטא-דאטה היא של ה-brief עצמו, והשער שמרגיש הוא המאמת.

**הפתרון, ואותו פתרון:** להיגזר, לא להיכתב.

| ‏שדה | ‏מקור הגזירה |
|---|---|
| ‏עקבת-האימות (`r1 NEEDS-REWORK · r2 …`) | ‏שמות קבצי הדוחות ב-`reports/<project>/` |
| ‏האש-בסיס | `git rev-parse` ‏בזמן ה-dispatch |
| ‏ספירת DoD / ‏ספירת בדיקות | ‏ספירה |

שלושה מתוך חמשת מקורות-הריקבון נסגרים מכנית. זה גם מקרה-מבחן ישיר ללינטר
(פריט 34): **"כל מספר בכותרת ה-brief שניתן לגזירה — האם הוא תואם?"**

**וסייג הפוך:** לא לתקן פנקסנות בסבב אימות. אם ה-brief כבר READY והממצא הוא
"העקבה לא עקבית" — זה תיקון של שורה אחת שלא נוגע בעבודה, ולא סיבה לסבב נוסף.
