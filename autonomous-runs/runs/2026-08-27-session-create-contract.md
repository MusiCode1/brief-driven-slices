# ‏ריצה 20 — `session-create-contract` (drive-coding)

> **‏תאריך**: 2026-08-27 · **‏בסיס**: `dev` @ `3952f4f9` · **‏ענף-הרצה**: `integration/run-session-create-contract`
> **‏תוצאה**: ‏כלב **GO** · ‏מוזג ל-**`edge`** ‏באישור המשתמש · ‏`dev` ‏לא נגע
> **‏חריגה מאושרת**: ‏**‏כל התפקידים על Cursor** — ‏מכסת Claude ‏התקרבה לסופה

## ‏מה נמסר

‏שלושה קומיטים: ‏`e1d6c2fc` (C0 — ‏`permission.ts` ‏ל-`core` + ‏`resolvePermissionPolicy`) ·
`413021f7` (C1 — ‏`permissionPolicy` ‏בשתי הסכימות + ‏שומר שלישי) ·
`0709e8fb` (C2 — ‏`env` ‏דרך `composeShapeEnv`).
‏מוזג ל-`edge` ‏ב-`44ec7c18`, ‏עם תיקון-המשך `b33eb7e0`.

## ‏מודל-ההרצה — ‏חריגה מלאה מ-§4-8

| ‏תפקיד | ‏מריץ | ‏תוצאה |
|---|---|---|
| ‏אביגיל | `cursor`/`composer-2.5` | ✅ ‏USABLE-AFTER-FIX · ‏🔴 ‏מצאה ממצא-חוסם שהמתכנן פספס |
| ‏מרדכי | `cursor`/**`grok-4.6`** | ⚠️ ‏פעל נכון · ‏**‏מת פעמיים בתעבורה** |
| ‏אליעזר · ‏כלב | `cursor`/`composer-2.5` | ✅ |

🟢 **‏המסקנה על הניתוב**: ‏איכות-האימות **‏לא נפגעה**. ‏אביגיל-על-Cursor ‏מצאה 🔴 ‏שהמתכנן
‏(Opus) ‏פספס — ‏שרשרת-החיווט של `permissionPolicy` ‏דרך יצירת-ה-host ‏**‏העצלה**.
‏וגרוק **‏ניצח ריצה כשורה**: ‏קרא את הגדרת-התפקיד, ‏פתח worktree ‏מענף-ההרצה, ‏כתב פרומפט
‏לאליעזר, ‏שיגר, ‏החזיק את ההמתנה (22 ‏ו-17 ‏קריאות ל-`await-dispatch`), ‏ו**‏תפס פגם בכלי
‏שקיבל**. ‏מה ששבר אותו הוא **‏יציבות התעבורה**, ‏לא היכולת.

## ‏כשלי-מסירה — 2

1. 🔴 **‏`typecheck` ‏אדום, ‏ודווח כירוק.** ‏אליעזר: ‏"linter, ‏typecheck ‏ו-lint:i18n ‏עברו".
   ‏בפועל `TS2305` ‏בעץ שלו עצמו. ‏כלב ב-light ‏סימן ⬜ ‏"handoff" ‏— ‏שני שערים, ‏אותה שורה.
   ‏**‏התגלה רק במיזוג ל-`edge`.** ⇒ ‏פער 3 ‏הורחב.
2. ‏**‏שער-המוטציה נכשל ב-timeout ‏ולא בטענה.** ‏אליעזר: ‏"‏עם המוטציה ההרצה נכשלה בגלל
   timeout". ‏כלב אימת עצמאית ‏וקיבל `timeout 5s ‏על allow_once` — ‏אדום, ‏אך לא מהסיבה
   ‏הצפויה. ⬜ **‏נותר פתוח**: ‏האם מוטציה שמכשילה ב-timeout ‏היא שער תקף.

## ‏התערבויות — ‏1 ‏מוצר / **‏4 ‏צנרת**

| ‏סיווג | ‏ההתערבות |
|---|---|
| ‏צנרת | ‏`await-dispatch` ‏בדק `git status` ‏על הריפו ולא על ה-worktree ⇒ ‏**‏תנאי "‏עץ נקי" ‏היה ריק, ‏גם בריצה 19** (‏נתפס ע"י **‏grok**) |
| ‏צנרת | ‏זרם-סרק של 9.5 ‏דק' ‏מפיל את הסוכן ⇒ ‏`timeout 570→60` (‏**‏פער 6 ‏חדש**) |
| ‏צנרת | ‏מיזוג slice→ענף-הרצה **‏לא בוצע** ‏— ‏מרדכי מת לפניו; ‏המתאם השלים ידנית |
| ‏צנרת | ‏פער 3 ‏היה צר מדי (‏"‏מספר" ‏במקום "‏טענת-שער") |
| ‏מוצר | ‏החלטת המיזוג ל-`edge` ‏ולא ל-`dev` |

## ‏תיקונים קבועים — 3

- `scripts/await-dispatch.sh --worktree` (`3e319e3`)
- `scripts/await-dispatch.sh` ‏timeout 570→60 + ‏פער 6 (`2f829c9`)
- ‏פער 3 ‏מורחב ל"‏טענת-שער" (`a48ed0b`)

## ‏🟡 ‏פערים 1+2 — ‏אומתו חלקית

**‏פער 1 (‏החזקת-ההמתנה) ‏עבד**: ‏39 ‏קריאות ל-`await-dispatch` ‏בשני התורות.
‏מרדכי **‏לא נטש** — ‏הוא נהרג.
**‏פער 2 (‏סיום לפי ארטיפקט) ‏עבד**: ‏הסקריפט הבחין נכון בין ‏"‏עדיין רץ" ‏ל"‏הושלם".
⚠️ ‏אך שניהם **‏נשארים 🟡** — ‏הריצה לא הסתיימה תחת המנגנון בלבד; ‏המתאם התערב שלוש פעמים.

## ‏מדד

| | |
|---|---|
| ‏התערבויות מוצר / **‏צנרת** | 1 / **4** |
| ‏כשלי-מסירה | **2** |
| ‏תיקונים קבועים | 3 |
| ‏סבבי-אימות | 1 (‏אביגיל) |
| **‏שטח חדש?** | ✅ ‏מרדכי מחוץ ל-Claude · ‏grok ‏כמנצח · ‏מיזוג ל-`edge` |
