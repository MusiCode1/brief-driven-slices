---
run: 27
date: 2026-08-31
project: drive-coding
mission: docs-for-llm/plans/missions/acp-wire.md
slices: [acp-wire]
interventions_product: 1
interventions_plumbing: 1
handoff_failures: 0
permanent_fixes: 0
plan_rounds: 1
brief_to_dispatch: "~0:16"
verdict: החזיקה — מוזג לענפי-ההרצה בלבד (שני ריפואים)
---

# דוח-ריצה 27 — `acp-wire`

> הדוח הזה על ה*ריצה*, לא על הפרויקט.
> אימות-תוכן: `$BDS_REPORTS/drive-coding/acp-wire-{avigail,calev,run}.md`

## שעון ה-plan-gate

| מדד | ערך | הסף |
|---|---|---|
| סבבי אביגיל עד dispatch | **1** (USABLE-AFTER-FIX → תיקון-במקום) | 1 ✅ |
| זמן-קיר בריף→dispatch | **~16 דק׳** (לפי דוח מרדכי) | ≤ שעתיים ✅ |
| חריגת-תקרה | הקפאת Claude — כל השיגורים `cli=cursor` | אין ✅ |

## מה נמסר

שלב 1 בלבד: חבילת `packages/acp-wire` (stdio + unix-socket) + listen ב־`cursor-sdk-acp`.  
מוזג ל־**`integration/run-acp-wire`** בשני הריפואים:

- drive-coding @ **`a0572cb4`** (מעל `2e24f0c4`) — לא ב־`edge`/`dev` (`merge-base --is-ancestor` → 1)
- cursor-sdk-acp @ **`2796669`** (מעל `a262ccf`) — לא ב־`main` (exit 1)

כלב **GO 6/6** (`acp-wire-calev.md`, `verdict: GO`).

## סשנים שנפתחו ונסגרו

| agentId | מי | מסלול | נסגר? | ראיה |
|---|---|---|---|---|
| `0686b4f1-674b-4e69-8b93-d1a1412fd616` | מרדכי (cursor / grok-4.6) | MCP · פתחתי אני | ✅ | `session_close` → `{ok:true}` (אחרי מסירה; ילדים כבר נסגרו אצלו) |
| `fbf9f6c5-…` | אביגיל | MCP (ילד מרדכי) | ✅ | דוח מרדכי |
| `3fbf3353-…` | אליעזר | MCP | ✅ | דוח מרדכי |
| `a2497932-…` | כלב | MCP | ✅ | דוח מרדכי |

צופה tmux `acp-wire-watch`: הופסק ב־`DONE` + `kill-session` אחרי מסירה.

## התערבויות-משתמש

| # | מה | סוג |
|---|---|---|
| 1 | בקשת טיק כל 5 דק׳ + חשד שהצופה לא מעיר | **צנרת** — חזר על באג ידוע 30/08 (`notify` חסר; תוקן תוך כדי) |
| 2 | אישור פרה-בריף + `/autorun` | מוצר (בחירת תור) |

**מוצר: 1 · צנרת: 1**

## כשלי-מסירה

אין על תוכן הסלייס.  
(צופה ראשון כתב לוג+`tg` בלי `dispatch-via-api notify` — המתאם לא התעורר; תוקן לפי העובדה מ־30/08 לפני סיום הריצה.)

## מה השערים תפסו

| שער | תפס |
|---|---|
| אביגיל | 3 ממצאים לא-חוסמים → תיקון-במקום |
| כלב | GO 6/6 כולל מוטציית listen |
| **המשתמש** | ממתין — מיזוג החוצה |

## תיקונים קבועים שנוצרו

אין קומיט לשיטה בריצה הזו. הלקח על צופה כבר מתועד ב־basic-memory (30/08); חזרנו עליו בביצוע.

## מה עדיין לא נבדד / מחוץ לסקופ

- Streamable HTTP (שלב 2)
- חיבור BE/sidecar
- מיזוג ל־`edge`/`dev`/`main`

## הערכה

הריצה **החזיקה**: plan-gate סבב אחד, runtime GO, מיזוג לענפי-ההרצה בלבד בשני ריפואים.  
החסם הבא: עיני המשתמשת למיזוג החוצה.
