---
run: 5
date: 2026-08-21
project: InboxSMS
mission: InboxSMS/plans/missions/m2m-api-on-d1.md
slices: [m2m-api-on-d1]
interventions_product: 1
interventions_plumbing: 1
handoff_failures: 1
permanent_fixes: 2
verdict: החזיקה — עם התערבות-מוצר אחת שנכנסה באמצע
---

# דוח-ריצה 5 — m2m-api-on-d1 (InboxSMS)

> ## ⛔ תיקון עובדתי — 2026-08-21
>
> **הטענה שהמיגרציה לא הורצה בייצור ושהודעות נזרקו — שגויה.** נמדד ישירות:
>
> ```
> d1_migrations: 0001_create_messages.sql  applied_at 2026-03-13 11:28:34
> SELECT count(*) FROM messages  →  83   (yemot 53 · zadarma 30)
> newest receive_date            →  2026-08-19T18:02:04Z
> ```
>
> מקור הטעות: `wrangler d1 list` מציג `num_tables: 0` — **שדה מטא־נתונים
> מיושן**, לא ספירה חיה. הוא נלקח כראיה בלי אימות מול `sqlite_master`.
> הכשל המקומי (`no such table`) היה אמיתי, אבל **מקומי בלבד**.
>
> **מה שכן נכון ואומת בייצור:** באג הדדופ. אותה הודעה מ-`PENSlA` שמורה
> **שלוש פעמים** — כי `receive_date` נגזר מ-`Date.now()` והאילוץ לא תפס.
> תיקון הדדופ, תיקון ה-`/v1` ותיקון ה-POST הפגום נשארים תקפים במלואם.


> ⚠️ הדוח על ה*ריצה*, לא על הפרויקט.

**הריצה השנייה על InboxSMS**, והראשונה שבה פקודת-המשימה **שונתה תוך כדי**.

## מה נמסר

סלייס אחד, 15 קומיטים מעל `a94f1f6`, מוזג ל-`integration/run-m2m-api-on-d1`
(`26bff02`). לא נדחף. שלושה אנדפוינטים חדשים + שכבת `pure/` מורחבת.

## התערבויות-משתמש — הספירה

| # | מה נדרש | סוג | היה נמנע אילו… |
|---|---|---|---|
| 1 | המשתמש הוסיף את ה-cron ל-scope **אחרי** שהמשימה ננעלה ⇒ `POST /api/sync` נוסף ב-`23107e1`, כשבריף r3 כבר היה כתוב | **מוצר** | — זו החלטת-מוצר, לא חיכוך. אבל התזמון עלה שלושה סבבי בריף |
| 2 | `BDS_REPORTS` עדיין אינו מוגדר ⇒ הנתיב הוצהר ידנית ב-§8 שוב | **צנרת** | `bds init` — **אותה שורה שנשארה פתוחה מריצה 4** |

**מוצר: 1 · צנרת: 1.**

התערבות 2 היא **חוב מריצה 4**. היא נספרת שוב כי לא נסגרה.

## כשלי-מסירה

| # | הכשל | "X" שנחשב ל-"Y" | עלות |
|---|---|---|---|
| 1 | **אליעזר שיגר את כלב בעצמו** | "המבצע סיים" ≈ "אומת עצמאית" | מרדכי הריץ הכול מחדש בעצמו; אפס אם לא היה |

מבצע שמזמן את המאמת של עצמו אינו שער עצמאי. זה **וריאנט חדש** של כשל
ריצה 4 (כלב שנתן `GO` בלי להריץ מוטציות) — ושניהם מצביעים על אותו שורש:
**המאמת אינו מוגן מפני מי שיש לו אינטרס בתוצאה.**

## מה השערים תפסו — ומה חמק

| שער | תפס | פספס |
|---|---|---|
| מרדכי (עצמי) | מיספור סעיף כפול · **שורת-שער שלא יכלה להיכשל**: "מתעלם מהחנק" נמדד בקוד 200, ומימוש שנחנק מחזיר 200 גם הוא | — |
| אביגיל r1–r3 | 34 ממצאים ב-4 סבבים. ה-🔴 המשמעותי (r3): **חור-כיסוי** — ארבע שורות ה-sync מדדו את *מעטפת* האנדפוינט ולא את מעשהו; שני מימושים שגויים היו עוברים 34/34 | — |
| אביגיל r4 | READY | — |
| כלב | GO | שוגר ע"י אליעזר ⇒ הפרדת-תפקידים הופרה |
| **המשתמש** | — | — |

**‏הממצא של r3 הוא ההוכחה החזקה ביותר לערך של אביגיל בריצה הזאת.** ‏שער בן
34 ‏שורות ירוקות שאינו מבחין בין "‏מושך" ‏ל"‏לא מושך" — ‏זה בדיוק דפוס ריצה 1
(30/30 ‏ירוקים על קוד שבור). ‏נסגר בשורה 35.

## סטייה מפקודת-המשימה — מוצדקת

§8א הורתה "‏ה-TTL עצמו הוא החנק". ‏**Cloudflare KV דוחה `expirationTtl < 60`** ⇒
‏חנק של 5 ‏שניות דרך TTL ‏אינו קיים. ‏המימוש עבר להשוואת-חותמות מעל מפתח עם
TTL ‏של רצפה, ‏בלי לגעת ב-`KVStore` (‏ולכן אינו טריגר-עצירה §6.1).
**‏המדידה הפריכה את המנגנון, ‏לא את המטרה** — ‏וזו הסיבה ש-§6 ‏מנוסח סביב
‏שינוי-ממשק ולא סביב שינוי-מימוש. ‏מתועד ב-`docs/decisions/InboxSMS.md`.

## תיקונים קבועים שנוצרו

| # | התיקון | נכנס ל- | commit |
|---|---|---|---|
| 1 | ‏שורת-שער חייבת להבחין בין המעשה למעטפת — ‏קוד תגובה אינו ראיה שפעולה קרתה | ‏שער הסלייס (‏שורה 35) + ‏הבריף | `fd9246f` |
| 2 | **‏המבצע לא משגר את המאמת.** ‏שיגור כלב הוא של המתכנן בלבד | ‏הגדרת-הסוכן `eliezer` | ⬜ טרם |

⚠️ ‏פתוחים גם **‏שני התיקונים מריצה 4** (`calev` ‏מוטציות · `bds init`). ‏שלושה
‏תיקוני-צנרת פתוחים במצטבר. ‏לפי כלל הפנקס — ‏חייבים להיסגר לפני הריצה הבאה.

## מה עדיין לא נבדק

- **‏הייצור לא תוקן.** D1 ‏שם `num_tables: 0`.
- **‏ימות אינה נגישה מ-Cloudflare** — ‏חסר `::/0` ‏ב-ACL. ‏מקומית עוקפים ב-IPv4.
- **‏אף אחד לא קורא ל-`/api/sync`** — ‏ה-Worker המתוזמן הוא סלייס B.
- **‏זדרמה האמיתית** — ‏השער מזריע דרך webhook עם חתימה מקומית.
- **`MockDB`/`MockKV`** — `vite dev` ‏מספק `platform.env`, ‏ולכן האמיתיים נבחרים.

## הערכה

‏הריצה החזיקה, ‏והשערים עבדו — ‏אביגיל תפסה חור-כיסוי שהיה מגיע לייצור.
‏אבל **‏שני כשלי-המסירה בשתי הריצות האחרונות נוגעים באותו מקום**: ‏עצמאות
‏המאמת. ‏בריצה 4 ‏הוא ויתר על מוטציות; ‏כאן הוא שוגר ע"י מי שנבדק.
‏זה כבר לא מקרה — ‏זו נקודת-תורפה מבנית שדורשת תיקון בהגדרות-הסוכנים.
