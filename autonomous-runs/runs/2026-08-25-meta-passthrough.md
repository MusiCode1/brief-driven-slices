---
run: 15
date: 2026-08-25
project: drive-coding
mission: docs-for-llm/plans/missions/meta-passthrough.md
slices: [meta-passthrough]
interventions_product: 0
interventions_plumbing: 2
handoff_failures: 1
permanent_fixes: 4
plan_rounds: 1 + דלתא
brief_to_dispatch: "00:48"
verdict: הריצה החזיקה
---

# דוח-ריצה 15 — `meta-passthrough` (drive-coding)

> ⚠️ **‏הדוח הזה על ה*‏ריצה*, ‏לא על הפרויקט.**

## ‏שעון ה-plan-gate  🔴

| ‏מדד | ‏ערך | ‏הסף |
|---|---|---|
| ‏סבבי אביגיל עד dispatch | **1 + ‏דלתא** | 1 (+‏דלתא על NEEDS-REWORK ‏עיצובי) ✅ |
| ‏זמן-קיר בריף→dispatch | **48 ‏דק'** (22:47 → 23:35) | ≤ ‏שעתיים ✅ |
| ‏חריגת-תקרה | ‏אין | — |

```
22:28  פקודת-משימה      22:47  בריף + 3 probes     23:03  אביגיל → NEEDS-REWORK
23:13  rewrite          23:25  דלתא → USABLE       23:35  אליעזר (cursor)
23:53  אליעזר סיים      00:11  כלב → PARTIAL       00:22  כלב סבב 2 → GO
00:24  → ענף-ההרצה      00:29  → edge (‏באישור-משתמש מפורש)
```

## ‏מה נמסר

‏סלייс אחד, ‏**7 ‏קומיטים**, ‏מוזג ל-`integration/run-meta-passthrough` ‏ומשם
‏ל-`edge` (`81c5e5aa`) ‏**‏באישור-משתמש**. ‏`dev` ‏לא נגעו בו.
‏שורות-תור 19–21, ‏ו-26 ‏נכנסה תוך כדי — ‏**‏דרך השער, ‏לא כזחילה** (‏להלן).

## ‏התערבויות-משתמש — ‏הספירה

| # | ‏מה נדרש | ‏סוג | ‏היה נמנע אילו… |
|---|---|---|---|
| 1 | *"‏אליעזר וכלב לא קלוד"* | **‏צנרת** | ‏הכלל היה **‏נאכף** ‏ולא כתוב |
| 2 | *"‏מה ה-URL?"* | **‏צנרת** | ‏הפריוויו נמסר עם מנהרה **‏ועם** ‏דגל-טרנספורט |

**‏מוצר: 0 · ‏צנרת: 2.**

🔴 **‏ושתיהן נסיגה.** ‏ריצה 13 ‏סגרה על אפס-צנרת. ‏שתי אלה אינן חדשות —
‏**‏שתיהן היו כתובות במסמכים ולא נאכפו.**

## 🔴 ‏התערבות 1 — ‏הכלל היה כתוב בארבעה מקומות, ‏והקובץ התפעולי סתר אותו

‏"‏אליעזר וכלב על cursor" ‏כתוב ב-`SKILL.md`, ‏ב-`MISSION_TEMPLATE` §4#8, ‏בפקודת-המשימה
‏של הריצה, ‏**‏ובדוח-ריצה 9 ‏שהכריז שזה הפך ל"‏ברירת-מחדל מוצהרת ‏במקום הנחיה
‏שהמשתמש חוזר עליה".**

‏ובכל זאת המשתמש מסר אותה שוב.

**‏השורש אינו שכחה.** ‏`agents/eliezer.md` — ‏הקובץ שהמבצע באמת קורא — ‏פתח ב:

```
## Mode 1 — סינכרוני (Task ממרדכי)
```

‏ארבעה מסמכים אסרו; ‏הקובץ התפעולי **‏הציג את המסלול האסור כברירת-המחדל**.

⇒ **‏תיקון קבוע 1: ‏שער בר-כישלון**, ‏לא שורה נוספת. ‏`BDS_SLICE` ‏לא-מוגדר ⇒
‏הסוכן מחזיר `WRONG-DISPATCH` ‏**‏ומסרב לבצע**. ‏עקיפה מודעת: ‏`IN-PROCESS-APPROVED`.
‏כלב-heavy ‏מחוץ לשער במכוון (‏Opus ‏ב-process, ‏§‏עיקרון-המודלים).

⚠️ **‏השער טרם נבדק מקצה-לקצה** — ‏לא שוגר אליעזר כדי לראותו מסרב, ‏ולא ידוע
‏אם סשן רץ קורא הגדרות-סוכן מחדש בכל שיגור. ‏**‏רשום ולא מוכח.**

## ‏התערבות 2 — ‏פריוויו ש"‏נמסר" ‏ולא היה ניתן לפתיחה

‏המתאם מסר `http://127.0.0.1:4310`. ‏**‏המשתמש אינו על המכונה.**
‏וכשנמסרה כתובת ציבורית — ‏היא הייתה **‏בלי `?sessionTransport=http`**, ‏ולכן טאב
‏חדש נפל ל-WS: ‏**‏המסלול שהסלייс אינו נוגע בו.**

⇒ ‏הבדיקה שהמשתמש הריץ **‏לא בדקה את הסלייс**. ‏זו הצורה "‏ירוק שאינו מעיד"
‏מריצה 1, ‏בלבוש של נקודת-עיניים.

⇒ **‏תיקון קבוע 4**: §7 ‏ב-`MISSION_TEMPLATE` — ‏פריוויו = ‏מנהרה + ‏אימות `curl`
‏חיצוני + ‏דגל-טרנספורט, ‏והכתובת המלאה בגוף המסירה.

## ‏כשל-מסירה אחד — ‏והוא של המתאם על עצמו

‏שורת-DoD ‏דרשה `bun run lint` ‏ירוק. **‏הוא נכשל גם על הבסיס** (258 ‏שגיאות) ⇒
‏`PARTIAL` ‏מלאכותי ‏וסבב-כלב שני. ‏הסלייс עצמו מסיים על 254 — ‏**‏מוריד חוב.**

‏זו ההופעה השנייה של "‏שער אדום-מראש" ‏בתוך יממה (‏ריצה 16 ‏חטפה אותה שוב עם
‏`.svelte-kit/tsconfig.json`). **‏מועמד לבדיקת-טרום: ‏האם השער ירוק על הבסיס?**

## ‏מה שהשער תפס — ‏ושלושתם היו חוסמים

‏אביגיל סבב 1: **NEEDS-REWORK**. ‏החוט ב-HTTP ‏**‏לעולם** ‏אינו נושא את הפועל
‏`tool_call` (‏הליבה כותבת הכל כ-`tool_call_update`), ‏והצרכן שמקנן יושב אך ורק
‏בענף `tool_call`. ⇒ ‏**‏מעגל סגור: ‏גם עם `_meta` ‏מושלם, ‏שום דבר לא היה משתנה.**

‏סבב-דלתא: ‏שער `G3` ‏ירוק **‏רק מפני ששני עודפים ביטלו זה את זה**, ‏והתיקון היה
‏מפילו; ‏והנפילה-לאחור הייתה מייצרת בועה כפולה.

**‏כל השלושה תוקנו לפני שנכתבה שורת-מימוש.**

## ‏הרחבת-scope שעברה דרך השער

‏פקודת-המשימה הוציאה את הקינון (‏שורה 26) ‏**‏במפורש**, ‏עם תנאי-עצירה §6#2:
*"‏אם מתברר שהקינון חייב להיכנס — ‏זה ממצא, ‏דווח; ‏אל תרחיב scope לבד."*

‏החוסם של אביגיל הראה שהוא **‏כן** ‏חייב. ‏התכנון נכתב מחדש ‏וההרחבה הייתה מודעת.
⇒ **‏זו אינה זחילת-scope; ‏זה בדיוק מה שתנאי-העצירה נועד לייצר.**

## ‏הראיה שסוגרת את #50 — ‏הרצה, ‏לא טסט

‏בילד production · `?sessionTransport=http` · ‏פרומפט אמיתי שמפעיל תת-סוכן ⇒
‏בועת-Task אחת, ‏הבן מופיע פעם אחת. ‏`_meta` ‏שרד **10/10** (‏היה 0/10),
‏חוצה-ספקים **‏אפס אבודים** (‏היה 5/5).

## ‏תיקונים קבועים

1. **‏שער-שיגור בר-כישלון** ‏ב-`agents/eliezer.md` ‏ו-`calev.md` (⚠️ ‏לא נבדק e2e).
2. **‏בדיקת-טרום #6** ‏ב-`/autorun` — "‏מי מריץ את מי?", ‏עם ציטוט הפקודה עצמה.
3. **`MISSION_TEMPLATE` §4#8** ‏מתקן את הטענה של דוח 9 ‏שהתערבות זו "‏בוטלה".
4. **`MISSION_TEMPLATE` §7** — ‏פריוויו = ‏מנהרה + `curl` ‏חיצוני + ‏דגל-טרנספורט.

## ‏הערכה

**‏הריצה החזיקה** — ‏שער-התכנון תפס שלושה חוסמים, ‏הרחבת-ה-scope עברה דרך
‏תנאי-עצירה ‏ולא סביבו, ‏והמסירה נשענה על הרצה חיה ‏ולא על טסט.

‏אבל **‏הצנרת עלתה מ-0 ‏ל-2**, ‏ושתי ההתערבויות היו על כללים **‏שכבר היו כתובים**.
⇒ ‏המסקנה התפעולית של הריצה: ‏**‏כלל שאינו נאכף אינו כלל**, ‏ודוח שמכריז
‏"‏ההתערבות בוטלה" ‏מבלי להתקין אכיפה — ‏מכריז מוקדם.
