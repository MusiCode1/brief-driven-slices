---
run: null
counted: false
date: 2026-08-29
project: drive-coding
slices: []
interventions_product: 0
interventions_plumbing: 1
handoff_failures: 2
permanent_fixes: 1
verdict: 🛑 הפלה בבדיקת-טרום — לא שוגר אף סוכן (התוצאה הנכונה)
# frontmatter הוסף בדיעבד 2026-08-31; אינה נספרת ברצף — לא שוגר סוכן
---
# ריצה — `queue-audit` (הפלה בבדיקת-טרום)

> **תאריך**: 2026-08-29 · **פרויקט**: drive-coding · **תוצאה**: 🛑 **לא שוגר אף סוכן**
> **סיבה**: בדיקות-הטרום פסלו את שני הפריטים הראשונים בתור — שניהם כבר בוצעו.

---

## 1 — מה קרה

`/autorun` ללא בחירת-פריט ⇒ «הראשון הפתוח». התור החזיר **#22 `session-scope-core`**.

בדיקת-טרום #1 (הבסיס) + #5 (נתיב-דוחות) חשפו ש-#22 כבר רץ: בריף מאומת,
`session-scope-core-calev.md` עם **GO**, ודוח-ריצה שמצהיר *«מוזג לענף-ההרצה
בלבד · פריוויו חי ממתין לעיני המשתמשת»*.

הפריט הבא, **#26 `subagent-nested`** — **גם הוא** נשא `-avigail` ו-`-calev`.

⇒ שני הראשונים ברצף. **הפלתי את הבחירה וסרקתי את כל התור.**

## 2 — הממצא

מתוך **28** פריטים שהוצגו פתוחים, **8** נשאו verdict של כלב
(7×GO · 1×PARTIAL שנסגר בהכרעת-משתמש) — **29%**.

## 3 — 🔴 האבחנה הראשונה שלי הייתה שגויה, והתיקון הוא הלקח

**כתבתי תחילה** שהפנקס «מתוחזק ידנית ולכן ירקב». **לא נכון.**
`backlog-status.sh:24-35` **מריץ את פקודת-האימות מכל שורה** — הסטטוס נגזר.

**מה שסחף הוא הפרוב.** נמדד על #46:

| | |
|---|---|
| הפקודה בשורה | `grep -c sidebarWidth …/ui-shell.svelte.ts` → **0** |
| איפה הקוד באמת | `…/settings.svelte.ts` → **8** |

הסלייס בוצע — ונחת ב-VM אחר מזה שהבריף חזה. **פרוב שמצביע על המקום
הלא-נכון מדווח «פתוח» לנצח.**

## 4 — וגם האות החלופי שהצעתי אינו אמין

בדקתי «אומת ב-dev» בגרפים אד-הוק משלי. ל-#65 השתמשתי ב-`lastTurnError`
(קיים) בעוד שהמרקר המוצהר הוא `activeTurns` — **שאינו קיים כלל**.
ה-✅ שלי שם נשען על סימן שאינו של הסלייס.

**שני אותות, שניהם סחופים:**

| אות | עונה על | חסר |
|---|---|---|
| `*-calev.md` קיים | אומת **בזמנו, על ענף כלשהו** | לא אומר שהקוד ב-`dev` |
| הפרוב בשורה | הסימן קיים **עכשיו ב-dev** | מצביע למקום שהבריף ניחש |

## 5 — התיקון המומלץ

**מצב שלישי ב-`backlog-status.sh`:** פריט שיש לו `*-calev.md` **והפרוב מחזיר
open** ⇒ `⚠️ פרוב-חשוד`, לא `open`. זה מפריד «לא בוצע» מ«בוצע והפרוב מפספס» —
היום הם נראים זהים, וזה בדיוק מה שכמעט ייצר עבודה כפולה.

## 6 — מדד-בשלות

| מדד | ספירה |
|---|---|
| **התערבויות-מוצר** | 0 |
| **התערבויות-צנרת** | **1** — התור החזיר פריטים מבוצעים כפתוחים |
| **כשלי-מסירה** | **2**, שניהם שלי: אבחנה שגויה על מנגנון הפנקס · מרקר-אימות שאינו של הסלייס |

⚠️ שני כשלי-המסירה **נתפסו בתוך הסבב** ולא דלפו — הראשון כי בדקתי את
`backlog-status.sh` בפועל, השני כי השוויתי מול המרקר המוצהר בשורה.
אך **שניהם היו הצהרה לפני עיגון** — הכלל הגלובלי שכבר קיים.

## 7 — סשנים שנפתחו ונסגרו

| סוכן | נפתח | נסגר |
|---|---|---|
| — | **אפס** | **אפס** |

הפלה בבדיקת-טרום ⇒ לא הגענו לשלב שיגור. אין חזקת-יתום.

## 8 — תוצרים

- `docs-repo/drive-coding/plans/BACKLOG.md` — סעיף ביקורת + מצב מתוקן ל-8 פריטים
  (`9f7da25` · תיקון-האבחנה `2ef388c` · תיקון-פרסר `c639470`)
- 🔴 **#22 חסום עליך** — כלב GO, פריוויו חי ממתין. **חוסם גם 23 ו-24.**

## 9 — מלכודת שנפלתי בה ותועדה

טבלת-סיכום בפורמט `| <מספר> | …` בתוך `BACKLOG.md` **מפורסרת כשורות-פנקס**
ומייצרת «שבורות». הומרה לרשימה. השבורה שנותרה (#64) קדמה לי.

---

**מסקנה:** הסבב לא ייצר קוד — וזו התוצאה הנכונה. בדיקת-הטרום עשתה בדיוק
את מה שהיא קיימת בשבילו: **עצרה לפני שהתחלנו לעבוד על מה שכבר עשוי.**
