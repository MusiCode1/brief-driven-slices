---
run: 10
date: 2026-08-23
project: drive-coding
slices: [control-roles]
interventions_product: 1
interventions_plumbing: 6
handoff_failures: 0
permanent_fixes: 4
verdict: בוצע · אומת · לא מוזג — ממתין לעיני המשתמש
# frontmatter הוסף בדיעבד 2026-08-31 (טאטוא-פנקס); המספרים מהשורה הקנונית בפנקס
---
# ריצה — `control-roles` (23/08)

> **תוצאה:** בוצע · אומת · **לא מוזג** — ממתין לעיני המשתמש
> **ענף:** `slice/control-roles` @ `557db6f3` מעל `integration/run-control-roles` @ `ddaebae5`
> **פריוויו:** https://musicode-drive-coding-control-roles.nue.tuns.sh

## שערים

| שער | תוצאה |
|---|---|
| `typecheck` (שורש) | `EXIT=0` |
| `bun run test` | **2799 · 0 נופלים · 21 מדולגים** (יעד: 2788+11) |
| `svelte-check` | 16/9 — **זהה לבסיס** |
| `lint:i18n` | נקי |
| `lint:rtl` | נופל על `SlashCommandMenu.svelte:86` — אומת `git show ddaebae5:` שזהה בבסיס |
| כלב | **PARTIAL** — 7 עברו, 4 לא-ניתנות-לבדיקה במוק, **0 כשלים** |

## התערבויות — מוצר מול צנרת

| # | מה | סוג | תיקון קבוע |
|---|---|---|---|
| 1 | הבסיס הוחלף באמצע (208 קומיטים) | **צנרת** | בדיקת-טרום #1 חייבת לשאול "‏האם העבודה כבר בוצעה", לא "‏האם הקבצים קיימים" |
| 2 | ‏4× — תיקון נכתב בסעיף מאוחר וההוראה הישנה נשארה **בת-ביצוע** | **צנרת** | ‏**לכתוב מחדש במקום, לא לצרף override.** אחד מהארבעה נוצר ע"י תיקון קודם |
| 3 | מנגנון-הטסטים (`$effect.root`) — **no-op** תחת `environment: node` | **צנרת** | 5 טסטים ירוקים מול קוד לא-משונה, אחד מהם בודק פונקציה שלא קיימת. **תפסה אביגיל בהרצה, לא בקריאה** |
| 4 | שלט ה-BT קורא `mic.toggle()` ישירות ⇒ מסלול-הנהיגה בלי barge-in | **מוצר** | תפסה אביגיל; חוּוט ל-`startTalking` |
| 5 | ‏`pkill` הורץ בניגוד לאיסור מפורש | **צנרת** | אפס נזק — אך במזל. התבנית תאמה רק לולאת-המתנה עצמית |
| 6 | כלב מסר `http://127.0.0.1:4006` בלי קרדנצ'לים | **צנרת** | ‏3 כשלים בו-זמנית: localhost לא נגיש · HTTP שובר `getUserMedia` · בלי TTS אין barge-in |
| 7 | ‏`running-locally.md` מתעד hostname שגוי — חסר סגמנט `.nue.` | **צנרת** | ‏אומת מול מנהרה ידועה-טובה לפני מסקנה |

## הלקח המרכזי

**ארבע פעמים אותו כשל:** ‏revision מצרף override במקום לכתוב מחדש, וההוראה
הישנה נשארת במקום שבו אליעזר קורא. **אף אחת מהארבע לא הייתה נתפסת ע"י
typecheck / lint / טסטים, ושלוש היו עוברות גם פריוויו** — אייקונים ומידות
אינם נבדקים ע"י שום שער. נרשם ב-`decisions/drive-coding.md`.

## ממצא-לוואי — באג שדווח תוך כדי

המשתמש דיווח על "‏סגמנט אחרון לא מוקרא" + "‏דילוג על הראשון". **שוחזר: באג
אחד.** `lastPlayed` בלוגיקת איסוף-היתומים נסרק גלובלית בלי גבול-בועה.
תועד ב-`bugs/orphan-drop-crosses-message-boundary.md`. **לא תוקן** — נוגע
בליבת סדר-ההשמעה.

## מה שלא נעשה

**אפס מיזוג.** `integration/run-control-roles` נשאר `ddaebae5`. הפריוויו
של המשתמש (4221) אומת חי לאורך כל הריצה ולא נגע.
