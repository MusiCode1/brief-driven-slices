---
run: 4
date: 2026-08-20
project: InboxSMS
mission: InboxSMS/plans/missions/d1-migration-dedup.md
slices: [d1-migration-dedup]
interventions_product: 0
interventions_plumbing: 3
handoff_failures: 1
permanent_fixes: 3
verdict: החזיקה — עם שער אחד שדלף ונתפס ע"י מרדכי
---

# דוח-ריצה 4 — d1-migration-dedup (InboxSMS)

> ⚠️ הדוח על ה*ריצה*, לא על הפרויקט.

**הריצה הראשונה מחוץ ל-drive-coding.** זה עצמו הממצא המרכזי: התשתית
האוטונומית קשורה לפרויקט אחד, ופרויקט חדש אינו יכול להיכנס לתור.

## מה נמסר

סלייס אחד, 10 קומיטים מעל `1dd7122`, מוזג ל-`integration/run-d1-migration-dedup`
(‏`cc203f1`). לא נדחף, לא יצא מגבול ההרצה.

## התערבויות-משתמש — הספירה

| # | מה נדרש | סוג | היה נמנע אילו… |
|---|---|---|---|
| 1 | הנתיב `docs-for-llm/plans/` ו-`backlog-status.sh` אינם קיימים ב-InboxSMS ⇒ שלבים 1–3 של הזרימה לא רצו; הסלייס נבחר ידנית | **צנרת** | היה `bds init <project>` שמזריע את מבנה התור |
| 2 | `roadmap.md` §הקשתות אינו קיים ⇒ בדיקת-טרום 3 לא ניתנת למענה | **צנרת** | כנ"ל |
| 3 | `BDS_REPORTS` אינו מוגדר במכונה ⇒ נאלצתי להצהיר נתיב ידנית ב-§8 | **צנרת** | `~/.config/bds/paths.env` היה קיים |

**מוצר: 0 · צנרת: 3.** שלושתן אותה שורש: **הפרויקט לא הוכנס לשיטה, רק הורץ בה.**

## כשלי-מסירה

| # | הכשל | "X" שנחשב ל-"Y" | עלות |
|---|---|---|---|
| 1 | כלב החזיר **GO, 12/12** בלי להריץ את מוטציות 2ב/2ג | "שורת-מוטציה בטבלה" ≈ "המוטציה הורצה" | נתפס ע"י מרדכי שהריץ אותן בעצמו; אפס אם לא היה |

זה **בדיוק** דפוס §8ב: שער שאינו יכול להיכשל מייצר ביטחון. כלב אף ציין
זאת בעצמו — כלומר הכשל אינו בשקיפות אלא בכך ש-`GO` ניתן למרות פער מוצהר.

## מה השערים תפסו — ומה חמק

| שער | תפס | פספס |
|---|---|---|
| אביגיל r1 | 13 (3🔴). ה-🔴 החשוב: הפרדיקט המתוכנן היה **בולע הודעות ימות אמיתיות** | — |
| אביגיל r2 | 10 (1🔴): שורות-השער החדשות ספרו מצטבר ⇒ מוטציה אחת הפילה שלוש | — |
| אביגיל r3 | 8 (2🔴): הטבלה עודכנה אך נוסחת-המעבר שלוש שורות מתחתיה — לא | — |
| אביגיל r4 | READY | — |
| כלב | GO, 0 findings | **מוטציות 2ב/2ג לא הורצו** |
| **המשתמש** | — | — |

ארבעה סבבי אביגיל הם **תפוקה, לא חיכוך**: כל אחד הפיל באג אמיתי בתכנון,
לפני שנכתבה שורת קוד. ה-🔴 של r1 היה מגיע לייצור ובולע הודעות בשקט.

## תיקונים קבועים שנוצרו

| # | התיקון | נכנס ל- | commit |
|---|---|---|---|
| 1 | ‏`GATE_PORT` ‏ולא `PORT` — ‏משתנה שיורש מהסביבה שולח את השער לשרת אחר ומחזיר 404 ‏במקום הכישלון האמיתי | `dev/gate-webhook.sh` + ‏§8ב בפקודת-המשימה | `d1a777f` |
| 2 | ‏כלב חייב **להריץ כל שורת-מוטציה בנפרד** ‏ולהדביק פלט; ‏`GO` ‏אסור עם מוטציה לא-מורצת | ‏הגדרת-הסוכן `calev` | ⬜ טרם — ר' למטה |
| 3 | ‏`bds init <project>` ‏שמזריע `plans/missions/`, `docs-for-llm/plans/`, `roadmap.md` ‏ו-`paths.env` | ‏ריפו-השיטה | ⬜ טרם |

⚠️ **‏2 ו-3 ‏עדיין לא נכנסו.** ‏לפי כלל הפנקס — ‏דוח שלא ייצר תיקון קבוע הוא
תיעוד, ‏לא ניסוי. ‏שתי השורות האלה חייבות להיסגר לפני הריצה הבאה.

## מה עדיין לא נבדק

- **הייצור לא תוקן.** ‏המיגרציה ב-`--remote` ‏לא הורצה; ‏ה-D1 ‏בייצור עדיין `num_tables: 0`.
- **שרשרת הספק האמיתית.** ‏ימות: `API_KEY_ACL_REJECT` (‏המפתח מוגבל ל-IP). ‏זדרמה: `401` (rotation).
  ‏האימות מגיע עד גבול ה-webhook עם חתימה מקומית בלבד.
- **`MockDB` ‏אינו מכוסה** — `vite dev` ‏מספק `platform.env.SMS_DB` ‏ולכן D1 ‏נבחר תמיד.
- **‏שארית מוצהרת:** ‏שתי הודעות זדרמה שונות-באמת עם אותו טקסט/שולח/יעד תוך 10 ‏דק' ‏יתמזגו.

## הערכה

הריצה החזיקה. ‏אפס התערבויות-מוצר, ‏ושלוש התערבויות-הצנרת כולן **‏הקמה חד-פעמית
של פרויקט חדש** — ‏לא חיכוך חוזר. ‏אבל הן אינן "‏אפס", ‏ולכן **‏רצף האפס נשבר**:
‏ריצה 2 ‏הייתה הראשונה, ‏וזו אינה השנייה.

‏המסקנה החדה: ‏השיטה בשלה, ‏**‏ה-onboarding ‏של פרויקט לתוכה — ‏לא**.
