---
run: 6
date: 2026-08-21
project: drive-coding
mission: docs-for-llm/plans/missions/bt-remote-engine.md
slices: [bt-remote-engine]
interventions_product: 2
interventions_plumbing: 2
handoff_failures: 1
permanent_fixes: 2
verdict: החזיקה — דלפה פעמיים בצנרת, אפס דליפה לתוצר
---

# דוח-ריצה 6 — `bt-remote-engine`

> ⚠️ **הדוח הזה על ה*ריצה*, לא על הפרויקט.**

## מה נמסר

סלייס אחד, 4 קומיטים + merge `89f24c48` על `integration/run-bt-remote-engine`.
‏7 קבצים חדשים, 1416 שורות, **אפס diff על קוד קיים** (`git diff --stat 0d5d0cfa..`).
‏`dev` נשאר על `0d5d0cfa`. פריוויו HTTPS חי נמסר למשתמש.

## התערבויות-משתמש — הספירה

| # | מה נשאל/נדרש | סוג | היה נמנע אילו… |
|---|---|---|---|
| 1 | "תן לי את התוכנית, ואחרי שאאשר תרוץ" | **מוצר** | — שער-אישור לגיטימי, לא אמור להיעלם |
| 2 | היפוך היעד: הרכב → D-pad | **מוצר** | — הכרעת-מוצר, בדיוק מה שהמשתמש אמור להכריע |
| 3 | "אליעזר וכלב יהיו ב-CLI קורסור… יש חומר, חפש עם recoll" | **צנרת** | ‏מודל-הביצוע היה **ברירת-מחדל בתבנית**. הפתק בזיכרון קיים מ-20/08 ולא הוזרק — המשתמש נאלץ לחזור עליו וגם להצביע על היכן החומר |
| 4 | "אתה עוד רץ?" | **צנרת** | ‏היה חוזה-קצב מפורש. השיגור חוסם ~110 דק'; האחריות לעדכן הואצלה למרדכי בלי סף-זמן, ולכן "מדי פעם" לא הוגדר ולא נאכף |

**מוצר: 2 · צנרת: 2.** הרצף לאפס-צנרת **לא** נשמר.

## כשלי-מסירה

| # | הכשל | "X" שנחשב ל-"Y" | עלות |
|---|---|---|---|
| 1 | אליעזר דיווח 4 קומיטים; `git log` הראה 2. ‏478 שורות (‏ה-route + 2 רכיבים) ישבו לא-מקומטות. בנוסף typecheck-FE עלה 10→11 בקובץ שלו, והוא לא ראה | "‏דיווחתי שביצעתי" ⇒ "‏העץ מכיל את זה" | ‏נתפס ע"י §8 בתוך התור; ‏0 דליפה למטה |

**‏שים לב שזה בדיוק הדפוס שריצה 1 נשרפה עליו** — ‏ו-§8 תפס אותו הפעם. ‏המנגנון עבד.

## מה השערים תפסו — ומה חמק

| שער | תפס | פספס |
|---|---|---|
| אביגיל (3 סבבים, 20 ממצאים) | **‏2 פגמי-עיצוב שהיו מודדים הפוך את השאלה שהמכשיר נבנה לענות עליה** (‏שחרור שבר-קדם-החזקה כ-`tap`; ‏dedup שמשווה DOWN-ראשון ולכן מת מבנית להחזקות) · **‏3 שערים ריקים**, ‏מהם **‏שתי המוטציות שנוספו כדי לשחזר את שני הפגמים האלה** | — |
| כלב (סטטי + מוטציות) | 5/5 מוטציות נפלו בדיוק בשורה הצפויה | — |
| כלב (ריצה חיה, דפדפן) | 7/7 · ‏ממצא 🟡: המונה לשארית-המוכרזת כיסה מסלול אחד מתוך שניים | — |
| **המשתמש** | ‏טרם — הפריוויו ממתין לו | — |

⚠️ **פער מוצהר:** ‏כלב אימת את `010cef80`. ‏התיקון לממצא שלו (`82c3dae2`) ‏נחת **‏אחריו**
‏ולא עבר סבב-כלב נוסף. ‏אני הרצתי את שער-הטסטים בעצמי על ה-merge: **94/1053 ‏ירוק**
‏מול baseline **92/1031** — ‏כלומר בדיוק +2 קבצים ו-+22 טסטים, ‏ואף שורה קיימת לא זזה.
‏זו ראיה לשער, **‏לא** ‏תחליף לאימות-ריצה של התוספת.

## תיקונים קבועים שנוצרו

| # | התיקון | נכנס ל- | commit |
|---|---|---|---|
| 1 | מודל-הביצוע (‏אליעזר+כלב על `cursor`/`composer-2.5` ‏דרך `dispatch-agent`) ‏הופך ל**‏ברירת-מחדל מוצהרת** ‏ב-`MISSION_TEMPLATE` §4, ‏במקום הנחיה שהמשתמש חוזר עליה | `autonomous-runs/MISSION_TEMPLATE.md` | ‏למטה |
| 2 | **‏חוזה-קצב**: ‏שיגור חוסם מחייב סף-זמן מפורש לעדכון, ‏לא "‏מדי פעם" | `MISSION_TEMPLATE.md` §7 + `prompts/kickoff-mordechai.md` | ‏למטה |

## מה עדיין לא נבדק

- **‏הדבר שהסלייס נבנה בשבילו טרם נמדד.** ‏זה מכשיר-מדידה; ‏ארבע השאלות
  (‏קושחה-מול-אנדרואיד · `MediaRecorder` ‏במסך כבוי · ‏שרידות 20 דק' · ‏מסך נעול)
  ‏נסגרות רק כשהמשתמש ילחץ על ה-D-pad האמיתי.
- ‏ה-engine נבדק על אירועים **‏מוזרקים** ‏בלבד. ‏זה היה תנאי-הבדיקוּת, ‏והוא גם התקרה:
  ‏שער ירוק **‏אינו טוען** ‏שהשלט מתנהג כמו הפיקסטורות.
- ‏מסלול Cursor לא נבחן תחת מכסה מלאה בריצה הזו.

## הערכה

הריצה החזיקה: ‏0 דליפות לתוצר, ‏שני שערים תפסו דברים אמיתיים, ‏וחובת-הראיה תפסה
‏כשל-מסירה בזמן. ‏אבל **‏שתי התערבויות-צנרת מאפסות את הרצף** — ‏ושתיהן מאותה משפחה:
‏**‏ידע שקיים במערכת ולא מוזרק לנקודה שצורכת אותו.** ‏פתק-הזיכרון על Cursor היה שם
‏מ-20/08; ‏חובת-העדכון הייתה בפרומפט אבל בלי סף. ‏החסם הבא אינו "‏עוד ריצה" ‏אלא
‏**‏הזרקה: ‏ברירות-מחדל תפעוליות שייכות לתבנית, ‏לא לזיכרון של המשתמש.**
