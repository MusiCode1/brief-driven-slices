---
run: 17
date: 2026-08-26
project: drive-coding
mission: docs-for-llm/plans/missions/fe-defaults.md
slices: [fe-defaults]
interventions_product: 1
interventions_plumbing: 0
handoff_failures: 0
permanent_fixes: 1
plan_rounds: 1
brief_to_dispatch: "<שעה"
verdict: הריצה החזיקה
---

# דוח-ריצה 17 — `fe-defaults` (drive-coding)

> ⚠️ **‏הדוח הזה על ה*‏ריצה*, ‏לא על הפרויקט.**

## ‏שעון ה-plan-gate  🔴

| ‏מדד | ‏ערך | ‏הסף |
|---|---|---|
| ‏סבבי אביגיל עד dispatch | **1** | ✅ ‏הסבב היחיד — ‏בלי דלתא |
| ‏זמן-קיר בריף→dispatch | **< ‏שעה** | ≤ ‏שעתיים ✅ |

**‏הריצה הראשונה שנכנסה דרך `EnterPlanMode`** — ‏התוכנית הוצגה למשתמש ‏ואושרה
‏**‏לפני** ‏כתיבת פקודת-המשימה. ‏שתי הכרעות-מוצר נסגרו שם ‏ולא באמצע הריצה.

## ‏מה נמסר

‏2 ‏קומיטים, ‏3 ‏קבצים, ‏מוזג ל-`dev` (`eba7ad4c`) ‏**‏באישור-משתמש מפורש**.
‏סוויטה אחרי המיזוג: ‏**263 ‏קבצים · 3,089 ‏בדיקות · 0 ‏כשלים.**
‏`main` ‏ו-`edge` ‏לא נגעו.

## ‏התערבות אחת — ‏בשלב התכנון, ‏ובדיוק במקום הנכון

| # | ‏מה | ‏סוג |
|---|---|---|
| 1 | *"‏אני לא רוצה שהן ישפיעו עליי — ‏רק משתמשים חדשים"* | **‏מוצר** |

‏**‏אפס התערבויות-צנרת.** ‏וההכרעה הזו **‏מחקה** ‏את החלק המסוכן: ‏בלי מיגרציה,
‏בלי דריסה, ‏בלי הכרעת-קדימות. ‏היא הגיעה **‏לפני** ‏שנכתבה שורה.

## 🔴 ‏שלוש "‏עובדות שנמדדו" ‏בפקודת-המשימה — ‏שלושתן שגויות

‏**‏זה הממצא של הריצה, ‏והוא עלי.**

| § | ‏מה טענתי | ‏מה נמדד |
|---|---|---|
| 11ה | ‏שער "‏blob קיים" ‏**‏אדום** ‏על ה-base | ‏**‏ירוק** — ‏ערכי-העבר שווים לברירות-המחדל של ה-base. ‏זהו שער-**‏רגרסיה**, ‏מוכח רק במוטציה |
| 11ד | ‏התווית ב-`SettingsScreen` ‏**‏משקרת** | ‏**‏לא** — ‏`??` ‏מקצר; ‏6/6 ‏צירופים נכונים. ‏הקובץ נשאר מחוץ ל-scope |
| 11ב | ‏`bunx svelte-kit sync` ‏לפני בדיקות | ‏**‏נכשל בעץ טרי** ‏ומוריד חבילת npm ‏זרה |

‏ואביגיל מצאה חוסם רביעי: ‏הפיכת ברירות-המחדל שוברת **8 ‏טסטים ירוקים קיימים**,
‏והבריף טען את ההפך **‏בארבעה מקומות** — ‏כולל שומר-ה-scope ("‏שני קבצים בלבד"),
‏**‏שהיה חוסם את התיקון של עצמו.**

⇒ ‏כל הארבעה נתפסו **‏לפני שנכתבה שורת-מימוש**. ‏אפס כשלי-מסירה.

## 🔴 ‏התיקון הקבוע — ‏והוא מתקן אותי

```
$ mkdir /tmp/probe && cd /tmp/probe && bunx svelte-kit sync
error: could not determine executable to run for package svelte-kit
→ ‏הוריד svelte-kit@1.2.0 — ‏חבילת npm ‏זרה
```

‏הפצתי את ההנחיה הזו ל**‏שלוש** ‏פקודות-משימה ‏ולתבנית. ‏היא עבדה אצלי **‏רק
‏מפני שהרצתי `bun install` ‏לפניה** — ‏צעד שלא תיעדתי.

‏**‏הכישלון נראה כמו בעיית-סביבה, ‏לא כמו פקודה שגויה** — ‏ולכן היה עולה סבב
‏לכל מי שנתקל בו.

⇒ **`MISSION_TEMPLATE` §8ו** ‏החדש: ‏`bun install` ‏קודם, ‏ואז הבינארי המקומי.
‏זו דוגמה ל-§8ה ("‏מה שנכתב לשער — ‏מורץ") ‏**‏על מי שכתב את §8ה.**

## ‏הערכה

**‏הריצה החזיקה, ‏והשער עשה את כל העבודה.** ‏ארבעה פגמים — ‏שלושה שלי ‏ואחד
‏של הבריף — ‏נתפסו בסבב אביגיל **‏יחיד**.

‏המסקנה: ‏§11 ("‏עובדות שנמדדו") ‏הוא הסעיף שהכי נוטה להיות **‏מדוד-למחצה**.
‏שלוש הטענות שנפלו כאן נראו כמו מדידות ‏— ‏שתיים היו קריאת-קוד, ‏ואחת הרצה
‏בסביבה שאינה זו של המבצע.
