---
run: 7  # תוקן 31/08 למספר הקנוני מהפנקס (פער 4)
date: 2026-08-22
project: InboxSMS
mission: InboxSMS/plans/missions/dedup-by-provider.md
slices: [dedup-by-provider]
interventions_product: 1
interventions_plumbing: 1
handoff_failures: 0
permanent_fixes: 3
verdict: החזיקה — אפס כשלי-מסירה, והשער תפס את עצמו
---

# דוח-ריצה 6 — dedup-by-provider (InboxSMS)

> ⚠️ הדוח על ה*ריצה*, לא על הפרויקט.

**הריצה הראשונה שבה כל התכנון נשען על מדידות ולא על השערות** — וזו גם
הריצה שבה לא היה אף כשל-מסירה.

## מה נמסר

סלייס אחד, 12 קומיטים מעל `b7cf2f6`, מוזג ל-`integration/run-dedup-by-provider`
(`abfe253`). לא נדחף.

## התערבויות-משתמש — הספירה

| # | מה נדרש | סוג | היה נמנע אילו… |
|---|---|---|---|
| 1 | "לא למחוק קוד מדידה, רק לבטל — אולי עם משתנה סביבה" | **מוצר** | — החלטת-מוצר. נכנסה לפני נעילת המשימה, ולכן לא עלתה כלום |
| 2 | `BDS_REPORTS` עדיין אינו מוגדר | **צנרת** | `bds init` — **חוב פתוח מריצה 4, שלישית ברציפות** |

**מוצר: 1 · צנרת: 1.**

## כשלי-מסירה

**אפס.** הראשונה מאז ריצה 2.

## מה השערים תפסו

| שער | תפס |
|---|---|
| מרדכי (עצמי, לפני הביצוע) | **שלושה פגמים בשער שאני כתבתי**: שורה שלא יכלה לעבור לעולם (`grep -c … \|\| echo 0` מחזיר `"0\n0"` דווקא בהצלחה), סעיף ימות שעבר ריק, וחור שהיה **מתגמל מחיקה** של קוד המדידה |
| אביגיל r1–r3 | 29 ממצאים, 4 חוסמים |
| אביגיל r4 | READY |
| מרדכי (אחרי הביצוע) | **באג נשא במילישניות** — `bumpReceiveDate` הפיק `…00.1000Z`, חותמת פסולה ל-D1 |
| כלב | GO · 12/12 DoD · 7 מוטציות |

**‏הממצא החשוב ביותר הוא עצמי-התייחסותי:** ‏השער שאני כתבתי הכיל שורה
‏שהייתה **‏מתגמלת מחיקה** ‏של קוד המדידה — ‏כלומר מימוש שמפר את §3.1 (‏האילוץ
‏המפורש של המשתמש) ‏היה עובר. ‏נתפס לפני הביצוע.

## סטייה מפקודת-המשימה — ‏מוצדקת ומוסלמת

§8ב ‏ביקש מפתח ימות בן **‏חמישה** ‏שדות; ‏המימוש משתמש ב**‏שלישייה** ‏שכבר בסכימה.
‏הנימוק: ‏השלישייה **‏מכילה** ‏את החמישייה, ‏ולכן פרדיקט-חמישייה לא היה יכול
‏לדחות דבר — ‏שוב "‏שורה שאי אפשר להפיל". ‏המחיר: ‏שתי הודעות שונות מאותו שולח
‏באותה שנייה ייבלעו במסלול ימות. ‏נמדד: ‏**‏אפס מקרים ב-53 ‏שורות אמיתיות**.
‏התיקון מחייב בניית-טבלה-מחדש (‏לא אדיטיבי) ⇒ ‏טריגר §6.1 ⇒ **‏מוסלם למשתמש**.

## תיקונים קבועים שנוצרו

| # | התיקון | נכנס ל- | commit |
|---|---|---|---|
| 1 | `wrangler d1 migrations apply` **מדלג אחרי `DROP TABLE`** — ‏פנקס המיגרציות אינו מתאפס. ‏להקמה: `d1 execute --file=…` | §8ד ‏בפקודת-המשימה + ‏הרנבוק | `a34e2e5` |
| 2 | ‏שורת-שער שהשוותה `ERR == ERR` **‏עברה**. `chk` ‏נכשל עכשיו על `ERR` | `dev/gate-dedup.sh` | `a34e2e5` |
| 3 | ‏שורת-שער חייבת להיבדק גם מול **‏המימוש הפוגע שהיא אמורה לאסור** — ‏לא רק מול הנכון | §8ד | `579d8ec` |

⚠️ **‏שלושת תיקוני-הצנרת מריצות 4–5 ‏עדיין פתוחים** (`bds init` · ‏מוטציות כלב ·
‏המבצע לא משגר את המאמת). ‏זו הפעם השלישית ש-`bds init` ‏נספר.

## מה עדיין לא נבדק

- ‏הייצור **‏לא נפרס** ‏בסלייс הזה.
- ‏שש ההודעות שנבלעו **‏אינן ניתנות לשחזור**.
- ‏התנהגות זדרמה נמדדה על **‏מדגם אחד** (7+2 ‏מסירות).
- `MockDB`/`MockKV` ‏אינם מכוסים.

## הערכה

‏הריצה הטובה ביותר עד כה: ‏אפס כשלי-מסירה, ‏ותכנון שנשען כולו על מדידות
‏שנעשו לפני הכתיבה. ‏ההבדל מריצות 1–5 ‏אינו במזל — ‏הוא בכך שהפעם
**‏העובדות נאספו לפני הבריף, ‏לא במהלכו**.

‏אבל `bds init` ‏נספר בפעם השלישית. ‏מדד-הבשלות לא יוכל להשתפר כל עוד
‏אותה שורה חוזרת.
