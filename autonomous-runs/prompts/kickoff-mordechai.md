# פרומפט-שיגור — מרדכי, ריצה אוטונומית

מוחלף `<...>` ונשלח כ-`Task({ subagent_type: "mordechai", ... })`.
**חוסם או ברקע?** **ברקע, תמיד** — ‏ראה §אזהרות למטה ‏ו-`SKILL.md §שיגור לא-חוסם`.

---

```
בצע את פקודת-המשימה: <נתיב>/plans/missions/<שם>.md

Project root: <נתיב>
Base: <ענף> @ <hash>
ענף-ההרצה: integration/run-<שם>

קרא את המסמך במלואו לפני שאתה עושה משהו. הוא הרובד מעל הבריף — הוא לא
מתכנן מימוש, הוא נותן לך סמכות וגבולות.

זו סמכותך המלאה. אל תשאל את המשתמש דבר שכתוב שם:
- §4 הוא ההרשאה שלך — כל ה-defaults מאושרים.
- §5 הן התקרות שלך.
- §6 הם התנאים היחידים לחזור למשתמש לפני §7.
- §7 הן נקודות-העיניים. בין השתיים — אל תעצור.
- §8 היא חובת-הראיה. כל טענת-סטטוס עם הפקודה שהפיקה אותה והפלט שלה.

שיגור צאצאים: **ברקע תמיד** (`run_in_background: true`), אחד-אחד. השרשור
הראשי נשאר פנוי לשאלות המשתמש. אל תמתין בהמתנה-חוסמת ואל תקרא את
הטרנסקריפט של הצאצא — עקוב בראיות בעץ (`git log` על ענף-הסלייס,
`$BDS_REPORTS/<project>/`, `tmux ls`). תדווח כשיש תוצאה מאומתת.

> 🔴 **‏ואחרי שיגור ל-`dispatch-agent` — ‏אתה מחזיק את ההמתנה. ‏אינך מסיים תור.**
> ‏**‏פער 1 ‏ב-`autonomous-runs/OPEN-GAPS.md`, ‏נצפה פעמיים בריצה 19**: ‏התור שלך
> ‏הסתיים בעוד ילד ה-tmux ‏רץ, ‏ואיש לא החזיק את ההמתנה ⇒ ‏הסבב נתקע **‏בשקט**.
>
> ```bash
> scripts/await-dispatch.sh <שם-השיגור> \
>   --repo <project-root> --branch slice/<שם> --base <hash> --expect-commits <N>
> ```
>
> ‏קרא אותו **‏שוב ושוב** ‏עד שהוא מחזיר משהו שאינו `2`:
>
> | ‏קוד | ‏משמעות | ‏מה לעשות |
> |---|---|---|
> | **0** | ‏הושלם **‏ואומת** — ‏קומיטים תואמים, ‏עץ נקי | ‏המשך לשער הבא |
> | **2** | ‏עדיין רץ (‏פג ה-timeout ‏של הקריאה) | ‏**‏אינו כישלון** — ‏קרא שוב |
> | **3** | ‏הסנטינל ירה אך העבודה **‏חלקית** | 🔴 **‏אל תשגר מאמת.** ‏שגר המשך |
>
> ⚠️ **‏הסנטינל הוא אות-חיוּת בלבד.** ‏בריצה 19 ‏`acpx` ‏יצא `exit=0` ‏אחרי
> ‏`RetriableError: http/2 stream closed (CANCEL)` ‏**‏באמצע C2** — ‏שני קומיטים
> ‏מתוך שלושה ועץ מלוכלך. ‏הסקריפט מכריע לפי **‏ארטיפקט** (`git log`/`status`),
> ‏ולכן הוא מחזיר `3` ‏בדיוק במצב הזה. ‏**‏אל תסיק סיום מקוד-היציאה של `acpx`.**

> ## ‏שני מסלולי-שיגור — ‏בחר במודע
>
> | | `dispatch-agent` (tmux) | `dispatch-via-api.mjs` |
> |---|---|---|
> | ‏תעבורה | acpx ‏ב-tmux | ‏ה-API ‏של drive-coding |
> | ‏נראוּת | ‏חלון tmux ‏על המכונה | ‏**‏ברשימת-הסוכנים, ‏עם URL, ‏מכל מכשיר** |
> | ‏אות-סיום | ‏סנטינל `.done` (‏**‏משקר** — ‏ר' פער 2) | ‏`state_update`+`stopReason` ‏בזרם |
> | ‏סגירה | ‏ידנית | ‏אוטומטית **‏בהצלחה בלבד** |
> | ‏דורש | ‏— | ‏BE ‏חי עם `env`+`permissionPolicy` (‏סלייс 59) |
>
> ```bash
> scripts/dispatch-via-api.mjs --base http://127.0.0.1:4050 \
>   --cli cursor --model composer-2.5 --cwd <worktree> --prompt-file <file> \
>   --env BDS_SLICE=<‏סלייס> --permission allow_once \
>   --public-url https://musicode-dc-edge.nue.tuns.sh --file <‏דוח> --timeout 1800
> ```
>
> ‏מדפיס **‏קישור לצפייה** ‏מיד אחרי היצירה, ‏ממתין, ‏וסוגר את הסוכן **‏רק אם הצליח**.
> ‏בכישלון הסוכן **‏נשאר חי** ‏והקישור מודפס — ‏כדי לא למחוק את הראיה.
> ‏אימות חי 27/08: ‏`exit 0` · `stopReason=end_turn` · ‏9 ‏פריימים.

מיזוגים: slice → ענף-ההרצה מותר לך ואינו דורש אישור. ענף-ההרצה החוצה —
המשתמש בלבד.

עדכוני-משתמש (§7 חוזה-קצב): tg בכל מעבר-שער, **ובכל 30 דקות שקט גם בלי
מעבר-שער** — שורה אחת: מה רץ, מתי הושק, מה הבא. --title חובה.

<הערות ספציפיות לריצה: חריגות מאושרות, סלייס שדורש כיסוי מורחב, וכו'>
```

---

## אזהרות שנקנו בדם (ריצה 1)

1. ~~**שיגור-ברקע קוטע צאצאים.**~~ **נמשכה 25/08 — ההכללה הייתה רחבה מדי.**
   הראיה עצמה נכונה (הורה סיים תור כדי לדווח → צאצאים מתו, 3 סבבים), אבל היא
   נמדדה על **המתנת-סנטינל בבש**, שחייבת תור חי — ולא על שיגור דרך הרנס,
   ששורד סגירת-תור ומודיע בסיום. **הכלל היום הפוך: כל שיגור ברקע**
   (`SKILL.md §שיגור לא-חוסם`). מה שנשאר תקף מהלקח: אחרי שיגור **אסור
   להיעלם** — עוקבים בראיות בעץ, לא ב"שיגרתי, נחכה".
2. **לציין את ענף-ההרצה מראש.** בלעדיו מרדכי ימזג לענף המשותף, בהיגיון
   סביר לחלוטין, בלי אישור.
3. **חריגה מאושרת מ-plan-gate מחייבת פיצוי בשער הבא.** אם סלייס יוצא בלי
   אביגיל — לכתוב במפורש שכלב צריך לכסות גם את מה שהיא הייתה תופסת.
4. **לתקן טעות נקודתית לא מייצר הרגל.** אחרי שנאמר לו "בדוק מול git" הוא
   בדק — ואז נכשל על קובץ-דוח שאינו בריפו. רק כלל **כללי** (§8) סוגר את זה.
5. **משתני-סביבה גנריים דולפים בעץ-התהליכים.** `PORT`, `FE_STATIC_DIR`
   ו-`NODE_ENV` נורשים מהסוכן להורה ומפנים את הסלייס לשרת/בילד של **סוכן
   אחר**. נמדד בריצה 13. בשיגור: להצהיר על הפורטים והנתיבים במפורש, ולוודא
   `env | grep -E 'PORT|FE_STATIC|NODE_ENV'` לפני שמאמינים לשער חי.
