# סקירת-תהליך — 2026-08-31

> **מי כתב**: Claude (Opus, סשן ייעודי בבקשת המשתמש) · **מה נסקר**: הפנקס ודוחות
> 29 הריצות (`autonomous-runs/runs/`), `OPEN-GAPS.md`, יומן ההחלטות האוטונומיות
> (`docs-repo/drive-coding/plans/autonomous-decisions-log.md`), `BACKLOG.md` של
> drive-coding, `patterns.md`, `learnings.md`, מפת הבאגים, היסטוריית git
> (512 קומיטים ב-3 שבועות על dev), ומצב ענפי-האינטגרציה בפועל (נמדד).
>
> **מה המסמך**: ניתוח-ייעול של אופן העבודה עם הסוכנים בתקופה האחרונה —
> ממצאים מדורגים לפי מינוף, כל אחד עם ראיה ותיקון קונקרטי.

---

## תמונת-מצב: המערכת התכנסה — הצוואר זז

המספרים מספרים סיפור ברור. התערבויות-צנרת לאורך הפנקס: ריצה 1 — **6**,
ריצות 10–20 — תנודתי (0–6), ריצות 26–29 — **0·1·1·1**. סבבי-אביגיל ירדו
מ-3–4 לסבב אחד קבוע מאז היפוך ה-plan-gate (23/08). `brief_to_dispatch` ירד
ל-6–16 דקות. ריצות 26–27 סגרו את כל הסשנים עם ראיות. **מנגנון-הייצור כמעט
בשל** — וזה בדיוק מה שמזיז את צוואר-הבקבוק למקום אחר.

---

## הממצאים, לפי מינוף

### 1 · צוואר-הבקבוק עבר מהצנרת אל שער-העיניים — ומצטבר חוב-מיזוג ממשי

נמדד (31/08): **9 ענפי-אינטגרציה עם קומיטים שממתינים לעיניים**, ביניהם
`run-acp-wire-be` (**45** קומיטים), `run-acp-wire` (33),
`run-cascade-close-children` (32), `run-http-transport-gaps` (15),
`run-playlist-nav-chrome` (11) — **~150 קומיטים** מחוץ ל-dev. כמעט כל
דוח-ריצה אחרון מסתיים באותו משפט: *"החסם הבא: עיני המשתמשת למיזוג החוצה."*

זה לא כשל — זה השער שתוכנן. אבל בקצב של 2–3 ריצות ביום מול אישור אחד כל
כמה ימים, הפער רק גדל, וענף שממתין שבוע צובר קונפליקטים מול dev שזז.

**הצעות:**
- **תור-פריוויו נגזר, לא כתוב ביד** — סקריפט במתכונת `backlog-status.sh`
  שמדפיס: ענף · כמה קומיטים לפני dev · גיל · האם יש `*-calev.md` עם GO ·
  מה נדרש לראות. מסמך "מה מחכה לעיניים" חי, במקום פסקת-סיום בכל דוח.
- **מפגשי-אישור מקובצים**: פריוויו אחד שמגיש כמה ענפים ברצף (אותו BE,
  החלפת `FE_STATIC_DIR`), במקום טקס נפרד לכל סלייס. חצי שעה סוגרת 4–5 ענפים.
- **היגיינת ענפים**: שורת ענפי `integration/run-*` ב-0 קומיטים לפני dev
  שכבר מוזגו ולא נמחקו. `worktrees:prune` מטפל ב-worktrees; אין מקבילה
  לענפים. שווה להוסיף.

### 2 · הפנקס — הלב של מדד-הבשלות — עדיין נכתב ביד, וכבר שבור

פער 0+4 מ-`OPEN-GAPS.md` מאובחן במדויק ועדיין לא נסגר, והראיות בפנקס עצמו:
**ריצה 23 מופיעה פעמיים עם ספירות סותרות** (`2` צנרת בשורה אחת, `1` בשנייה),
ריצה 18 חסרה לגמרי, וההיסטוריה כוללת שלוש כפילויות-מספור. כל הניסוי נשען
על המדד הזה — "שתי ריצות רצופות נקיות" — ואי אפשר לספור רצף על טבלה עם
שורות כפולות וחסרות.

**זה התיקון הזול-ביותר-בעל-התשואה-הגבוהה-ביותר**: הדוחות האחרונים כבר
נושאים frontmatter מלא (`run`, `interventions_product`,
`interventions_plumbing`, `handoff_failures`…) — סקריפט של ~50 שורות שמרנדר
את הפנקס מ-`runs/*.md` סוגר את פער 0, פער 4, ואת הצעת עמודת "שטח חדש?"
(להוסיף `new_territory:` ל-frontmatter) במכה אחת. העיקרון כבר הופעל בהצלחה
על `BACKLOG.md` — רק להחיל אותו על הפנקס.

### 3 · הצופה נכתב מחדש כל ריצה — וחוזר על באגים שכבר תועדו

בריצה 27: *"צופה ראשון כתב לוג+`tg` בלי `dispatch-via-api notify` — המתאם
לא התעורר"* — חזרה מדויקת על באג שתועד ב-basic-memory יום קודם (30/08).
ועמודת "תיקונים קבועים" של אותה ריצה: **0**. הלקח קיים בזיכרון אבל הצופה
נכתב JIT בכל ריצה מחדש — וזה בדיוק הדפוס שהמסמכים כאן עצמם מגדירים ככשל:
*"ערנות אינה מנגנון"*, *"טקסט שאוסר אינו אוכף"*.

**הצעה:** צופה קנוני אחד — `scripts/watch-dispatch.sh` לצד
`await-dispatch.sh` — שכבר מגלם notify + גלאי-קיפאון (גיל-טרנסקריפט, הפער
הפתוח מריצה 14), ואיסור בתבנית על כתיבת צופה ad-hoc. ההתערבות-צנרת הבודדת
שנשארה בריצות 27–29 היא בדיוק ממשפחת ההמתנה/צופה — זה הפריט שסוגר את
תנאי-היציאה.

### 4 · ידע מעופש ב-`learnings.md` — ✅ טופל (31/08)

`docs-for-llm/learnings.md` (drive-coding) הכיל: *"gotcha: BE חייב OneCLI"*
— טענה ש-`AGENTS.md` משך במפורש ב-⛔ ב-26/08 אחרי שעלתה זמן אמיתי — וכן
*"claude CLI אינו ACP-compliant, רק opencode עובד"*, שהופרך בשטח (קיים ספק
claude מלא ב-`packages/provider/src/providers/claude/`).

**תיקון עובדתי לגרסה הראשונה של הדוח:** הקובץ *אינו* מוזרק בפועל — השורה
ב-`opencode.jsonc` מוערת מאז 05/2026 ("migrated to opencode-memfs"). הכותרת
של הקובץ עצמו היא שטענה שהוא נטען, וגם היא הייתה מעופשת.

**בוצע (31/08):** שתי הרשומות נמשכו ⛔ בגוף עם הראיות (כלל 60), ובראש הקובץ
נוספה הערת-סטטוס: קובץ היסטורי, אינו מוזרק, ידע חי → AGENTS.md + basic-memory.

### 5 · הזיקוק נעצר לפני העידן הנוכחי — והטקסונומיה כבר לא מתארת את הכשלים

`distillations/` — אחרון מ-**03/08**, לפני שכל 29 הריצות האוטונומיות קרו.
`patterns.md` בנוי על כשלי-*מבצע* (Slice 9, מאי), אבל הערת ריצות 16–17
בפנקס אומרת במפורש: *"החוליה החלשה עברה במעלה הזרם, אל התכנון"* — 3/3
מההנחות שנפלו היו ב-§11 של פקודת-המשימה, "נראו כמדידות; בפועל היו
קריאת-קוד". הכשל הדומיננטי היום הוא **הצהרה מדודה-למחצה של המתכנן**,
וקטגוריה כזו לא קיימת בטקסונומיה.

**הצעה:** זיקוק חדש שהקלט שלו הוא דוחות הריצות + דוחות אביגיל/כלב מאוגוסט,
עם טקסונומיה למעלה-הזרם: מה נחזה כ"נמדד" ולא היה · אילו סוגי §11 נופלים ·
איזה שער תפס. יש כבר TEMPLATE ותשתית — רק להריץ.

### 6 · שני תיקונים קטנים שכבר אובחנו ורק מחכים למימוש

- **`⚠️ פרוב-חשוד` ב-`backlog-status.sh`**: פריט עם `*-calev.md` שהפרוב
  שלו מחזיר "פתוח" (המקרה של #46 — הפרוב מצביע על קובץ שהקוד כבר לא בו).
  כבר קרה פעמיים גם בכיוון ההפוך (פריטים 3–4 בבאקלוג היו חיובי-שווא).
  זו הגנה על הכלי שכבר הוכיח את עצמו.
- **סגירת פער 6 (מות-זרם ב-idle)**: התיקון (timeout 60s) מסומן "השערה עם
  מנגנון, לא מדידה" — אבל ריצות 26–29 רצו איתו על Cursor בלי מקרה-מוות
  אחד. זו בדיוק המדידה שחסרה; שווה לרשום את הסגירה במסמך במקום להשאיר
  🔴 פתוח שכבר אינו.

---

## מה דווקא לשמר (עובד, ואל תגעו)

- **שערים שבודקים ארטיפקט ולא דיווח** — `await-dispatch.sh`, בדיקת
  `git log` לפני קבלת דוח, "מספר בשורת-DoD נמדד מחדש". זה הלקח העמוק
  ביותר של כל המסלול והוא מוטמע היטב.
- **BACKLOG נגזר-מפקודות** — כבר תפס שני חיובי-שווא ורקב-סטטוס. להרחיב
  (ממצאים 1–2 למעלה הם בדיוק "עוד מאותו דבר").
- **גיוון ספקים** — הקפאת קלוד לא עצרה את הקו; Grok כמתכנן נמדד כשיר.
  החוסן הזה הוא נכס.
- **מבנה סשן-לכל-תפקיד עם חובת-סוגר** — ריצות 26–27 סגרו הכול עם ראיה.
  פער 7 נסגר בפועל.

## סדר-תקיפה מומלץ

| # | פעולה | עלות | מה נסגר | סטטוס (31/08, אחרי אישור-המשתמש) |
|---|---|---|---|---|
| 1 | סקריפט פנקס-נגזר מ-frontmatter | ~שעה | פערים 0+4, אמינות מדד-הבשלות | ✅ `scripts/render-run-ledger.py` · frontmatter הושלם ל-4 דוחות · מספור יושר (9b/23b/23c) · README מצביע עליו |
| 2 | תור-פריוויו נגזר + מפגש-אישור מקובץ | ~שעה + טקס | חוב הקומיטים, שער-העיניים | ✅ `docs-repo/drive-coding/plans/preview-queue.sh` — מדד 13 ענפים · 249 קומיטים + 16 ענפים למחיקה |
| 3 | `watch-dispatch.sh` קנוני | ~שעתיים | ההתערבות-צנרת האחרונה שנשארה | ✅ `scripts/watch-dispatch.sh` (נבדק: סיום + קיפאון) · חווט ב-MISSION_TEMPLATE + kickoff · פער 9 נפתח · פיצ'ר-BE ב-roadmap |
| 4 | טאטוא `learnings.md` | חצי שעה | ידע מופרך במסמכים | ✅ שתי משיכות-⛔ עם ראיות + הערת-סטטוס (הקובץ ממילא לא מוזרק — הכותרת שלו שיקרה) |
| 5 | זיקוק אוגוסט + טקסונומיית-מתכנן | ריצת-סוכן | patterns.md רלוונטי לעידן הנוכחי | 🔄 סוכן-זיקוק שוגר 31/08 → `distillations/2026-08-31-report.md` + סעיף ב-patterns.md |
| 6 | פרוב-חשוד + סגירת פער 6 | קטן | היגיינת מנגנונים קיימים | ✅ `backlog-status.sh` מסמן 🟡 (מצא 11 מיד) · פער 6 ✅ אומת בריצות 26–29 |

**השורה התחתונה**: הניסוי כמעט עמד בתנאי-היציאה שלו — ההתערבות-צנרת
שנותרה היא כמעט תמיד "צופה/המתנה", והחוב האמיתי כבר אינו בייצור אלא
בזרימת-האישור החוצה.
