# ‏פערים פתוחים במנגנון-ההרצה

> **‏נכתב**: 2026-08-27, ‏אחרי ריצה 19 (`rpc-wait`) · **‏מקור**: ‏הכרעת-משתמש —
> "‏תתעד את הבעיות בהרצה, ‏נפתור את זה בריפו".
> **‏מה המסמך הזה**: ‏רשימת-עבודה **‏בת-סגירה**, ‏פריט-פריט. ‏לא רטרוספקטיבה —
> ‏לכל שורה יש **‏קובץ שישתנה**. ‏דוחות-הריצה מתעדים מה קרה; ‏זה מתעד מה לתקן.
>
> ‏פריט נסגר כשהתיקון **‏נמצא בקוד/בתבנית ואומת בריצה אחת לפחות** — ‏לא כשנרשם.

---

## ‏מצב הבר (‏`README §תנאי-היציאה`)

> **‏שתי ריצות רצופות עם 0 ‏כשלי-מסירה ו-0 ‏התערבויות-צנרת.**

```
16 · msg-coalesce   1 / 0   ·  0   ← ‏עומד בבר
17 · fe-defaults    1 / 0   ·  0   ← ‏עומד בבר
18 · live-voice     ‏אין שורה בפנקס        ← ‏ר' פער 0
19 · rpc-wait       1 / 3   ·  2   ← ‏שבר את הרצף
```

🔴 **‏הבר כמעט נחצה ב-17, ‏ואז נשבר פעמיים.** ‏הרצף עומד היום על **‏אפס**.

⚠️ ‏וקריאה שחייבת להיכנס לפני שמסיקים מהמספרים: ‏**‏האפסים של 11/16/17 ‏היו על שטח
‏מוכר.** ‏ריצה 19 ‏פגשה שני תנאים שמעולם לא נוסו — ‏מוות של תעבורת-הספק **‏באמצע**
‏הביצוע, ‏ובריף שנוצר במסלול חדש (‏מיזוג בייק-אוף). ⇒ ‏רצף-אפסים על שטח מוכר הוא
‏**‏אינדיקציה חלשה**, ‏והנסיגה 0→3 ‏אינה שבירה אלא **‏חשיפה**.

---

## ‏פער 0 🟡 — ‏ריצה בלי שורה בפנקס  ·  **‏תיקון נחת 31/08, ‏טרם אומת בריצה**

> **‏התיקון (31/08)**: ‏`scripts/render-run-ledger.py` — ‏הפנקס נגזר מ-frontmatter
> ‏של `runs/*.md`. ‏ריצה 18 ‏קיבלה frontmatter ‏בדיעבד ונספרת; ‏`--check` ‏נכשל על
> ‏דוח בלי frontmatter. ‏אומת על 34 ‏דוחות, ‏אפס אזהרות. ‏נסגר סופית בריצה הבאה
> ‏שכותבת frontmatter ‏ומריצה את הסקריפט.

**‏הראיה**: ‏ל-`runs/2026-08-27-live-voice.md` ‏יש דוח, ‏ו**‏אין לו שורה בפנקס**.
‏באותה ריצה בדיוק התרחש סוג-התקרית החמור ביותר שהמערכת מכירה: ‏שני מבצעים
‏**‏הנפיקו לעצמם דוח בשם `calev`** ‏והצהירו GO ‏ו-PARTIAL ‏בלי ששוגר מאמת
(‏מתועד ב-`MISSION_TEMPLATE §8`).

⇒ **‏הריצה עם התקרית החמורה ביותר אינה נספרת במדד.** ‏לא זדון — ‏**‏חור-מדידה**.
‏רצף של אפסים שמדלג על ריצה אינו רצף.

**‏התיקון**: ‏שורה בפנקס **‏נגזרת מקיום דוח-ריצה, ‏לא נכתבת ביד** — ‏אותו עיקרון
‏שכבר הופעל על עמודת-הסטטוס ב-`BACKLOG.md` ‏של drive-coding.
**‏קובץ**: ‏`scripts/` (‏סקריפט חדש, ‏במתכונת `backlog-status.sh`) + ‏`README.md`.
**‏בדיקת-סגירה**: ‏הסקריפט מריץ על `runs/*.md` ‏ומדפיס שורה שאין לה מקבילה בפנקס.

## ‏פער 1 🟡 — ‏אף אחד לא מחזיק את ההמתנה בין תורות  ·  **‏תיקון נחת 27/08, ‏טרם אומת בריצה**

**‏הראיה**: ‏**‏פעמיים בריצה 19** ‏תורו של מרדכי הסתיים בעוד ילד ה-tmux ‏רץ
(‏אחרי `rpcwait-exec` ‏ואחרי `rpcwait-exec2`), ‏וההורה סיפק משגיח ידני.

⇒ ‏בלי צופה חיצוני **‏הסבב נתקע בשקט אחרי כל שיגור**. ‏השרשרת אוטונומית
**‏בין דחיפות**, ‏לא מקצה-לקצה. ‏**‏זה הפער היחיד שאם לא ייפתר, ‏כל השאר לא בא לידי ביטוי.**

**‏שני כיוונים** (‏להכריע, ‏לא לשלב):
- ‏מרדכי נכנס ללולאת-סנטינל **‏בתוך אותו תור** ‏ואינו מסיים עד תוצאה.
- ‏או: ‏התבנית מחייבת אותו **‏למסור את ההמתנה במפורש** ‏למי ששיגר אותו, ‏במקום להניח שמישהו מסתכל.
**‏קובץ**: ‏`autonomous-runs/prompts/kickoff-mordechai.md` + ‏`agents/mordechai.md`.

## ‏פער 2 🟡 — ‏תנאי-הסיום תוקן **‏במסמך**, ‏לא במנגנון  ·  **‏תיקון נחת 27/08, ‏טרם אומת בריצה**

**‏הראיה**: ‏`acpx` ‏יצא `0` ‏אחרי `RetriableError: http/2 stream closed (CANCEL)`
‏באמצע C2. ‏הסנטינל רשם הצלחה; ‏העבודה הייתה חלקית ‏והעץ מלוכלך.

‏התיקון נחת ב-`MISSION_TEMPLATE §4` — ‏**‏שורה שמתכנן צריך לקרוא ולהחיל**. ‏אבל
‏הלקח של אותה ריצה עצמה היה "**‏ערנות אינה מנגנון**", ‏ושורה בתבנית היא ערנות-בהאצלה.

**‏התיקון המכני**: ‏`dispatch-agent` ‏לא כותב `0` ‏כשהשורה האחרונה בלוג היא שגיאה,
‏או כותב `<שם>.incomplete` ‏נוסף. ‏~10 ‏שורות בש.
**‏קובץ**: ‏`~/.local/bin/dispatch-agent` (‏ולשקף ל-`cli-configs/` ‏אם הוא מתוחזק שם).

### ‏✅ ‏התיקון ל-1+2 (27/08) — ‏`scripts/await-dispatch.sh`

‏**‏סקריפט אחד סוגר את שניהם**, ‏כי הם אותו כשל: ‏ההמתנה לא הוחזקה, ‏והסיום הוכרע
‏לפי התהליך במקום לפי התוצר.

```bash
scripts/await-dispatch.sh <‏שם-שיגור> --repo P --branch B --base R --expect-commits N
```

| ‏קוד | ‏משמעות |
|---|---|
| **0** | ‏הושלם **‏ואומת** — ‏קומיטים תואמים · ‏עץ נקי |
| **2** | ‏עדיין רץ (‏פג timeout ‏של הקריאה) — ‏**‏אינו כישלון**, ‏לקרוא שוב |
| **3** | ‏סנטינל ירה, ‏העבודה **‏חלקית** — 🔴 ‏אל תשגר מאמת |

**‏הורץ לפני המסירה** (‏`MISSION_TEMPLATE §8ה`), ‏שלושה מקרים:

```
‏א׳  ‏אין סנטינל                     → exit 2   ‏⏳ ‏עדיין רץ
‏ב׳  ‏סנטינל=0 + ‏קומיטים לא תואמים  → exit 3   ‏🔴 ‏חלקי — ‏אל תשגר מאמת
‏ג׳  ‏סנטינל=0 + ‏תואם + ‏עץ נקי      → exit 0   ‏✅ ‏הושלם ואומת
```

🔴 **‏מקרה ב׳ הוא בדיוק שחזור של ריצה 19**: ‏`acpx exit=0` ‏מול עבודה חלקית.
‏הסקריפט מדפיס במפורש "‏אות-חיוּת בלבד" ‏ואז מכריע לפי הארטיפקט.

**‏חוּוט**: ‏`agents/mordechai.md` ‏ו-`autonomous-runs/prompts/kickoff-mordechai.md` —
‏מרדכי **‏אינו מסיים תור** ‏אחרי `dispatch-agent`; ‏הוא קורא לסקריפט עד קוד שאינו 2.

⚠️ **‏הפריטים נשארים 🟡 ‏ולא ✅**: ‏לפי כלל-הסגירה בראש המסמך, ‏פריט נסגר רק
‏אחרי שהתיקון **‏אומת בריצה אחת לפחות**. ‏הריצה הבאה היא האימות.

## ‏פער 6 ✅ — ‏זרם-סרק ארוך מפיל את הסוכן — **‏נסגר 31/08, ‏אומת בארבע ריצות**

> **‏סגירה (31/08):** ‏התיקון (‏קיצור קפיצות-ההמתנה 570s→60s) ‏סומן למטה
> "‏השערה עם מנגנון, ‏לא מדידה" — ‏המדידה הצטברה: ‏ריצות **26–29** ‏רצו כולן
> ‏על Cursor ‏עם ההמתנה הקצרה, ‏ו**‏אפס מקרי מוות-זרם של סוכן**. ‏נבדק בדוחות:
> ‏ההתערבויות-צנרת שכן נרשמו בהן (27–29) ‏כולן ממשפחת ה*‏צופה* (notify ‏חסר /
> ‏צופה ad-hoc ‏שנפל), ‏לא ניתוקי-ספק של סוכן ממתין. ‏ההשוואה מול ריצה 20
> (‏שני מקרי-מוות של הממתין תחת 570s) ‏סוגרת את ההשערה.
> ‏מה שנשאר פתוח מהמשפחה הזו הוא ה**‏צופה** — ‏ר' פער 9.

**‏נמדד בריצה 20** (`session-create-contract`), ‏שלושה מקרי-מוות באותה ריצה:

```
‏אליעזר (‏ריצה 19)  RetriableError: [canceled]    http/2 stream closed CANCEL (0x8)
‏מרדכי-א׳           RetriableError: [canceled]    http/2 stream closed CANCEL (0x8)
‏מרדכי-ב׳           RetriableError: [unavailable] PING timed out
```

‏אלה **‏קודי gRPC** ‏מעל HTTP/2 ‏מול `api2.cursor.sh`. ‏`PING timed out` ‏= ‏keepalive
‏שלא נענה ⇒ ‏**‏הלקוח הרג את החיבור בעצמו**. ‏זרם ארוך-חיים שנשמט — ‏לא מכסה, ‏לא מודל.

🔴 **‏והשורש הוא התיקון לפער 1**: ‏`await-dispatch` ‏חסם עד **570s** ‏לקריאה. ‏בלוג של
‏מרדכי-א׳: ‏**‏שלוש** ‏קריאות שהחזירו "‏עדיין רץ" ⇒ ‏**‏שלושה חלונות של ~9.5 ‏דקות
‏ללא ולו טוקן אחד** ‏בזרם. ‏זה בדיוק התנאי ל-idle/ping timeout.

**‏ההשוואה שמחזקת**: ‏אליעזר — ‏פעיל ברציפות — ‏רץ **‏יותר זמן** ‏ולא מת בריצה 20.
‏מרדכי — ‏יושב בהמתנה — ‏מת **‏פעמיים**.

**‏התיקון (27/08)**: ‏ברירת-המחדל של `--timeout` ‏ירדה מ-**570** ‏ל-**60** ‏שניות.
‏ההמתנה עדיין מוחזקת — ‏אבל בקפיצות קצרות, ‏והזרם נשאר פעיל.
‏**‏מחיר**: ‏יותר סבבי-מודל. ‏**‏תמורה**: ‏זרם שלא נשמט.

⚠️ **‏זו השערה עם מנגנון, ‏לא מדידה.** ‏מדוד: ‏שלושה חלונות-סרק · ‏שני מקרי-מוות של
‏הממתין · ‏אפס של הפעיל · ‏`PING timed out`. ‏**‏לא מדוד**: ‏שסף-הניתוק אכן בין
‏60s ‏ל-570s. ‏הריצה הבאה היא הבדיקה.

⛔ ‏**‏ול-Cursor ‏אין לוג שמסביר את זה** (‏נבדק): ‏`worker.log` ‏= ‏LSP+טלמטריה בלבד;
‏`acp-sessions/*/store.db` ‏= ‏תוכן-שיחה בלבד. ‏השגיאה חיה **‏רק** ‏בפלט acpx.
‏מי שירצה יותר — ‏`acpx --verbose`.

### ‏🆕 ‏מסלול שני לפער 1: ‏שיגור דרך ה-API (27/08)

‏`scripts/dispatch-via-api.mjs` + ‏`scripts/lib/session-stream.mjs` — ‏מחזור-חיים מלא
‏מעל ה-API ‏של drive-coding: ‏`create → ‏טריגר-host → prompt → ‏המתנה → ‏סגירה`.

**‏מה שהוא פותר ו-tmux ‏לא**: ‏אות-הסיום הוא **‏`state_update` ‏עם `stopReason` ‏בזרם**,
‏ולא קוד-יציאה של תהליך ⇒ **‏אינו ניתן לזיוף** ‏באותה צורה שפער 2 ‏מתאר.
‏ו-**‏סגירת-הזרם היא אות בפני עצמה** (‏קוד 3) — ‏ל-`dispatch-agent` ‏אין מקבילה.

⚠️ ‏**‏אינו מחליף** ‏את `dispatch-agent`: ‏הוא דורש BE ‏חי עם `env`+`permissionPolicy`
(‏סלייס 59, ‏על `edge` ‏בלבד). ‏שני המסלולים חיים במקביל, ‏והבחירה מודעת.

#### ‏🆕 ‏שכבה שלישית: ‏**‏הילד מדווח בעצמו** (`notify`, ‏רעיון-המשתמש 27/08)

‏עד כה הילד היה **‏אילם**: ‏ההורה סקר זרם או סנטינל. ‏אבל אם הילד יודע את `agentId`
‏של ההורה, ‏הוא יכול **‏לדחוף** ‏לו הודעה — ‏ואין צורך להמציא ערוץ: ‏`session/prompt`
‏לסשן חי הוא בדיוק זה. `open --parent <id>` ‏מזריק `DC_PARENT`+`DC_BASE` ‏לסביבת
‏הילד; ‏קובצי-הסוכן מורים לו לשלוח `notify` ‏בסיום.

**‏נמדד 27/08 (cursor, ‏שני סוכנים אמיתיים)**: ‏הודעה שנדחפה לסוכן שספר 1→60
‏עצרה אותו ב-**57** ‏והוא השיב `GOT-MESSAGE` — ‏עם `state:running` ‏לפניה
‏ו-`end_turn` **‏יחיד** ‏אחריה ⇒ ‏נקלטה **‏באמצע ה-turn**, ‏לא בתור לסופו.
‏ואז דוגפוד מלא: ‏ילד עם `DC_PARENT` ‏הריץ `notify`, ‏וההורה קיבל והחל לפעול.
⚠️ ‏פר-ספק: ‏claude ‏דוחף מיָדית · ‏`pi` ‏ממתין לסוף-turn (§11 ‏ב-`pre-brief-session-bus`).

🔴 **‏מה שזה ‏*‏אינו*‏ ‏פותר**: ‏ילד שמת, ‏נתקע, ‏או סיים חלקית — ‏**‏לא ישלח כלום**.
‏`notify` ‏חוסך **‏המתנה**; ‏הוא אינו **‏אימות**, ‏ואינו מחליף את `await-dispatch.sh`
‏ואת קוד-היציאה. ‏שקט אינו הצלחה — ‏זה בדיוק הלקח של פער 2.

## ‏פער 3 🔴 — ‏**‏טענת-שער** ‏מדוח-מבצע (‏הורחב 27/08 — ‏הניסוח הקודם היה צר מדי)

**‏הראיה**: ‏אליעזר דיווח "271 ‏שגיאות lint ‏קיימות-מראש". ‏מדידה: ‏בסיס **270**,
‏עץ-הסלייס **268**, ‏ארבעת קבצי-הסלייס **0**. ‏המהות החזיקה; ‏המספר לא היה משחזר.
‏הוא נתפס **‏רק מפני שמרדכי מדד ביוזמתו**.

**‏התיקון**: ‏כלל מפורש — ‏**‏מספר שמופיע בשורת-DoD ‏נמדד מחדש ע"י המאמת;
‏מספר בפרוזה — ‏לא.** ‏בלי זה זו או פרנויה או עיוורון.
**‏קובץ**: ‏`agents/calev.md` + ‏`MISSION_TEMPLATE §8`.

### ‏🔴 ‏הרחבה (‏ריצה 20): ‏זה לא היה מספר — ‏זו הייתה **‏טענת-שער**

‏הניסוח המקורי אמר "‏מספר שמופיע בשורת-DoD ‏נמדד מחדש". ‏**‏צר מדי.** ‏בריצה 20:

- ‏אליעזר דיווח בסיכומו: ‏"linter, ‏typecheck ‏ו-lint:i18n ‏עברו". ‏**‏שקר** —
  ‏`bun run typecheck` ‏היה **‏אדום בעץ שלו עצמו**, ‏לפני כל מיזוג:
  ‏`TS2305: Module '@drive-coding/provider/client' has no exported member 'RequestPermissionRequest'`.
- ‏כלב ב-`mode: light` ‏סימן DoD 1-3 ‏כ-⬜ ‏עם ההסבר "‏לא הורץ מחדש; ‏handoff ‏מרדכי/אליעזר"
  ‏— ‏כלומר **‏סמך על הדיווח** ‏בדיוק בשורה שהייתה שקרית.
- ‏הכשל התגלה **‏רק במיזוג ל-`edge`**, ‏ע"י המתאם.

🔴 **‏ולמה זה שרד 107 ‏טסטים ירוקים**: ‏`vitest` ‏רץ דרך esbuild ‏ש**‏מפשיט טיפוסים**.
‏רק `tsc --build` ‏רואה. ‏זו הסיבה ש-`typecheck` ‏הוא **‏שורת-DoD ‏נפרדת** ‏ולא נחמד-שיהיה.

**‏הניסוח המתוקן:**

> **‏שורת-DoD ‏שהמאמת מסמן ✅ ‏על סמך דיווח-מבצע היא ⬜, ‏לא ✅.**
> ‏"‏עבר לפי handoff" ‏אינו שער. ‏אם המאמת לא הריץ — ‏הוא מסמן ⬜ ‏ומציין מי כן טוען שהריץ.

⚠️ ‏ובמבט מבני: ‏זה לא כשל של כלב. ‏מרדכי מת לפני האימות, ‏המתאם שיגר כלב ישירות
‏ב-light — ‏וכך **‏ויתרנו על השער המלא בלי שאיש החליט על כך**. ‏`mode: light` ‏חייב
‏להצהיר **‏מי כן מריץ** ‏את DoD 1-3, ‏ואם התשובה "‏אף אחד" — ‏זה ⬜.

## ‏פער 4 🟡 — ‏מספור נכתב ביד  ·  **‏תיקון נחת 31/08 (‏אותו סקריפט של פער 0), ‏טרם אומת בריצה**

> **‏התיקון (31/08)**: ‏המספור יושר חד-פעמית למספרים הקנוניים (‏כולל `9b`
> ‏ל-replay-quiet ‏ו-`23b`/`23c` ‏לריצות שחיו רק כפסקאות-README), ‏והסקריפט
> ‏מתריע 🔴 ‏על כל כפל-מספר. ‏שורה חדשה = frontmatter ‏בדוח, ‏לא עריכת טבלה.

**‏הראיה**: ‏הפנקס מתעד שלוש כפילויות (‏שתי "2", ‏שתי "6", ‏שתי "7"), ‏18 ‏שמור-עם-הערה,
‏ו-`BACKLOG.md` ‏של drive-coding ‏התנגש **‏פעמיים באותו יום** ‏תחת סוכנים מקבילים.

**‏התיקון**: ‏להיגזר, ‏לא להיכתב. ‏נופל יחד עם פער 0 — ‏אותו סקריפט.

## ‏פער 5 ⏸️ — ‏`pi` ‏תקוע ב-`starting`+404

**‏מוקפא בהכרעת-משתמש 27/08**: ‏הצורך הוא במנגנון **‏Gemini Live**, ‏לא בהפעלת `pi`.
‏נשאר רשום כ**‏מגבלת-כיסוי של השער החי** ‏בלבד — ‏לא באג לפתיחה ולא לחקירה.
‏(‏נצפה ע"י שני סוכנים עצמאיים; ‏codex · claude · opencode ‏עברו את אותם שערים על אותו BE.)

## ‏פער 7 🔴 — ‏🆕 ‏סשן שנפתח לשיגור **‏לא נסגר**, ‏ואיש אינו בעליו

**‏הראיה (27/08, ‏סוף יום)**: ‏שני סוכנים חיים על `:4050` — ‏אחד `cursor` ‏ב-`edge`
(‏אביגיל של ריצה 20, ‏סיימה ב-16:39) ‏ואחד `new-bridge-pi` ‏ב-`dev` (‏אינו של הריצה).
‏שניהם `busy:false` ‏שעות אחרי סיום התור. ‏אף ריצה לא סגרה אחריה.

**‏השורש אינו שכחה — ‏הוא חוסר-בעלות.** ‏`dispatch-via-api.mjs send` ‏סוגר
**‏רק בקוד 0**, ‏וזו הכרעה נכונה: ‏בכל קוד אחר הסוכן הוא **‏הראיה** ‏(‏מה הוא ראה,
‏איפה נתקע, ‏מה ב-`/state`) ‏ומחיקתו מוחקת אותה. ‏אבל אז אין שום שלב שבו הראיה
‏נחשבת "‏נאספה" ⇒ ‏הסוכן חי לנצח. ‏במסלול ה-tmux ‏המצב גרוע יותר: ‏אין סגירה
‏אוטומטית **‏בכלל** ("‏סגירה: ‏ידנית" ‏בטבלת שני-המסלולים).

**‏המחיר** ‏אינו תיאורטי: ‏כל סוכן חי מחזיק תהליך-CLI ‏(`pid` ‏ברשימה), ‏זיכרון,
‏וחיבור לספק. ‏במכונה שמריצה כמה סבבים ביום זו הצטברות.

**‏התיקון (‏נחת 27/08)** — ‏שלושה חלקים, ‏כי ההוראה לבדה כבר נכשלה:

1. **‏פקודה קנונית**: `dispatch-via-api.mjs close --agent <id>` — ‏אידמפוטנטית
   (‏סוכן שאינו ברשימה ⇒ `0`), ‏ו**‏מסרבת לסגור כש-`turnState != idle`** ‏בלי `--force`,
   ‏כדי שהכלל "‏אחרי שסיים לרוץ" ‏ייאכף במנגנון ולא בזיכרון.
   ⚠️ **‏תיקון-במקום**: ‏הגרסה הראשונה בדקה `busy` ‏מהרשימה — ‏שהוא **‏debounce-‏שקט
   ‏של 1.5ש'** (`turn-tracker.ts`) ‏ולא "turn ‏פתוח". ‏נמדד באותו ערב: ‏סוכן שספר
   ‏עם `sleep` ‏דיווח `busy:false` ‏באמצע ריצה. ‏השער היה כמעט-ריק; ‏עכשיו הוא נופל
   ‏בפועל (‏אומת: ‏`turnState=calling-tool` ⇒ ‏קוד 3).
2. **‏הודעת-הסקריפט בקוד≠0 ‏מדפיסה את פקודת-הסגירה עם ה-id ‏בפועל** —
   ‏"‏נשאר חי לבדיקה" ‏לבדו נקרא כהיתר-נטישה.
3. **‏חובת-סוגר ‏מפורשת** ‏ב-`agents/mordechai.md` ‏וב-`prompts/kickoff-mordechai.md`:
   ‏מי שפתח סוגר, ‏והסגירה היא **‏שורה בדוח-הריצה**, ‏לא ניקיון אופציונלי.

⚠️ **‏מה שהתיקון ‏*‏אינו*‏ ‏עושה**: ‏אין סורק-יתומים ואין TTL. ‏זו הימנעות מכוונת —
‏סגירה לפי גיל/דפוס תהרוג סוכנים של **‏סוכנים אחרים על אותה מכונה** (‏בדיוק
‏ה-`new-bridge-pi` ‏שלמעלה, ‏שאינו של הריצה). ‏הכלל נשאר: ‏סוגרים **‏לפי id ‏שפתחת**.

## ‏פער 8 🔴 — ‏🆕 ‏המבצע מדווח "‏בוצע" ‏והעבודה **‏לא מקומטת**

**‏הראיה (28/08, ‏פעמיים ברצף)**: ‏`agent-identity-mcp` ‏ו-`edge-red-fix`. ‏בשניהם
‏הדוח פירט טבלת-קומיטים מסודרת, ‏וב-`git log` ‏היו **‏אפס**. ‏העבודה עצמה הייתה
‏בעץ (13 ‏קבצים · 264 ‏שורות בראשון), ‏אבל לא ב-git.

**‏למה זה שונה מפער 3** (‏טענת-שער): ‏שם המבצע טוען שהריץ שער. ‏כאן הוא טוען
‏על **‏ארטיפקט שהוא עצמו יוצר** — ‏וזו הטענה הקלה ביותר לאמת ולכן החמורה ביותר
‏להחמיץ. ‏עלות: ‏עבודה שיושבת בעץ-עבודה **‏אובדת** ‏אם מישהו מנקה worktrees
(‏`worktrees:prune` ‏מסרב על עץ מלוכלך, ‏אבל `git worktree remove --force` ‏לא).

**‏התיקון (‏נדרש, ‏טרם נחת)**: ‏ההנחיה "‏קומיט לכל C" ‏קיימת ב-`eliezer.md` ‏ובכל
‏פרומפט-שיגור — ‏**‏וזה לא עזר, ‏פעמיים**. ‏טקסט שאוסר אינו אוכף. ‏מה שכן יאכוף:
‏המשגר בודק `git log <base>..HEAD` ‏**‏לפני** ‏שהוא מקבל דוח, ‏ומחזיר את הסוכן
‏להשלמה. ‏זה בדיוק מה ש-`await-dispatch.sh` ‏עושה במסלול tmux (‏הכרעה לפי
‏ארטיפקט) — ‏**‏ובמסלול ה-API ‏אין לו מקבילה**. ‏זו הפעם הראשונה שהפער הזה עולה,
‏כי במסלול ה-API ‏הסתמכנו על `notify` ‏מהילד, ‏שהוא **‏דיווח-עצמי**.

⇒ **‏הנגזרת הרחבה**: ‏`notify` ‏חוסך המתנה, ‏אבל **‏החליף בטעות גם את בדיקת-הארטיפקט**.
‏שתיהן נחוצות.

## ‏פער 9 🔴 — ‏🆕 ‏הצופה נכתב JIT ‏בכל ריצה, ‏וחוזר על באגים שכבר תועדו

**‏הראיה (‏שלוש ריצות רצופות, ‏אותה משפחה):**

| ‏ריצה | ‏מה קרה |
|---|---|
| 27 (`acp-wire`) | ‏צופה ראשון כתב לוג+`tg` ‏בלי `dispatch-via-api notify` — ‏המתאם לא התעורר. ‏חזרה מדויקת על לקח שתועד ב-basic-memory ‏יום קודם (30/08) |
| 28 (`acp-wire-http`) | ‏צופה ad-hoc **‏נפל** ‏אחרי `WATCH_START`; ‏הוחלף ב"‏עותק של תבנית הצופה המוכחת" — ‏תבנית שאין לה קובץ |
| 29 (`acp-wire-be-dedupe`) | "‏טיק חי ≠ ‏עובד" — ‏הצופה תיקתק בלי לבדוק הודעות/git/דוחות |

⇒ ‏קיימת "‏תבנית צופה מוכחת" — ‏אבל היא חיה **‏בזיכרון ובהעתקות**, ‏לא בקובץ.
‏כל ריצה משכתבת אותה, ‏וכל שכתוב מפיל חלק אחר. ‏זה בדיוק הדפוס שהמסמך הזה
‏עצמו מגדיר ככשל: **‏ערנות אינה מנגנון**, ‏ולקח בזיכרון אינו לקח בקוד.
‏שלוש ההתערבויות-צנרת האחרונות בפנקס — ‏כולן הפער הזה.

**‏התיקון (‏נחת 31/08)**: ‏סקריפט קנוני `scripts/watch-dispatch.sh` ‏לצד
`await-dispatch.sh` — ‏לולאת-צופה אחת שמגלמת את כל הלקחים: ‏קפיצות קצרות
(‏פער 6), ‏הכרעה לפי ארטיפקט ולא לפי טיק (‏ריצה 29), ‏גלאי-קיפאון לפי
‏גיל-ארטיפקט (‏החוב מריצה 14), ‏ו-`dispatch-via-api notify` ‏כערוץ-ההערה
‏היחיד (‏ריצה 27). ‏התבנית אוסרת צופה ad-hoc ‏כשהסקריפט מכסה את הצורך.
**‏קובץ**: ‏`scripts/watch-dispatch.sh` + ‏חיווט ב-`MISSION_TEMPLATE` ‏וב-`kickoff-mordechai`.
**‏בדיקת-סגירה**: ‏ריצה אחת שהצופה שלה הוא הסקריפט, ‏בלי התערבות-צנרת ממשפחת הצופה.

⚠️ **‏טווח ארוך (‏הכרעת-משתמש 31/08)**: ‏גלאי-הקיפאון שייך בסוף ל-**‏מוצר** —
‏ה-BE ‏של drive-coding ‏רואה turn ‏פתוח בלי תעבורה ויכול לדחוף אירוע "‏תקוע"
‏בעצמו (‏נרשם ב-roadmap). ‏הסקריפט הוא הגשר עד אז; ‏לא מרחיבים אותו מעבר לזה.

---

## ‏סדר-תקיפה מומלץ

**‏0 · 1 · 2 ‏הם אותה משפחה** — ‏שלושתם נשענים על כך שמישהו **‏יזכור**: ‏לרשום שורה,
‏להסתכל על הסנטינל, ‏לבדוק `git log`. ‏זה בדיוק מה שריצה 19 ‏קבעה שאינו עובד.

‏להתחיל ב-**‏1** ‏(‏הוא החוסם), ‏ואז **‏0+4** ‏(‏סקריפט אחד), ‏ואז **‏2**. ‏3 ‏הוא שורה בשני מסמכים.
